{
  "_governance": {
    "owner": "runtime-client-foundation",
    "reason": "UI Map<String,dynamic> literals may only decrease",
    "expires_when": "max_ui_map_string_dynamic_occurrences reaches zero",
    "measure": "counts `Map<String, dynamic>` occurrences under lib/service/**/presentation/**, lib/runtime/shell/** and lib/design_system/**, excluding *.g.dart. A missing scan root is a BLOCK, never a zero count.",
    "superseded_measure": "The prior measure scanned lib/ui only. That directory disappeared when UI moved into service/presentation, so the count silently fell to 0 and the budget of 4 read green while the debt was untouched. Re-measured at 69 on the day of the switch."
  },
  "max_ui_map_string_dynamic_occurrences": 21,
  "_comment": "Regenerated by verify_ui_map_literal_budget.py --write-baseline; do not increase without remediation plan."
}
