#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$ROOT"
# Full local-contract shards belong to CI Delivery Gate.  Keep commits inside
# the repository's L0 budget and run the same entrypoint for every scope.
bash quwoquan_ops/gate/commit_gate.sh
