# fortune_astrology 工具调用指引

## 调用顺序
1. 先用已有上下文和记录记忆判断是否足够回答
2. 仅当用户明确需要实时黄历、节气或天象信息时调用 `web_search`
3. 若需要位置信息，只允许读取系统默认注入的上下文摘要

## 重试策略
- 外部检索失败最多重试 1 次
- 重试后仍失败则降级为参考性答复

## 降级要求
- 明确说明当前外部信息不足
- 保留启发式、陪伴式表达
- 不输出确定性预言
