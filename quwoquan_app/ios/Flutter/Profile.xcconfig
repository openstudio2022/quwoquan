#include "Generated.xcconfig"

// 明确设置 SDK 路径，避免 native_assets 构建错误
SDKROOT = iphoneos
SUPPORTED_PLATFORMS = iphoneos iphonesimulator
QWQ_APS_ENVIRONMENT = production

// Flutter tooling compatibility config. Flavored Runner targets use
// Base/Profile.xcconfig and select their exact dependency graph in the wrapper.
