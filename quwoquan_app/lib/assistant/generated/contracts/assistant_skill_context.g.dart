// Code generated by tools/codegen_app_metadata from assistant/assistant_skill_context/schema.yaml. DO NOT EDIT.

// ignore_for_file: avoid_classes_with_only_static_members

import 'package:quwoquan_app/assistant/contracts/runtime_enums.dart';

class AssistantContextRequirementWire {
  const AssistantContextRequirementWire({
    required this.slotId,
    this.required = false,
    this.acceptedSourceKinds = const <String>[],
    required this.authority,
    required this.sensitivity,
    this.consentScopes = const <String>[],
    this.freshnessSeconds = 0,
    this.tokenBudget = 0,
    required this.resolverRef,
    this.fallbackPolicy = "clarify",
  });

  final String slotId;
  final bool required;
  final List<String> acceptedSourceKinds;
  final AssistantContextAuthority authority;
  final AssistantContextSensitivity sensitivity;
  final List<String> consentScopes;
  final int freshnessSeconds;
  final int tokenBudget;
  final String resolverRef;
  final String fallbackPolicy;

  Map<String, dynamic> toJson() => <String, dynamic>{
        'slotId': slotId,
        'required': required,
        'acceptedSourceKinds': acceptedSourceKinds,
        'authority': authority.wireName,
        'sensitivity': sensitivity.wireName,
        'consentScopes': consentScopes,
        'freshnessSeconds': freshnessSeconds,
        'tokenBudget': tokenBudget,
        'resolverRef': resolverRef,
        'fallbackPolicy': fallbackPolicy,
      };

  factory AssistantContextRequirementWire.fromJson(Map<String, dynamic> json) {
    return AssistantContextRequirementWire(
      slotId: (json['slotId'] as String?)?.trim() ?? "",
      required: json['required'] == true,
      acceptedSourceKinds: _assistantStringList(json['acceptedSourceKinds']),
      authority: parseAssistantContextAuthorityStrict((json['authority'] as String?)?.trim() ?? ''),
      sensitivity: parseAssistantContextSensitivityStrict((json['sensitivity'] as String?)?.trim() ?? ''),
      consentScopes: _assistantStringList(json['consentScopes']),
      freshnessSeconds: (json['freshnessSeconds'] as num?)?.toInt() ?? 0,
      tokenBudget: (json['tokenBudget'] as num?)?.toInt() ?? 0,
      resolverRef: (json['resolverRef'] as String?)?.trim() ?? "",
      fallbackPolicy: (json['fallbackPolicy'] as String?)?.trim() ?? "clarify",
    );
  }

  static List<String> _assistantStringList(Object? value) {
    if (value is List) {
      return value.map((item) => item.toString().trim()).where((item) => item.isNotEmpty).toList(growable: false);
    }
    return const <String>[];
  }
}

class AssistantContextRequirementWireFields {
  static const String slotId = 'slotId';
  static const String required = 'required';
  static const String acceptedSourceKinds = 'acceptedSourceKinds';
  static const String authority = 'authority';
  static const String sensitivity = 'sensitivity';
  static const String consentScopes = 'consentScopes';
  static const String freshnessSeconds = 'freshnessSeconds';
  static const String tokenBudget = 'tokenBudget';
  static const String resolverRef = 'resolverRef';
  static const String fallbackPolicy = 'fallbackPolicy';
}

class AssistantContextProfileWire {
  const AssistantContextProfileWire({
    required this.profileId,
    this.requirements = const <AssistantContextRequirementWire>[],
    required this.assetDigest,
  });

  final String profileId;
  final List<AssistantContextRequirementWire> requirements;
  final String assetDigest;

  Map<String, dynamic> toJson() => <String, dynamic>{
        'profileId': profileId,
        'requirements': requirements.map((item) => item.toJson()).toList(growable: false),
        'assetDigest': assetDigest,
      };

  factory AssistantContextProfileWire.fromJson(Map<String, dynamic> json) {
    return AssistantContextProfileWire(
      profileId: (json['profileId'] as String?)?.trim() ?? "",
      requirements: (json['requirements'] as List?)?.whereType<Map>().map((item) => AssistantContextRequirementWire.fromJson(item.cast<String, dynamic>())).toList(growable: false) ?? const <AssistantContextRequirementWire>[],
      assetDigest: (json['assetDigest'] as String?)?.trim() ?? "",
    );
  }
}

class AssistantContextProfileWireFields {
  static const String profileId = 'profileId';
  static const String requirements = 'requirements';
  static const String assetDigest = 'assetDigest';
}
