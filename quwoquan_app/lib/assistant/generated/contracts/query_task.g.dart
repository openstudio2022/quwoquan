// Code generated by tools/codegen_app_metadata from assistant/query_task/schema.yaml. DO NOT EDIT.
// ignore_for_file: prefer_const_constructors

import 'package:quwoquan_app/assistant/generated/enums/assistant_runtime_enums.g.dart';

enum QueryTaskDimension {
  currentState,
  decisionImpact,
  candidateSpace,
  tradeoffs,
  fitConstraints,
  fitScenarios,
  riskBoundaries,
  keyFacts,
  decisionThreshold,
  coreObject,
  supportingEvidence,
  latestSignal,
  unknown,
}

QueryTaskDimension parseQueryTaskDimension(String raw) {
  switch (raw.trim()) {
    case "current_state":
    case "当前状态":
      return QueryTaskDimension.currentState;
    case "decision_impact":
    case "决策影响":
      return QueryTaskDimension.decisionImpact;
    case "candidate_space":
    case "候选范围":
    case "候选方案":
      return QueryTaskDimension.candidateSpace;
    case "tradeoffs":
    case "关键取舍":
      return QueryTaskDimension.tradeoffs;
    case "fit_constraints":
    case "适配条件":
      return QueryTaskDimension.fitConstraints;
    case "fit_scenarios":
    case "适用场景":
      return QueryTaskDimension.fitScenarios;
    case "risk_boundaries":
    case "风险边界":
      return QueryTaskDimension.riskBoundaries;
    case "key_facts":
    case "关键事实":
      return QueryTaskDimension.keyFacts;
    case "decision_threshold":
    case "判断条件":
      return QueryTaskDimension.decisionThreshold;
    case "core_object":
    case "核心对象":
      return QueryTaskDimension.coreObject;
    case "supporting_evidence":
    case "支撑依据":
      return QueryTaskDimension.supportingEvidence;
    case "latest_signal":
    case "最新变化":
      return QueryTaskDimension.latestSignal;
    default:
      return QueryTaskDimension.unknown;
  }
}

extension QueryTaskDimensionX on QueryTaskDimension {
  String get wireName {
    switch (this) {
      case QueryTaskDimension.currentState:
        return "current_state";
      case QueryTaskDimension.decisionImpact:
        return "decision_impact";
      case QueryTaskDimension.candidateSpace:
        return "candidate_space";
      case QueryTaskDimension.tradeoffs:
        return "tradeoffs";
      case QueryTaskDimension.fitConstraints:
        return "fit_constraints";
      case QueryTaskDimension.fitScenarios:
        return "fit_scenarios";
      case QueryTaskDimension.riskBoundaries:
        return "risk_boundaries";
      case QueryTaskDimension.keyFacts:
        return "key_facts";
      case QueryTaskDimension.decisionThreshold:
        return "decision_threshold";
      case QueryTaskDimension.coreObject:
        return "core_object";
      case QueryTaskDimension.supportingEvidence:
        return "supporting_evidence";
      case QueryTaskDimension.latestSignal:
        return "latest_signal";
      case QueryTaskDimension.unknown:
        return "";
    }
  }

  String get displayLabel {
    switch (this) {
      case QueryTaskDimension.currentState:
        return "当前状态";
      case QueryTaskDimension.decisionImpact:
        return "决策影响";
      case QueryTaskDimension.candidateSpace:
        return "候选范围";
      case QueryTaskDimension.tradeoffs:
        return "关键取舍";
      case QueryTaskDimension.fitConstraints:
        return "适配条件";
      case QueryTaskDimension.fitScenarios:
        return "适用场景";
      case QueryTaskDimension.riskBoundaries:
        return "风险边界";
      case QueryTaskDimension.keyFacts:
        return "关键事实";
      case QueryTaskDimension.decisionThreshold:
        return "判断条件";
      case QueryTaskDimension.coreObject:
        return "核心对象";
      case QueryTaskDimension.supportingEvidence:
        return "支撑依据";
      case QueryTaskDimension.latestSignal:
        return "最新变化";
      case QueryTaskDimension.unknown:
        return "";
    }
  }
}

class QueryTask {
  const QueryTask({
    required this.id,
    required this.query,
    this.label = '',
    this.dimension = QueryTaskDimension.unknown,
    this.entityAnchors = const <String>[],
    this.negativeKeywords = const <String>[],
    this.answerShape = AnswerShape.unspecified,
    this.freshnessNeed = FreshnessNeed.unspecified,
  });

  final String id;
  final String query;
  final String label;
  final QueryTaskDimension dimension;
  final List<String> entityAnchors;
  final List<String> negativeKeywords;
  final AnswerShape answerShape;
  final FreshnessNeed freshnessNeed;

  String get effectiveLabel =>
      label.trim().isNotEmpty ? label.trim() : dimension.displayLabel;

  String get dimensionCode => dimension.wireName;

  String get dimensionLabel => dimension.displayLabel;

  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'query': query,
        'label': effectiveLabel.isNotEmpty ? effectiveLabel : query,
        if (dimension != QueryTaskDimension.unknown) 'dimension': dimension.wireName,
        if (dimension.displayLabel.isNotEmpty) 'dimensionLabel': dimension.displayLabel,
        if (entityAnchors.isNotEmpty) 'entityAnchors': entityAnchors,
        if (negativeKeywords.isNotEmpty) 'negativeKeywords': negativeKeywords,
        if (answerShape != AnswerShape.unspecified)
          'answerShape': answerShape.wireName,
        if (freshnessNeed != FreshnessNeed.unspecified)
          'freshnessNeed': freshnessNeed.wireName,
      };

  factory QueryTask.fromJson(Map<String, dynamic> json) {
    final query = (json['query'] as String?)?.trim() ?? '';
    final label = (json['label'] as String?)?.trim() ?? '';
    final dimension = parseQueryTaskDimension(
      (json['dimension'] as String?)?.trim().isNotEmpty == true
          ? (json['dimension'] as String).trim()
          : (json['dimensionLabel'] as String?)?.trim() ?? label,
    );
    return QueryTask(
      id: (json['id'] as String?)?.trim().isNotEmpty == true
          ? (json['id'] as String).trim()
          : _normalizeTaskId(
              query,
              preferred: label.isNotEmpty ? label : dimension.displayLabel,
            ),
      query: query,
      label: label,
      dimension: dimension,
      entityAnchors: _stringList(json['entityAnchors']),
      negativeKeywords: _stringList(json['negativeKeywords']),
      answerShape: parseAnswerShape((json['answerShape'] as String?)?.trim() ?? ''),
      freshnessNeed: parseFreshnessNeed(
        (json['freshnessNeed'] as String?)?.trim() ?? '',
      ),
    );
  }

  QueryTask copyWith({
    String? id,
    String? query,
    String? label,
    QueryTaskDimension? dimension,
    List<String>? entityAnchors,
    List<String>? negativeKeywords,
    AnswerShape? answerShape,
    FreshnessNeed? freshnessNeed,
  }) {
    return QueryTask(
      id: id ?? this.id,
      query: query ?? this.query,
      label: label ?? this.label,
      dimension: dimension ?? this.dimension,
      entityAnchors: entityAnchors ?? this.entityAnchors,
      negativeKeywords: negativeKeywords ?? this.negativeKeywords,
      answerShape: answerShape ?? this.answerShape,
      freshnessNeed: freshnessNeed ?? this.freshnessNeed,
    );
  }

  static List<QueryTask> normalizeList(Object? raw) {
    final items = raw is List
        ? raw.whereType<Map>().map((item) => item.cast<String, dynamic>())
        : const Iterable<Map<String, dynamic>>.empty();
    final normalized = <QueryTask>[];
    final seen = <String>{};
    for (final item in items) {
      final task = QueryTask.fromJson(item);
      final query = task.query.trim();
      if (query.isEmpty || !seen.add(query)) continue;
      normalized.add(task);
    }
    return normalized;
  }

  static List<Map<String, dynamic>> toJsonList(Iterable<QueryTask> tasks) =>
      tasks.map((item) => item.toJson()).toList(growable: false);

  static List<String> _stringList(Object? value) {
    if (value is List) {
      return value
          .map((item) => item.toString().trim())
          .where((item) => item.isNotEmpty)
          .toList(growable: false);
    }
    return const <String>[];
  }

  static String _normalizeTaskId(String query, {String preferred = ''}) {
    final seed = preferred.trim().isNotEmpty ? preferred.trim() : query.trim();
    final normalized = seed
        .toLowerCase()
        .replaceAll(RegExp(r'[\\s\-]+'), '_')
        .replaceAll(RegExp(r'[^a-z0-9_\u4e00-\u9fff]'), '')
        .replaceAll(RegExp(r'_+'), '_')
        .replaceAll(RegExp(r'^_|_$'), '');
    return normalized.isNotEmpty ? normalized : 'query_task';
  }
}
