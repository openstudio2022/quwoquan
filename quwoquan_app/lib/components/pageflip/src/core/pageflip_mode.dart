enum PageflipMode {
  single,
  spread,
}

enum PageflipDirection {
  forward,
  back,
}

enum PageflipCorner {
  top,
  bottom,
}

enum PageflipStateKind {
  read,
  dragging,
  settling,
  completed,
}
