/// 创作排序模式
enum CreationSortMode { latest, hot, featured }

/// 创作视图模式
enum CreationViewMode { grid, list }

/// 圈子作品区的内容形态筛选；不复用 Persona 页面 presentation 状态。
enum CircleCreationSubTab { all, image, video, article }

/// 圈子作品区的展示格式筛选。
enum CircleCreationWorkFormat { all, image, video, note }
