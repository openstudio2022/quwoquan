// Code generated by tools/codegen_app_metadata from recommendation/model_release/projections/intersection_dimension_tally.yaml. DO NOT EDIT.
// ignore_for_file: prefer_const_constructors, unnecessary_null_in_if_null_operators

import 'intersection_text_span.g.dart';
import 'intersection_visual.g.dart';

class IntersectionDimensionTally {
  final String dimension;
  final String label;
  final int count;
  final int newCount;
  final String briefText;
  final String subtitleText;
  final List<IntersectionTextSpan> briefSpans;
  final List<IntersectionVisual> sampleVisuals;
  final String sourceRef;
  final String countObjectKind;
  final int strengthenedCount;
  final int reactivatedCount;
  final String iconKey;

  IntersectionDimensionTally({
    this.dimension = '',
    this.label = '',
    this.count = 0,
    this.newCount = 0,
    this.briefText = '',
    this.subtitleText = '',
    this.briefSpans = const <IntersectionTextSpan>[],
    this.sampleVisuals = const <IntersectionVisual>[],
    this.sourceRef = '',
    this.countObjectKind = '',
    this.strengthenedCount = 0,
    this.reactivatedCount = 0,
    this.iconKey = '',
  });

  factory IntersectionDimensionTally.fromMap(Map<String, dynamic> m) {
    return IntersectionDimensionTally(
      dimension: m['dimension']?.toString() ?? '',
      label: m['label']?.toString() ?? '',
      count: (m['count'] as num?)?.toInt() ?? 0,
      newCount: (m['newCount'] as num?)?.toInt() ?? 0,
      briefText: m['briefText']?.toString() ?? '',
      subtitleText: m['subtitleText']?.toString() ?? '',
      briefSpans: _parseProjectionDtoList(m['briefSpans'], IntersectionTextSpan.fromMap),
      sampleVisuals: _parseProjectionDtoList(m['sampleVisuals'], IntersectionVisual.fromMap),
      sourceRef: m['sourceRef']?.toString() ?? '',
      countObjectKind: m['countObjectKind']?.toString() ?? '',
      strengthenedCount: (m['strengthenedCount'] as num?)?.toInt() ?? 0,
      reactivatedCount: (m['reactivatedCount'] as num?)?.toInt() ?? 0,
      iconKey: m['iconKey']?.toString() ?? '',
    );
  }

  Map<String, dynamic> toMap() {
    return <String, dynamic>{
      'dimension': dimension,
      'label': label,
      'count': count,
      'newCount': newCount,
      'briefText': briefText,
      'subtitleText': subtitleText,
      'briefSpans': briefSpans,
      'sampleVisuals': sampleVisuals,
      'sourceRef': sourceRef,
      'countObjectKind': countObjectKind,
      'strengthenedCount': strengthenedCount,
      'reactivatedCount': reactivatedCount,
      'iconKey': iconKey,
    };
  }

  IntersectionDimensionTally copyWith({
    String? dimension,
    String? label,
    int? count,
    int? newCount,
    String? briefText,
    String? subtitleText,
    List<IntersectionTextSpan>? briefSpans,
    List<IntersectionVisual>? sampleVisuals,
    String? sourceRef,
    String? countObjectKind,
    int? strengthenedCount,
    int? reactivatedCount,
    String? iconKey,
  }) {
    return IntersectionDimensionTally(
      dimension: dimension ?? this.dimension,
      label: label ?? this.label,
      count: count ?? this.count,
      newCount: newCount ?? this.newCount,
      briefText: briefText ?? this.briefText,
      subtitleText: subtitleText ?? this.subtitleText,
      briefSpans: briefSpans ?? this.briefSpans,
      sampleVisuals: sampleVisuals ?? this.sampleVisuals,
      sourceRef: sourceRef ?? this.sourceRef,
      countObjectKind: countObjectKind ?? this.countObjectKind,
      strengthenedCount: strengthenedCount ?? this.strengthenedCount,
      reactivatedCount: reactivatedCount ?? this.reactivatedCount,
      iconKey: iconKey ?? this.iconKey,
    );
  }
}

List<T> _parseProjectionDtoList<T>(
  Object? v,
  T Function(Map<String, dynamic> m) fromMap,
) {
  if (v == null) return List<T>.empty(growable: false);
  if (v is! List) return List<T>.empty(growable: false);
  final out = <T>[];
  for (final e in v) {
    if (e is Map<String, dynamic>) {
      out.add(fromMap(e));
    } else if (e is Map) {
      out.add(fromMap(Map<String, dynamic>.from(e)));
    }
  }
  return out;
}
