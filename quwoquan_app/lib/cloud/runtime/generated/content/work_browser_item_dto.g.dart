// Code generated by tools/codegen_app_metadata from content/post/projections/work_browser_item.yaml. DO NOT EDIT.
// ignore_for_file: prefer_const_constructors, unnecessary_null_in_if_null_operators

import 'package:quwoquan_app/cloud/runtime/generated/recommendation/intersection_reason.g.dart';
import 'article_entity_mention_dto.g.dart';
import 'work_browser_media_item_dto.g.dart';

class WorkBrowserItemDto {
  final String workId;
  final String workType;
  final String workIdentity;
  final String? title;
  final String? caption;
  final String authorId;
  final String authorName;
  final String authorAvatarUrl;
  final String? authorBadge;
  final int likeCount;
  final int commentCount;
  final int shareCount;
  final String coverUrl;
  final List<String> imageUrls;
  final String? videoUrl;
  final int? durationMs;
  final int? width;
  final int? height;
  final List<WorkBrowserMediaItemDto> mediaItems;
  final String? articleMarkdown;
  final String? articleTemplate;
  final String? articleFontPreset;
  final Map<String, dynamic>? articleRenderProfile;
  final String? contentVertical;
  final String? paperThemeMode;
  final String? paperTexture;
  final List<ArticleEntityMentionDto> entityMentions;
  final List<IntersectionReason>? intersectionReasons;

  WorkBrowserItemDto({
    this.workId = '',
    this.workType = '',
    this.workIdentity = 'work',
    this.title,
    this.caption,
    this.authorId = '',
    this.authorName = '',
    this.authorAvatarUrl = '',
    this.authorBadge,
    this.likeCount = 0,
    this.commentCount = 0,
    this.shareCount = 0,
    this.coverUrl = '',
    this.imageUrls = const <String>[],
    this.videoUrl,
    this.durationMs,
    this.width,
    this.height,
    this.mediaItems = const <WorkBrowserMediaItemDto>[],
    this.articleMarkdown,
    this.articleTemplate,
    this.articleFontPreset,
    this.articleRenderProfile,
    this.contentVertical,
    this.paperThemeMode,
    this.paperTexture,
    this.entityMentions = const <ArticleEntityMentionDto>[],
    this.intersectionReasons,
  });

  factory WorkBrowserItemDto.fromMap(Map<String, dynamic> m) {
    return WorkBrowserItemDto(
      workId: m['postId']?.toString() ?? m['id']?.toString() ?? m['_id']?.toString() ?? '',
      workType: m['contentType']?.toString() ?? m['type']?.toString() ?? m['category']?.toString() ?? '',
      workIdentity: m['contentIdentity']?.toString() ?? m['identity']?.toString() ?? 'work',
      title: m['title']?.toString() ?? null,
      caption: m['body']?.toString() ?? m['description']?.toString() ?? m['content']?.toString() ?? m['caption']?.toString() ?? null,
      authorId: m['authorId']?.toString() ?? m['userId']?.toString() ?? m['author_id']?.toString() ?? '',
      authorName: m['authorNickname']?.toString() ?? m['displayName']?.toString() ?? m['authorDisplayNameSnapshot']?.toString() ?? m['nickname']?.toString() ?? m['username']?.toString() ?? '',
      authorAvatarUrl: m['authorAvatarUrl']?.toString() ?? m['avatarUrl']?.toString() ?? m['authorAvatarUrlSnapshot']?.toString() ?? m['avatar']?.toString() ?? '',
      authorBadge: m['badge']?.toString() ?? m['authorBadge']?.toString() ?? null,
      likeCount: (m['likeCount'] as num?)?.toInt() ?? (m['likesCount'] as num?)?.toInt() ?? (m['like_count'] as num?)?.toInt() ?? 0,
      commentCount: (m['commentCount'] as num?)?.toInt() ?? (m['commentsCount'] as num?)?.toInt() ?? (m['comment_count'] as num?)?.toInt() ?? 0,
      shareCount: (m['shareCount'] as num?)?.toInt() ?? (m['shares'] as num?)?.toInt() ?? (m['share_count'] as num?)?.toInt() ?? 0,
      coverUrl: m['coverUrl']?.toString() ?? m['cover']?.toString() ?? m['thumbnail']?.toString() ?? m['thumbnailUrl']?.toString() ?? '',
      imageUrls: _parseStringList(m['mediaUrls']) ?? _parseStringList(m['imageUrls']) ?? _parseStringList(m['images']) ?? _parseStringList(m['image_urls']) ?? <String>[],
      videoUrl: m['videoUrl']?.toString() ?? m['video_url']?.toString() ?? null,
      durationMs: (m['durationMs'] as num?)?.toInt() ?? (m['duration_ms'] as num?)?.toInt() ?? (m['duration'] as num?)?.toInt() ?? null,
      width: (m['width'] as num?)?.toInt() ?? (m['imageWidth'] as num?)?.toInt() ?? (m['w'] as num?)?.toInt() ?? null,
      height: (m['height'] as num?)?.toInt() ?? (m['imageHeight'] as num?)?.toInt() ?? (m['h'] as num?)?.toInt() ?? null,
      mediaItems: _parseProjectionDtoList(m['mediaItems'], WorkBrowserMediaItemDto.fromMap),
      articleMarkdown: m['articleMarkdown']?.toString() ?? null,
      articleTemplate: m['articleTemplate']?.toString() ?? null,
      articleFontPreset: m['articleFontPreset']?.toString() ?? null,
      articleRenderProfile: _parseStringKeyMap(m['articleRenderProfile']) ?? null,
      contentVertical: m['contentVertical']?.toString() ?? m['categoryId']?.toString() ?? m['category']?.toString() ?? m['vertical']?.toString() ?? null,
      paperThemeMode: m['paperThemeMode']?.toString() ?? null,
      paperTexture: m['paperTexture']?.toString() ?? m['articlePaperTexture']?.toString() ?? null,
      entityMentions: _parseProjectionDtoList(m['entityMentions'], ArticleEntityMentionDto.fromMap),
      intersectionReasons: m['intersectionReasons'] == null ? null : _parseProjectionDtoList(m['intersectionReasons'], IntersectionReason.fromMap),
    );
  }

  Map<String, dynamic> toMap() {
    return <String, dynamic>{
      'workId': workId,
      'workType': workType,
      'workIdentity': workIdentity,
      'title': title,
      'caption': caption,
      'authorId': authorId,
      'authorName': authorName,
      'authorAvatarUrl': authorAvatarUrl,
      'authorBadge': authorBadge,
      'likeCount': likeCount,
      'commentCount': commentCount,
      'shareCount': shareCount,
      'coverUrl': coverUrl,
      'imageUrls': imageUrls,
      'videoUrl': videoUrl,
      'durationMs': durationMs,
      'width': width,
      'height': height,
      'mediaItems': mediaItems,
      'articleMarkdown': articleMarkdown,
      'articleTemplate': articleTemplate,
      'articleFontPreset': articleFontPreset,
      'articleRenderProfile': articleRenderProfile,
      'contentVertical': contentVertical,
      'paperThemeMode': paperThemeMode,
      'paperTexture': paperTexture,
      'entityMentions': entityMentions,
      'intersectionReasons': intersectionReasons,
    };
  }

  WorkBrowserItemDto copyWith({
    String? workId,
    String? workType,
    String? workIdentity,
    String? title,
    String? caption,
    String? authorId,
    String? authorName,
    String? authorAvatarUrl,
    String? authorBadge,
    int? likeCount,
    int? commentCount,
    int? shareCount,
    String? coverUrl,
    List<String>? imageUrls,
    String? videoUrl,
    int? durationMs,
    int? width,
    int? height,
    List<WorkBrowserMediaItemDto>? mediaItems,
    String? articleMarkdown,
    String? articleTemplate,
    String? articleFontPreset,
    Map<String, dynamic>? articleRenderProfile,
    String? contentVertical,
    String? paperThemeMode,
    String? paperTexture,
    List<ArticleEntityMentionDto>? entityMentions,
    List<IntersectionReason>? intersectionReasons,
  }) {
    return WorkBrowserItemDto(
      workId: workId ?? this.workId,
      workType: workType ?? this.workType,
      workIdentity: workIdentity ?? this.workIdentity,
      title: title ?? this.title,
      caption: caption ?? this.caption,
      authorId: authorId ?? this.authorId,
      authorName: authorName ?? this.authorName,
      authorAvatarUrl: authorAvatarUrl ?? this.authorAvatarUrl,
      authorBadge: authorBadge ?? this.authorBadge,
      likeCount: likeCount ?? this.likeCount,
      commentCount: commentCount ?? this.commentCount,
      shareCount: shareCount ?? this.shareCount,
      coverUrl: coverUrl ?? this.coverUrl,
      imageUrls: imageUrls ?? this.imageUrls,
      videoUrl: videoUrl ?? this.videoUrl,
      durationMs: durationMs ?? this.durationMs,
      width: width ?? this.width,
      height: height ?? this.height,
      mediaItems: mediaItems ?? this.mediaItems,
      articleMarkdown: articleMarkdown ?? this.articleMarkdown,
      articleTemplate: articleTemplate ?? this.articleTemplate,
      articleFontPreset: articleFontPreset ?? this.articleFontPreset,
      articleRenderProfile: articleRenderProfile ?? this.articleRenderProfile,
      contentVertical: contentVertical ?? this.contentVertical,
      paperThemeMode: paperThemeMode ?? this.paperThemeMode,
      paperTexture: paperTexture ?? this.paperTexture,
      entityMentions: entityMentions ?? this.entityMentions,
      intersectionReasons: intersectionReasons ?? this.intersectionReasons,
    );
  }

  /// 作品类型闭集归一：image / video / article / micro（moment 别名归 micro）。
  String get normalizedWorkType {
    if (workType == 'video' || (videoUrl != null && videoUrl!.isNotEmpty)) return 'video';
    if (workType == 'article' || workType == 'note') return 'article';
    if (workType == 'micro' || workType == 'moment') return 'micro';
    return 'image';
  }
  /// 视频集序列：mediaItems[kind=video]；为空时由 videoUrl 派生单元素序列。
  List<WorkBrowserMediaItemDto> get videoItems {
    final fromContract = mediaItems
    .where((item) => item.kind == 'video' && item.url.isNotEmpty)
    .toList(growable: false);
    if (fromContract.isNotEmpty) return fromContract;
    final fallback = videoUrl;
    if (fallback == null || fallback.isEmpty) return const <WorkBrowserMediaItemDto>[];
    return <WorkBrowserMediaItemDto>[
    WorkBrowserMediaItemDto(
    kind: 'video',
    url: fallback,
    coverUrl: coverUrl.isEmpty ? null : coverUrl,
    durationMs: durationMs,
    width: width,
    height: height,
    title: null,
    ),
    ];
  }
  /// 图片序列：mediaItems[kind=image] 的 url；为空时回落 imageUrls。
  List<String> get effectiveImageUrls {
    final fromContract = mediaItems
    .where((item) => item.kind == 'image' && item.url.isNotEmpty)
    .map((item) => item.url)
    .toList(growable: false);
    if (fromContract.isNotEmpty) return fromContract;
    return imageUrls;
  }
  /// 作者区交集摘要数字（同一 reasons 列表派生，禁止第二事实来源）。
  int get intersectionCount {
    return intersectionReasons?.length ?? 0;
  }
}

List<String>? _parseStringList(dynamic v) {
  if (v == null) return null;
  if (v is List) return v.map((e) => e?.toString() ?? '').toList();
  return null;
}
List<T> _parseProjectionDtoList<T>(
  Object? v,
  T Function(Map<String, dynamic> m) fromMap,
) {
  if (v == null) return List<T>.empty(growable: false);
  if (v is! List) return List<T>.empty(growable: false);
  final out = <T>[];
  for (final e in v) {
    if (e is Map<String, dynamic>) {
      out.add(fromMap(e));
    } else if (e is Map) {
      out.add(fromMap(Map<String, dynamic>.from(e)));
    }
  }
  return out;
}


Map<String, dynamic>? _parseStringKeyMap(dynamic v) {
  if (v == null) return null;
  if (v is Map<String, dynamic>) return v;
  if (v is Map) {
    return Map<String, dynamic>.from(
      v.map((k, val) => MapEntry(k.toString(), val)),
    );
  }
  return null;
}
