// Code generated by tools/codegen_app_metadata from content/content/post/projections/author_impact_evidence_item.yaml. DO NOT EDIT.
// ignore_for_file: prefer_const_constructors, unnecessary_null_in_if_null_operators

import '../recommendation/intersection_visual.g.dart';
import '../recommendation/intersection_target.g.dart';
import '../recommendation/intersection_representative_actor.g.dart';
import '../recommendation/intersection_action_hint.g.dart';

class AuthorImpactEvidenceItem {
  final String evidenceId;
  final String impactId;
  final String helpType;
  final String action;
  final String intersectionDimension;
  final String occurredAt;
  final String summaryText;
  final IntersectionVisual? sampleVisual;
  final IntersectionRepresentativeActor? representativeActor;
  final List<IntersectionActionHint> actionHints;
  final IntersectionTarget? contentTarget;

  AuthorImpactEvidenceItem({
    required this.evidenceId,
    required this.impactId,
    required this.helpType,
    required this.action,
    required this.intersectionDimension,
    required this.occurredAt,
    required this.summaryText,
    this.sampleVisual,
    this.representativeActor,
    required this.actionHints,
    this.contentTarget,
  });

  factory AuthorImpactEvidenceItem.fromMap(Map<String, dynamic> m) {
    _validateAuthorImpactEvidenceItemWire(m);
    return AuthorImpactEvidenceItem(
      evidenceId: m['evidenceId'] as String,
      impactId: m['impactId'] as String,
      helpType: m['helpType'] as String,
      action: m['action'] as String,
      intersectionDimension: m['intersectionDimension'] as String,
      occurredAt: m['occurredAt'] as String,
      summaryText: m['summaryText'] as String,
      sampleVisual: m['sampleVisual'] == null ? null : IntersectionVisual.fromMap(_parseStringKeyMap(m['sampleVisual'])!),
      representativeActor: m['representativeActor'] == null ? null : IntersectionRepresentativeActor.fromMap(_parseStringKeyMap(m['representativeActor'])!),
      actionHints: _parseProjectionDtoList(m['actionHints'], IntersectionActionHint.fromMap),
      contentTarget: m['contentTarget'] == null ? null : IntersectionTarget.fromMap(_parseStringKeyMap(m['contentTarget'])!),
    );
  }

  Map<String, dynamic> toMap() {
    return <String, dynamic>{
      'evidenceId': evidenceId,
      'impactId': impactId,
      'helpType': helpType,
      'action': action,
      'intersectionDimension': intersectionDimension,
      'occurredAt': occurredAt,
      'summaryText': summaryText,
      'sampleVisual': sampleVisual?.toMap(),
      'representativeActor': representativeActor?.toMap(),
      'actionHints': actionHints.map((value) => value.toMap()).toList(growable: false),
      'contentTarget': contentTarget?.toMap(),
    };
  }

  AuthorImpactEvidenceItem copyWith({
    String? evidenceId,
    String? impactId,
    String? helpType,
    String? action,
    String? intersectionDimension,
    String? occurredAt,
    String? summaryText,
    IntersectionVisual? sampleVisual,
    IntersectionRepresentativeActor? representativeActor,
    List<IntersectionActionHint>? actionHints,
    IntersectionTarget? contentTarget,
  }) {
    return AuthorImpactEvidenceItem(
      evidenceId: evidenceId ?? this.evidenceId,
      impactId: impactId ?? this.impactId,
      helpType: helpType ?? this.helpType,
      action: action ?? this.action,
      intersectionDimension: intersectionDimension ?? this.intersectionDimension,
      occurredAt: occurredAt ?? this.occurredAt,
      summaryText: summaryText ?? this.summaryText,
      sampleVisual: sampleVisual ?? this.sampleVisual,
      representativeActor: representativeActor ?? this.representativeActor,
      actionHints: actionHints ?? this.actionHints,
      contentTarget: contentTarget ?? this.contentTarget,
    );
  }
}

void _validateAuthorImpactEvidenceItemWire(Map<String, dynamic> m) {
  const allowed = <String>{
    'evidenceId',
    'impactId',
    'helpType',
    'action',
    'intersectionDimension',
    'occurredAt',
    'summaryText',
    'sampleVisual',
    'representativeActor',
    'actionHints',
    'contentTarget',
  };
  final unknown = m.keys.where((key) => !allowed.contains(key)).toList(growable: false);
  if (unknown.isNotEmpty) {
    throw FormatException('AuthorImpactEvidenceItem contains unknown fields: ${unknown.join(',')}');
  }
  if (!m.containsKey('evidenceId') || m['evidenceId'] == null || (m['evidenceId'] is! String)) {
    throw FormatException('AuthorImpactEvidenceItem.evidenceId has an invalid wire value');
  }
  if (!m.containsKey('impactId') || m['impactId'] == null || (m['impactId'] is! String)) {
    throw FormatException('AuthorImpactEvidenceItem.impactId has an invalid wire value');
  }
  if (!m.containsKey('helpType') || m['helpType'] == null || (m['helpType'] is! String)) {
    throw FormatException('AuthorImpactEvidenceItem.helpType has an invalid wire value');
  }
  if (!m.containsKey('action') || m['action'] == null || (m['action'] is! String)) {
    throw FormatException('AuthorImpactEvidenceItem.action has an invalid wire value');
  }
  if (!m.containsKey('intersectionDimension') || m['intersectionDimension'] == null || (m['intersectionDimension'] is! String)) {
    throw FormatException('AuthorImpactEvidenceItem.intersectionDimension has an invalid wire value');
  }
  if (!m.containsKey('occurredAt') || m['occurredAt'] == null || (m['occurredAt'] is! String)) {
    throw FormatException('AuthorImpactEvidenceItem.occurredAt has an invalid wire value');
  }
  if (!m.containsKey('summaryText') || m['summaryText'] == null || (m['summaryText'] is! String)) {
    throw FormatException('AuthorImpactEvidenceItem.summaryText has an invalid wire value');
  }
  if (m.containsKey('sampleVisual') && m['sampleVisual'] != null && (m['sampleVisual'] is! Map || (m['sampleVisual'] as Map).keys.any((key) => key is! String))) {
    throw FormatException('AuthorImpactEvidenceItem.sampleVisual has an invalid wire value');
  }
  if (m.containsKey('representativeActor') && m['representativeActor'] != null && (m['representativeActor'] is! Map || (m['representativeActor'] as Map).keys.any((key) => key is! String))) {
    throw FormatException('AuthorImpactEvidenceItem.representativeActor has an invalid wire value');
  }
  if (!m.containsKey('actionHints') || m['actionHints'] == null || (m['actionHints'] is! List || (m['actionHints'] as List).any((value) => value is! Map || value.keys.any((key) => key is! String)))) {
    throw FormatException('AuthorImpactEvidenceItem.actionHints has an invalid wire value');
  }
  if (m.containsKey('contentTarget') && m['contentTarget'] != null && (m['contentTarget'] is! Map || (m['contentTarget'] as Map).keys.any((key) => key is! String))) {
    throw FormatException('AuthorImpactEvidenceItem.contentTarget has an invalid wire value');
  }
}

List<T> _parseProjectionDtoList<T>(
  Object? v,
  T Function(Map<String, dynamic> m) fromMap,
) {
  if (v == null) return List<T>.empty(growable: false);
  if (v is! List) return List<T>.empty(growable: false);
  final out = <T>[];
  for (final e in v) {
    if (e is Map<String, dynamic>) {
      out.add(fromMap(e));
    } else if (e is Map) {
      out.add(fromMap(Map<String, dynamic>.from(e)));
    }
  }
  return out;
}


Map<String, dynamic>? _parseStringKeyMap(dynamic v) {
  if (v == null) return null;
  if (v is Map<String, dynamic>) return v;
  if (v is Map) {
    return Map<String, dynamic>.from(
      v.map((k, val) => MapEntry(k.toString(), val)),
    );
  }
  return null;
}
