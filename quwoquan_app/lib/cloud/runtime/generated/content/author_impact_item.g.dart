// Code generated by tools/codegen_app_metadata from content/content/post/projections/author_impact_item.yaml. DO NOT EDIT.
// ignore_for_file: prefer_const_constructors, unnecessary_null_in_if_null_operators

import '../recommendation/intersection_text_span.g.dart';
import '../recommendation/intersection_visual.g.dart';
import '../recommendation/intersection_target.g.dart';
import '../recommendation/intersection_propagation_path.g.dart';
import '../recommendation/intersection_representative_actor.g.dart';
import '../recommendation/intersection_action_hint.g.dart';

class AuthorImpactItem {
  final String helpType;
  final String action;
  final String intersectionDimension;
  final String tagRef;
  final String source;
  final int count;
  final String primaryText;
  final String subtitleText;
  final String impactId;
  final List<IntersectionTextSpan> primarySpans;
  final List<IntersectionVisual> sampleVisuals;
  final IntersectionRepresentativeActor? representativeActor;
  final List<IntersectionActionHint> actionHints;
  final IntersectionTarget? countTarget;
  final String evidenceSnapshotId;
  final String countObjectKind;
  final IntersectionPropagationPath? propagationPath;
  final String iconKey;
  final String freshAt;
  final String timeBucket;
  final String lifecycleState;
  final double previousStrength;
  final double strengthDelta;

  AuthorImpactItem({
    required this.helpType,
    required this.action,
    required this.intersectionDimension,
    required this.tagRef,
    required this.source,
    required this.count,
    required this.primaryText,
    required this.subtitleText,
    required this.impactId,
    required this.primarySpans,
    required this.sampleVisuals,
    this.representativeActor,
    required this.actionHints,
    this.countTarget,
    required this.evidenceSnapshotId,
    required this.countObjectKind,
    this.propagationPath,
    required this.iconKey,
    required this.freshAt,
    required this.timeBucket,
    required this.lifecycleState,
    required this.previousStrength,
    required this.strengthDelta,
  });

  factory AuthorImpactItem.fromMap(Map<String, dynamic> m) {
    _validateAuthorImpactItemWire(m);
    return AuthorImpactItem(
      helpType: m['helpType'] as String,
      action: m['action'] as String,
      intersectionDimension: m['intersectionDimension'] as String,
      tagRef: m['tagRef'] as String,
      source: m['source'] as String,
      count: m['count'] as int,
      primaryText: m['primaryText'] as String,
      subtitleText: m['subtitleText'] as String,
      impactId: m['impactId'] as String,
      primarySpans: _parseProjectionDtoList(m['primarySpans'], IntersectionTextSpan.fromMap),
      sampleVisuals: _parseProjectionDtoList(m['sampleVisuals'], IntersectionVisual.fromMap),
      representativeActor: m['representativeActor'] == null ? null : IntersectionRepresentativeActor.fromMap(_parseStringKeyMap(m['representativeActor'])!),
      actionHints: _parseProjectionDtoList(m['actionHints'], IntersectionActionHint.fromMap),
      countTarget: m['countTarget'] == null ? null : IntersectionTarget.fromMap(_parseStringKeyMap(m['countTarget'])!),
      evidenceSnapshotId: m['evidenceSnapshotId'] as String,
      countObjectKind: m['countObjectKind'] as String,
      propagationPath: m['propagationPath'] == null ? null : IntersectionPropagationPath.fromMap(_parseStringKeyMap(m['propagationPath'])!),
      iconKey: m['iconKey'] as String,
      freshAt: m['freshAt'] as String,
      timeBucket: m['timeBucket'] as String,
      lifecycleState: m['lifecycleState'] as String,
      previousStrength: (m['previousStrength'] as num).toDouble(),
      strengthDelta: (m['strengthDelta'] as num).toDouble(),
    );
  }

  Map<String, dynamic> toMap() {
    return <String, dynamic>{
      'helpType': helpType,
      'action': action,
      'intersectionDimension': intersectionDimension,
      'tagRef': tagRef,
      'source': source,
      'count': count,
      'primaryText': primaryText,
      'subtitleText': subtitleText,
      'impactId': impactId,
      'primarySpans': primarySpans.map((value) => value.toMap()).toList(growable: false),
      'sampleVisuals': sampleVisuals.map((value) => value.toMap()).toList(growable: false),
      'representativeActor': representativeActor?.toMap(),
      'actionHints': actionHints.map((value) => value.toMap()).toList(growable: false),
      'countTarget': countTarget?.toMap(),
      'evidenceSnapshotId': evidenceSnapshotId,
      'countObjectKind': countObjectKind,
      'propagationPath': propagationPath?.toMap(),
      'iconKey': iconKey,
      'freshAt': freshAt,
      'timeBucket': timeBucket,
      'lifecycleState': lifecycleState,
      'previousStrength': previousStrength,
      'strengthDelta': strengthDelta,
    };
  }

  AuthorImpactItem copyWith({
    String? helpType,
    String? action,
    String? intersectionDimension,
    String? tagRef,
    String? source,
    int? count,
    String? primaryText,
    String? subtitleText,
    String? impactId,
    List<IntersectionTextSpan>? primarySpans,
    List<IntersectionVisual>? sampleVisuals,
    IntersectionRepresentativeActor? representativeActor,
    List<IntersectionActionHint>? actionHints,
    IntersectionTarget? countTarget,
    String? evidenceSnapshotId,
    String? countObjectKind,
    IntersectionPropagationPath? propagationPath,
    String? iconKey,
    String? freshAt,
    String? timeBucket,
    String? lifecycleState,
    double? previousStrength,
    double? strengthDelta,
  }) {
    return AuthorImpactItem(
      helpType: helpType ?? this.helpType,
      action: action ?? this.action,
      intersectionDimension: intersectionDimension ?? this.intersectionDimension,
      tagRef: tagRef ?? this.tagRef,
      source: source ?? this.source,
      count: count ?? this.count,
      primaryText: primaryText ?? this.primaryText,
      subtitleText: subtitleText ?? this.subtitleText,
      impactId: impactId ?? this.impactId,
      primarySpans: primarySpans ?? this.primarySpans,
      sampleVisuals: sampleVisuals ?? this.sampleVisuals,
      representativeActor: representativeActor ?? this.representativeActor,
      actionHints: actionHints ?? this.actionHints,
      countTarget: countTarget ?? this.countTarget,
      evidenceSnapshotId: evidenceSnapshotId ?? this.evidenceSnapshotId,
      countObjectKind: countObjectKind ?? this.countObjectKind,
      propagationPath: propagationPath ?? this.propagationPath,
      iconKey: iconKey ?? this.iconKey,
      freshAt: freshAt ?? this.freshAt,
      timeBucket: timeBucket ?? this.timeBucket,
      lifecycleState: lifecycleState ?? this.lifecycleState,
      previousStrength: previousStrength ?? this.previousStrength,
      strengthDelta: strengthDelta ?? this.strengthDelta,
    );
  }
}

void _validateAuthorImpactItemWire(Map<String, dynamic> m) {
  const allowed = <String>{
    'helpType',
    'action',
    'intersectionDimension',
    'tagRef',
    'source',
    'count',
    'primaryText',
    'subtitleText',
    'impactId',
    'primarySpans',
    'sampleVisuals',
    'representativeActor',
    'actionHints',
    'countTarget',
    'evidenceSnapshotId',
    'countObjectKind',
    'propagationPath',
    'iconKey',
    'freshAt',
    'timeBucket',
    'lifecycleState',
    'previousStrength',
    'strengthDelta',
  };
  final unknown = m.keys.where((key) => !allowed.contains(key)).toList(growable: false);
  if (unknown.isNotEmpty) {
    throw FormatException('AuthorImpactItem contains unknown fields: ${unknown.join(',')}');
  }
  if (!m.containsKey('helpType') || m['helpType'] == null || (m['helpType'] is! String)) {
    throw FormatException('AuthorImpactItem.helpType has an invalid wire value');
  }
  if (!m.containsKey('action') || m['action'] == null || (m['action'] is! String)) {
    throw FormatException('AuthorImpactItem.action has an invalid wire value');
  }
  if (!m.containsKey('intersectionDimension') || m['intersectionDimension'] == null || (m['intersectionDimension'] is! String)) {
    throw FormatException('AuthorImpactItem.intersectionDimension has an invalid wire value');
  }
  if (!m.containsKey('tagRef') || m['tagRef'] == null || (m['tagRef'] is! String)) {
    throw FormatException('AuthorImpactItem.tagRef has an invalid wire value');
  }
  if (!m.containsKey('source') || m['source'] == null || (m['source'] is! String)) {
    throw FormatException('AuthorImpactItem.source has an invalid wire value');
  }
  if (!m.containsKey('count') || m['count'] == null || (m['count'] is! int)) {
    throw FormatException('AuthorImpactItem.count has an invalid wire value');
  }
  if (!m.containsKey('primaryText') || m['primaryText'] == null || (m['primaryText'] is! String)) {
    throw FormatException('AuthorImpactItem.primaryText has an invalid wire value');
  }
  if (!m.containsKey('subtitleText') || m['subtitleText'] == null || (m['subtitleText'] is! String)) {
    throw FormatException('AuthorImpactItem.subtitleText has an invalid wire value');
  }
  if (!m.containsKey('impactId') || m['impactId'] == null || (m['impactId'] is! String)) {
    throw FormatException('AuthorImpactItem.impactId has an invalid wire value');
  }
  if (!m.containsKey('primarySpans') || m['primarySpans'] == null || (m['primarySpans'] is! List || (m['primarySpans'] as List).any((value) => value is! Map || value.keys.any((key) => key is! String)))) {
    throw FormatException('AuthorImpactItem.primarySpans has an invalid wire value');
  }
  if (!m.containsKey('sampleVisuals') || m['sampleVisuals'] == null || (m['sampleVisuals'] is! List || (m['sampleVisuals'] as List).any((value) => value is! Map || value.keys.any((key) => key is! String)))) {
    throw FormatException('AuthorImpactItem.sampleVisuals has an invalid wire value');
  }
  if (m.containsKey('representativeActor') && m['representativeActor'] != null && (m['representativeActor'] is! Map || (m['representativeActor'] as Map).keys.any((key) => key is! String))) {
    throw FormatException('AuthorImpactItem.representativeActor has an invalid wire value');
  }
  if (!m.containsKey('actionHints') || m['actionHints'] == null || (m['actionHints'] is! List || (m['actionHints'] as List).any((value) => value is! Map || value.keys.any((key) => key is! String)))) {
    throw FormatException('AuthorImpactItem.actionHints has an invalid wire value');
  }
  if (m.containsKey('countTarget') && m['countTarget'] != null && (m['countTarget'] is! Map || (m['countTarget'] as Map).keys.any((key) => key is! String))) {
    throw FormatException('AuthorImpactItem.countTarget has an invalid wire value');
  }
  if (!m.containsKey('evidenceSnapshotId') || m['evidenceSnapshotId'] == null || (m['evidenceSnapshotId'] is! String)) {
    throw FormatException('AuthorImpactItem.evidenceSnapshotId has an invalid wire value');
  }
  if (!m.containsKey('countObjectKind') || m['countObjectKind'] == null || (m['countObjectKind'] is! String)) {
    throw FormatException('AuthorImpactItem.countObjectKind has an invalid wire value');
  }
  if (m.containsKey('propagationPath') && m['propagationPath'] != null && (m['propagationPath'] is! Map || (m['propagationPath'] as Map).keys.any((key) => key is! String))) {
    throw FormatException('AuthorImpactItem.propagationPath has an invalid wire value');
  }
  if (!m.containsKey('iconKey') || m['iconKey'] == null || (m['iconKey'] is! String)) {
    throw FormatException('AuthorImpactItem.iconKey has an invalid wire value');
  }
  if (!m.containsKey('freshAt') || m['freshAt'] == null || (m['freshAt'] is! String)) {
    throw FormatException('AuthorImpactItem.freshAt has an invalid wire value');
  }
  if (!m.containsKey('timeBucket') || m['timeBucket'] == null || (m['timeBucket'] is! String)) {
    throw FormatException('AuthorImpactItem.timeBucket has an invalid wire value');
  }
  if (!m.containsKey('lifecycleState') || m['lifecycleState'] == null || (m['lifecycleState'] is! String)) {
    throw FormatException('AuthorImpactItem.lifecycleState has an invalid wire value');
  }
  if (!m.containsKey('previousStrength') || m['previousStrength'] == null || (m['previousStrength'] is! num)) {
    throw FormatException('AuthorImpactItem.previousStrength has an invalid wire value');
  }
  if (!m.containsKey('strengthDelta') || m['strengthDelta'] == null || (m['strengthDelta'] is! num)) {
    throw FormatException('AuthorImpactItem.strengthDelta has an invalid wire value');
  }
}

List<T> _parseProjectionDtoList<T>(
  Object? v,
  T Function(Map<String, dynamic> m) fromMap,
) {
  if (v == null) return List<T>.empty(growable: false);
  if (v is! List) return List<T>.empty(growable: false);
  final out = <T>[];
  for (final e in v) {
    if (e is Map<String, dynamic>) {
      out.add(fromMap(e));
    } else if (e is Map) {
      out.add(fromMap(Map<String, dynamic>.from(e)));
    }
  }
  return out;
}


Map<String, dynamic>? _parseStringKeyMap(dynamic v) {
  if (v == null) return null;
  if (v is Map<String, dynamic>) return v;
  if (v is Map) {
    return Map<String, dynamic>.from(
      v.map((k, val) => MapEntry(k.toString(), val)),
    );
  }
  return null;
}
