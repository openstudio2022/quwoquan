// Code generated by tools/codegen_app_metadata from content/content/post/projections/author_impact_summary.yaml. DO NOT EDIT.
// ignore_for_file: prefer_const_constructors, unnecessary_null_in_if_null_operators

import 'author_impact_item.g.dart';

class AuthorImpactSummary {
  final String authorId;
  final int total;
  final List<AuthorImpactItem> items;

  AuthorImpactSummary({
    required this.authorId,
    required this.total,
    required this.items,
  });

  factory AuthorImpactSummary.fromMap(Map<String, dynamic> m) {
    _validateAuthorImpactSummaryWire(m);
    return AuthorImpactSummary(
      authorId: m['authorId'] as String,
      total: m['total'] as int,
      items: _parseProjectionDtoList(m['items'], AuthorImpactItem.fromMap),
    );
  }

  Map<String, dynamic> toMap() {
    return <String, dynamic>{
      'authorId': authorId,
      'total': total,
      'items': items.map((value) => value.toMap()).toList(growable: false),
    };
  }

  AuthorImpactSummary copyWith({
    String? authorId,
    int? total,
    List<AuthorImpactItem>? items,
  }) {
    return AuthorImpactSummary(
      authorId: authorId ?? this.authorId,
      total: total ?? this.total,
      items: items ?? this.items,
    );
  }
}

void _validateAuthorImpactSummaryWire(Map<String, dynamic> m) {
  const allowed = <String>{
    'authorId',
    'total',
    'items',
  };
  final unknown = m.keys.where((key) => !allowed.contains(key)).toList(growable: false);
  if (unknown.isNotEmpty) {
    throw FormatException('AuthorImpactSummary contains unknown fields: ${unknown.join(',')}');
  }
  if (!m.containsKey('authorId') || m['authorId'] == null || (m['authorId'] is! String)) {
    throw FormatException('AuthorImpactSummary.authorId has an invalid wire value');
  }
  if (!m.containsKey('total') || m['total'] == null || (m['total'] is! int)) {
    throw FormatException('AuthorImpactSummary.total has an invalid wire value');
  }
  if (!m.containsKey('items') || m['items'] == null || (m['items'] is! List || (m['items'] as List).any((value) => value is! Map || value.keys.any((key) => key is! String)))) {
    throw FormatException('AuthorImpactSummary.items has an invalid wire value');
  }
}

List<T> _parseProjectionDtoList<T>(
  Object? v,
  T Function(Map<String, dynamic> m) fromMap,
) {
  if (v == null) return List<T>.empty(growable: false);
  if (v is! List) return List<T>.empty(growable: false);
  final out = <T>[];
  for (final e in v) {
    if (e is Map<String, dynamic>) {
      out.add(fromMap(e));
    } else if (e is Map) {
      out.add(fromMap(Map<String, dynamic>.from(e)));
    }
  }
  return out;
}
