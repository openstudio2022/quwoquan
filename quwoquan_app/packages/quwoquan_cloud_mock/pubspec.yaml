name: quwoquan_cloud_mock
description: Alpha/test-only Cloud adapters and immutable fixture bundle.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.12.2 <4.0.0'

dependencies:
  crypto: ^3.0.7
  quwoquan_cloud_contracts:
    path: ../quwoquan_cloud_contracts

dev_dependencies:
  test: ^1.30.0
