---
globs: *.dart
---

# 趣我圈App 设计系统规范

## 🎨 设计系统核心

### 颜色系统完整规范

#### 1. 品牌色彩层级

```dart
// 主色调蓝色系 (Primary Colors)
AppColors.primaryColor           // #1877F2 - 主要按钮、链接、品牌标识
AppColors.primaryColorHover      // #166FE5 - 悬停状态
AppColors.primaryColorActive     // #1565C0 - 激活状态
AppColors.primaryColorLight      // #E7F3FF - 浅色版本
AppColors.primaryColorDark       // #0D47A1 - 深色版本

// 次要色调紫色系 (Secondary Colors)
AppColors.secondaryColor         // #8B5CF6 - 次要按钮、图标、装饰元素
AppColors.secondaryColorHover    // #7C3AED
AppColors.secondaryColorActive   // #6D28D9
AppColors.secondaryColorLight    // #F3E8FF
AppColors.secondaryColorDark     // #4C1D95

// 强调色调绿色系 (Accent Colors)
AppColors.accentColor           // #10B981 - 强调元素、特殊功能按钮
AppColors.accentColorHover      // #059669
AppColors.accentColorActive     // #047857
AppColors.accentColorLight      // #D1FAE5
AppColors.accentColorDark       // #064E3B
```

#### 2. 功能性颜色

```dart
// 功能性颜色 (Functional Colors)
AppColors.success               // #00BA7C - 成功状态
AppColors.warning               // #FF9500 - 警告状态
AppColors.error                 // #ED4956 - 错误状态
AppColors.info                  // #1877F2 - 信息提示

// 特殊功能色 (Special Colors)
AppColors.special.linkColor        // #1877F2 - 链接
AppColors.special.hoverColor       // #F5F5F5 - 悬停
AppColors.special.selectedColor    // #E7F3FF - 选中
AppColors.special.focusColor       // #1877F2 - 焦点
AppColors.special.disabledColor    // #A8A8A8 - 禁用

// 渐变色系 (Gradient Colors)
AppColors.special.primaryGradient     // 蓝→紫
AppColors.special.secondaryGradient   // 紫→绿
AppColors.special.accentGradient      // 绿→蓝

// 状态色系 (Status Colors)
AppColors.special.onlineColor      // #00BA7C - 在线
AppColors.special.offlineColor     // #A8A8A8 - 离线
AppColors.special.awayColor        // #FF9500 - 离开
AppColors.special.busyColor        // #ED4956 - 忙碌

// 等级色系 (Level Colors)
AppColors.special.level1Color      // #8B5CF6 - 紫色
AppColors.special.level2Color      // #1877F2 - 蓝色
AppColors.special.level3Color      // #10B981 - 绿色
AppColors.special.level4Color      // #FF9500 - 橙色
AppColors.special.level5Color      // #ED4956 - 红色
```

#### 3. 主题色彩

```dart
// 浅色主题 (Light Theme)
AppColors.light.backgroundPrimary      // #FFFFFF
AppColors.light.backgroundSecondary    // #F8F9FA
AppColors.light.textPrimary           // #212529
AppColors.light.textSecondary         // #6C757D
AppColors.light.borderPrimary         // #DEE2E6

// 深色主题 (Dark Theme)
AppColors.dark.backgroundPrimary      // #121212
AppColors.dark.backgroundSecondary    // #1E1E1E
AppColors.dark.textPrimary           // #FFFFFF
AppColors.dark.textSecondary         // #B3B3B3
AppColors.dark.borderPrimary         // #404040
```

### 响应式间距系统

#### 1. 基础间距

```dart
AppSpacing.xs        // 4.0  - 极小间距（紧密元素）
AppSpacing.sm        // 8.0  - 小间距（相关元素）
AppSpacing.md        // 16.0 - 中等间距（一般元素）
AppSpacing.lg        // 24.0 - 大间距（分组元素）
AppSpacing.xl        // 32.0 - 超大间距（区块间距）
```

#### 2. 语义间距（自动响应式）

```dart
// 组内间距 (intraGroup) - 同一组内相关元素之间
AppSpacing.semantic[DesignSemanticConstants.intraGroup]?[DesignSemanticConstants.xs] ?? AppSpacing.intraGroupXs  // 紧密标签组
AppSpacing.semantic[DesignSemanticConstants.intraGroup]?[DesignSemanticConstants.sm] ?? AppSpacing.intraGroupSm  // 标签组、按钮组
AppSpacing.semantic[DesignSemanticConstants.intraGroup]?[DesignSemanticConstants.md] ?? AppSpacing.intraGroupMd  // 表单项、列表项
AppSpacing.semantic[DesignSemanticConstants.intraGroup]?[DesignSemanticConstants.lg] ?? AppSpacing.intraGroupLg  // 卡片内容
AppSpacing.semantic[DesignSemanticConstants.intraGroup]?[DesignSemanticConstants.xl] ?? AppSpacing.intraGroupXl  // 宽松布局

// 组间间距 (interGroup) - 不同组之间
AppSpacing.semantic[DesignSemanticConstants.interGroup]?[DesignSemanticConstants.xs] ?? AppSpacing.interGroupXs  // 紧密相关组
AppSpacing.semantic[DesignSemanticConstants.interGroup]?[DesignSemanticConstants.sm] ?? AppSpacing.interGroupSm  // 相关组
AppSpacing.semantic[DesignSemanticConstants.interGroup]?[DesignSemanticConstants.md] ?? AppSpacing.interGroupMd  // 一般组
AppSpacing.semantic[DesignSemanticConstants.interGroup]?[DesignSemanticConstants.lg] ?? AppSpacing.interGroupLg  // 独立组
AppSpacing.semantic[DesignSemanticConstants.interGroup]?[DesignSemanticConstants.xl] ?? AppSpacing.interGroupXl  // 页面区块

// 容器间距 (container) - 容器内边距
AppSpacing.semantic[DesignSemanticConstants.container]?[DesignSemanticConstants.xs] ?? AppSpacing.containerXs  // 极小容器
AppSpacing.semantic[DesignSemanticConstants.container]?[DesignSemanticConstants.sm] ?? AppSpacing.containerSm  // 小容器
AppSpacing.semantic[DesignSemanticConstants.container]?[DesignSemanticConstants.md] ?? AppSpacing.containerMd  // 中等容器
AppSpacing.semantic[DesignSemanticConstants.container]?[DesignSemanticConstants.lg] ?? AppSpacing.containerLg  // 大容器
AppSpacing.semantic[DesignSemanticConstants.container]?[DesignSemanticConstants.xl] ?? AppSpacing.containerXl  // 超大容器
```

#### 3. 组件尺寸

```dart
// 头像尺寸
AppSpacing.avatarSize              // 40.0 - 标准头像
AppSpacing.smallAvatarSize         // 32.0 - 小头像
AppSpacing.largeAvatarSize         // 64.0 - 大头像

// 按钮尺寸
AppSpacing.buttonHeight            // 48.0 - 标准按钮高度
AppSpacing.smallButtonSize         // 32.0 - 小按钮
AppSpacing.largeButtonSize         // 56.0 - 大按钮

// 图标尺寸
AppSpacing.iconSmall               // 16.0 - 小图标
AppSpacing.iconMedium              // 24.0 - 中等图标
AppSpacing.iconLarge               // 32.0 - 大图标

// 布局高度
AppSpacing.bottomNavHeight         // 56.0 - 底部导航栏
AppSpacing.tabNavigationHeight     // 48.0 - 标签导航栏
AppSpacing.appBarHeight            // 56.0 - 应用栏
```

#### 4. 圆角尺寸

```dart
AppSpacing.smallBorderRadius       // 4.0  - 小圆角（按钮、标签）
AppSpacing.borderRadius            // 8.0  - 标准圆角（卡片、输入框）
AppSpacing.largeBorderRadius       // 12.0 - 大圆角（大卡片）
AppSpacing.circularBorderRadius    // 50%  - 圆形（头像、圆形按钮）
```

### 响应式设计原则

#### 屏幕断点

```dart
// 手机端: < 768px
// 平板端: 768px - 1024px
// 桌面端: > 1024px

// 自动适配机制
context.getIntraGroupSpacing('md')   // 自动根据屏幕尺寸返回合适的间距
context.getInterGroupSpacing('lg')   // 自动根据屏幕尺寸返回合适的间距
context.getContainerSpacing('sm')    // 自动根据屏幕尺寸返回合适的间距
```

## 🎭 组件设计规范

### 按钮组件

```dart
// 主要按钮
ElevatedButton(
  style: ElevatedButton.styleFrom(
    backgroundColor: AppColors.primaryColor,
    foregroundColor: AppColors.light.textPrimary,
    minimumSize: Size(double.infinity, AppSpacing.buttonHeight),
    shape: RoundedRectangleBorder(
      borderRadius: BorderRadius.circular(AppSpacing.borderRadius),
    ),
  ),
  onPressed: onPressed,
  child: Text(UITextConstants.confirm),
)

// 次要按钮
OutlinedButton(
  style: OutlinedButton.styleFrom(
    foregroundColor: AppColors.primaryColor,
    side: BorderSide(color: AppColors.primaryColor),
    minimumSize: Size(double.infinity, AppSpacing.buttonHeight),
    shape: RoundedRectangleBorder(
      borderRadius: BorderRadius.circular(AppSpacing.borderRadius),
    ),
  ),
  onPressed: onPressed,
  child: Text(UITextConstants.cancel),
)
```

### 卡片组件

```dart
Card(
  elevation: 2,
  shape: RoundedRectangleBorder(
    borderRadius: BorderRadius.circular(AppSpacing.borderRadius),
  ),
  child: Padding(
    padding: EdgeInsets.all(
      AppSpacing.semantic[DesignSemanticConstants.container]?[DesignSemanticConstants.md] ?? AppSpacing.containerMd,
    ),
    child: Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Text(
          title,
          style: Theme.of(context).textTheme.titleMedium,
        ),
        SizedBox(
          height: AppSpacing.semantic[DesignSemanticConstants.intraGroup]?[DesignSemanticConstants.sm] ?? AppSpacing.intraGroupSm,
        ),
        Text(
          content,
          style: Theme.of(context).textTheme.bodyMedium,
        ),
      ],
    ),
  ),
)
```

### 输入组件

```dart
TextField(
  decoration: InputDecoration(
    labelText: UITextConstants.username,
    hintText: UITextConstants.enterUsername,
    contentPadding: EdgeInsets.symmetric(
      horizontal: AppSpacing.semantic[DesignSemanticConstants.container]?[DesignSemanticConstants.sm] ?? AppSpacing.containerSm,
      vertical: AppSpacing.semantic[DesignSemanticConstants.container]?[DesignSemanticConstants.sm] ?? AppSpacing.containerSm,
    ),
    border: OutlineInputBorder(
      borderRadius: BorderRadius.circular(AppSpacing.borderRadius),
    ),
    focusedBorder: OutlineInputBorder(
      borderRadius: BorderRadius.circular(AppSpacing.borderRadius),
      borderSide: BorderSide(color: AppColors.special.focusColor, width: 2),
    ),
  ),
)
```

## 📱 无障碍设计

### 触摸目标

```dart
// 最小触摸目标: 44px x 44px
// 推荐触摸目标: 48px x 48px

InkWell(
  onTap: onTap,
  child: Container(
    constraints: BoxConstraints(
      minWidth: AppSpacing.buttonHeight,  // 48.0
      minHeight: AppSpacing.buttonHeight, // 48.0
    ),
    child: Center(child: child),
  ),
)
```

### 颜色对比度

```dart
// AA级别：
// - 正常文字: 4.5:1
// - 大号文字: 3:1

// AAA级别：
// - 正常文字: 7:1
// - 大号文字: 4.5:1

// 确保文字与背景有足够对比度
Text(
  text,
  style: TextStyle(
    color: AppColors.light.textPrimary,  // 确保对比度
  ),
)
```

## 相关文档

- [03_DESIGN_RULES.md](mdc:03_DESIGN_RULES.md)
- [DESIGN_SYSTEM_DOCUMENTATION.md](mdc:DESIGN_SYSTEM_DOCUMENTATION.md)
- [04.1_DESIGN_COLOR_CHECKLIST.md](mdc:04.1_DESIGN_COLOR_CHECKLIST.md)
