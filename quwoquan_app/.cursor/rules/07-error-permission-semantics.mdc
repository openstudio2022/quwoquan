---
description: 云端/网络错误与权限类统一语义约束
globs: **/*.dart
---

# 错误与权限统一语义

> 完整规范：`specs/ux/error-and-permission-semantics.md`  
> 特性树：`runtime/runtime-client-foundation/error-permission-display-semantics`（L3）及子 L4

## 云端/网络错误

| 严重度 | 展示方式 | 适用场景 |
|--------|----------|----------|
| **阻塞性** | 内联占位（卡片式，持久） | 页面/列表首次加载失败、无降级数据 |
| **次要** | AppToast（3s） | 提交失败、单次操作失败、可重试的瞬时错误 |

## 权限类（AppPermissionCoordinator）

| Phase | 展示 | 主操作 |
|-------|------|--------|
| L0 granted | 无 | — |
| L1 requestable | **仅系统弹窗**（JIT 默认）；page 可先 L2 preBrief | 允许/继续 |
| L3 settingsRequired | gateCard **一次** | **去设置** |
| L4 suppress | **仅 AppToast** | 知道了 |

**Surface**：

- `AppPermissionSurface.jit`：按住说话、RTC — **默认无 L2 primer**
- `AppPermissionSurface.page`：相册、定位、相机页 — 可 L2 → L1

**红线**：

- 禁止 `permission.request()` 后再 `record.hasPermission(request:true)`
- L2 按钮「继续」时正文禁止仅写「请点允许」
- JIT 语音发送失败：仅 status bar（`chatVoicePendingRetry`），禁止 actionDialog 叠加

**l10n**：`permissionSettingsGateTitle`、`permissionPrimerContinue`、`chatVoicePendingRetry` 等，禁止硬编码。

## 地图/位置特定

- 加载：`locationFetchingResult` + `CircularProgressIndicator`
- 权限未批：权限 gate + 去设置（Coordinator）
- 云端错误：内联错误卡片 + 重试
