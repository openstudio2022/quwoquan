---
globs: *_test.dart,test/**/*.dart
---

# 趣我圈 App 测试标准

唯一测试分层真相源：`specs/03_TESTING_STRATEGY.md`。

App 新增测试只使用三层：

- `local_contract`：Provider、Widget、Mapper、MockRepository、DTO/wire、错误码 mapper、页面状态机。
- `api_integration`：RemoteRepository、gateway/API、真实错误响应、真实 seed 数据、端云字段一致性。
- `user_acceptance`：用户旅程、页面功能、UI/UX 可用性、页面生命周期、权限、弱网、灰度发布验收。

`unit/widget/integration` 只描述技术形态，不再作为仓库测试层级或新增目录名。

## 目录

```text
quwoquan_app/test/
  local_contract/<domain>/<capability>/<story>/<case>_test.dart
  api_integration/<domain>/<capability>/<story>/<case>_test.dart
  user_acceptance/<journey_id>/<scenario_id>/<case>_test.dart
  user_acceptance/pages/<domain>/<surface_id>/<case>_test.dart
```

存量 `test/cloud`、`test/ui`、`test/components`、`test/core`、`test/patrol` 只作为迁移映射；新测试不得继续新增旧目录概念。

## 必守

- App 不得单独完成端云链路：`api_integration` 中验证的字段、状态码、错误码、边界行为，必须在 `local_contract` Mock/Provider/Widget 测试中有对应断言。
- 页面或用户可见能力必须补 `user_acceptance`，覆盖页面功能、UI/UX、生命周期、加载/空态/错误/权限/恢复动作。
- 环境数据遵守 alpha/beta/gamma/prod 分层：alpha 可用 contract-seeded Mock，beta/gamma/prod 必须走 Remote/gateway/API seed 或真实数据。
- 生产灰度是 prod rollout stage，不存在独立 `prod-gray` App 包。
- 禁止空断言、`skip: true` 新增、手写全绿 report、UI 直连 Mock 目录、硬编码 path/surface/operation/error code。

## 推荐命令

```bash
make test-local-contract
make test-api-integration ENV=gamma
make test-user-acceptance TARGET=gamma-local
make verify-test-specs
make verify-app-page-horizontal-quality
bash quwoquan_ops/gate/gate_repo.sh --scope app
```

## 命名

- 用例 ID：`<layer>.<domain>.<capability>.<story>.<case>`。
- 页面用例：`user_acceptance.page.<surface_id>.<state_or_action>`。
- 文件名：`<story_or_operation>__<case>_test.dart`。
