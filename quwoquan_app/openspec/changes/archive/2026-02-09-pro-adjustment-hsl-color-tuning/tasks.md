## 1. HSL 状态与模型

- [x] 1.1 定义 HSL 通道枚举（红/橙/黄/绿/青/蓝/紫）与通道参数结构（hue/saturation/luminance）
- [x] 1.2 在图片编辑器状态中新增 HSL 参数容器，范围统一为 `-100..100`，默认值为 `0`
- [x] 1.3 增加 HSL 会话快照与会话内操作栈（undo/redo）数据结构

## 2. HSL 面板 UI

- [x] 2.1 实现 HSL 顶部颜色通道选择栏（含选中态与中性色语义）
- [x] 2.2 实现 H/S/L 三条调节线与数值展示，默认中点、范围 `-100..100`
- [x] 2.3 统一 HSL 底部 `X / ✓` 与基础分组样式、尺寸、点击热区

## 3. 系统级会话操作条（可复用）

- [x] 3.1 新建系统级 `EditorSessionOpsStrip` 组件（支持能力开关：undo/redo/compare）
- [x] 3.2 定义并接入“工具会话基线图像”接口，供 compare 复用
- [x] 3.3 在 HSL 会话中接入系统级会话操作条，替换 HSL 私有按钮实现
- [x] 3.4 完成会话操作条的语义样式与安全区适配，保证跨工具一致性

## 4. HSL 交互能力

- [x] 4.1 实现取色器入口与画面采样交互（指针/放大镜或等价反馈）
- [x] 4.2 实现采样颜色到七通道的映射逻辑并联动选中通道
- [x] 4.3 接入会话内撤回/重做行为（通过系统级会话操作条）
- [x] 4.4 接入原图对比行为（对比会话基线图像）

## 5. 预览与导出落地

- [x] 5.1 将 HSL 参数实时作用到预览图，确保拖动调节即时生效
- [x] 5.2 处理 HSL 与基础分组叠加顺序，保证视觉稳定与可预测
- [x] 5.3 在确认保存时将 HSL 结果写入全局编辑历史（单步 `hslAdjustments`）
- [x] 5.4 在编辑完成导出时将 HSL 调整结果落地到输出图文件

## 6. 验证与回归

- [x] 6.1 覆盖通道切换保值、滑线同步、范围边界（-100/100）验证
- [x] 6.2 覆盖取色器映射准确性与异常图片场景验证
- [x] 6.3 覆盖系统级会话操作条在 HSL 与其它工具下的复用验证
- [x] 6.4 覆盖 undo/redo 与对比原图交互验证
- [x] 6.5 运行 `flutter analyze` 与关键流程手测（进入 HSL、取消、确认、返回上层）

