# 阶段契约：0.plan

## PRE

AI 在 OPEN 只提交并冻结 `execution_manifest.json`、`0.plan/request.json`、`0.plan/target_set.json` 的 exact refs。

## DURING

AI 确认三份文件由 `task init` 原子创建，再理解并逐项核对 confirmed demand、immutable candidate bindings、execution identity、载体、目标与 quota。不得读取旧 execution spec、provider/model/capacity、宿主调度或 sequence-017；此阶段不新增业务产物，不改写 init 文件，不创建 task/batch/lane/worker 身份。

## POST

机械 verifier：

```bash
python3 quwoquan_data/scripts/cli.py verify task-init-contract --execution-id <id>
```

AI self-check：目标、载体、quota、candidate binding 与 execution identity 是否一致且足以进入逐 target 来源规划。任何 identity、schema、摘要或 binding 漂移均 blocked，并保留 typed issue。

## HANDOFF

- receipt ref/digest；
- `resultRefs`：三份 init artifact exact refs/digests；
- `typedIssues`；
- Skill 固定后继：`sources`。
