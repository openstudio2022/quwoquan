# 阶段契约：0.plan

## PRE

- 前置：`task init` 已原子写入 `execution_manifest.json`、`0.plan/request.json`、`0.plan/target_set.json`。
- AI 在 OPEN 显式提交上述三份 ref/digest；内核冻结 exact bytes。
- 输入必须包含 confirmed demand、immutable candidate bindings、载体、目标、配额与 execution identity；不得读取旧 execution spec、provider/model/capacity 或 sequence-017。

## DURING

AI 逐项理解并核对冻结目标、数量、载体与 candidate bindings。此阶段不新增业务产物，不改写 init 文件，不创建 task/batch/lane/worker 身份。

## POST

逐条运行：

```bash
python3 quwoquan_data/scripts/cli.py verify runtime-input-ownership
python3 quwoquan_data/scripts/cli.py verify task-init-contract --execution-id <id>
```

AI 根据真实 verifier facts 决定 pass/blocked。任何 identity、schema、摘要或 binding 漂移均 blocked，并保留 typed issue。

## HANDOFF

- `resultRefs`：三份 init artifact exact refs/digests。
- pass 后由 Skill 固定进入 `sources`；不得在 receipt 中派生 next。
