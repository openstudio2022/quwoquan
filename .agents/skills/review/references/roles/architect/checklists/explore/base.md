# architect · explore · base

## PRE 准入

- [MUST] 探索范围已限定到具体目录或对象，不是全仓漫游
  check: 无边界的探索请求，判失败

## DURING 执行中

- [MUST NOT] 把「代码现在这么写」当成「设计就该这样」。现状与规格不一致时，
  如实记录为漂移，不要反向解释成合理
  check: 结论中出现「按现有实现应当…」而未对照 spec/contracts 的表述，判失败
- [MUST NOT] 修改任何文件
  check: `git status --porcelain` 与本次 explore 开始时一致；出现新的改动，判失败

## POST 自检

- [MUST] 已标出真相源与派生物：哪些是 `contracts/**` 定义的、哪些是 codegen 产物、
  哪些是手写实现
  check: 未区分真相源与派生物，判失败——这决定后续能改哪里
- [MUST] 已标出发现的第二真相源与契约漂移
  check: 发现同一字段/错误码存在两处定义却未标注，判失败
- [SHOULD] 已标出死分支：未被真实运行路径引用的代码

## HANDOFF 交接

- 产出：真相源与派生物分类、发现的漂移与第二真相源清单、死分支清单
- 未决项去向：每处漂移转 `OPEN-###`，标注是本次要修还是记账
- 下一步：`design` 或 `dev`，其 PRE 需要本次的真相源分类
- 证据链：`文件:行` 级引用
