class DartRuntimeErrorGenerator {
  const DartRuntimeErrorGenerator();

  void generate() {
    // The first milestone keeps generation explicit and side-effect free.
    // Future milestones can materialize package files from the schema here.
  }
}
