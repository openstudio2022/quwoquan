# docs 目录边界

`docs/` 只保存长期工程说明、执行流程、外部依赖登记和正式风险 backlog。

产品规格、架构裁定、特性树、验收标准、变更记录统一进入 `specs/`。功能级规格不得在 `docs/` 下另建第二入口。

当前允许的长期文档类型：

- Codex / Cursor 执行协议与命令说明。
- 外部服务依赖与代理配置说明。
- 长期工程计划、正式风险 backlog 和用户可见提示 backlog。
- 已确认仍有工程价值的实施复盘或迁移说明。
