## 1. 分组与状态模型迁移

- [x] 1.1 将专业修图分组从“基础/HSL/曲线”迁移为“整体/局部/HSL/曲线”，并更新常量与文案映射
- [x] 1.2 建立“整体”参数兼容层，确保旧基础参数可平滑迁移并保持默认值语义
- [x] 1.3 新增局部锚点状态模型（锚点集合、当前锚点、半径、15项参数、当前参数类型）
- [x] 1.4 新增 HSL 通道状态模型（七通道 × H/S/L）与会话快照

## 1A. 专业修图工具箱入口与面板路由

- [x] 1A.1 专业修图入口改为底栏上方工具箱弹窗（不直接切页）
- [x] 1A.2 工具箱项使用图标+label，并可进入独立工具编辑态
- [x] 1A.3 编辑态底部统一 `X / ✓`，移除返回箭头语义

## 2. 局部锚点交互与面板

- [x] 2.1 实现局部与整体共享参数列表与同一调节线（不再使用局部四条独立线）
- [x] 2.2 实现“添加锚点”一次添加模式：按钮激活后点击一次仅创建一个锚点并自动退出添加态
- [x] 2.3 实现锚点拖拽与放大镜（圆形放大、准星、跟随移动）
- [x] 2.4 实现“查看全部锚点”眼睛开关与锚点点击切换逻辑
- [x] 2.5 实现锚点菜单（复制/删除）及复制后可继续拖动定位
- [x] 2.6 实现锚点中心首字与外圈 360 度映射联动（负值逆时针、正值顺时针）
- [x] 2.7 实现锚点数量上限（最多10个）与超限提示
- [x] 2.8 实现锚点非当前暗淡、当前高亮
- [x] 2.9 实现锚点半径缩放手势与添加态提示文案
- [x] 2.10 按性能策略实现拖拽中视觉跟随、松手后一次性重算并恢复旧位置

## 2A. 调整图片参数扩展（15项）

- [x] 2A.1 将调整图片参数从7项扩展为15项并固定顺序
- [x] 2A.2 15项全部接入整体调节与局部调节数据模型
- [x] 2A.3 15项在预览矩阵与导出流程中保持一致语义

## 3. HSL 完整能力落地

- [x] 3.1 实现 HSL 七通道选择栏与中性选中态视觉
- [x] 3.2 实现 H/S/L 三参数调节线（范围 `-100..100`、中点 `0`、精细滑动）
- [x] 3.3 实现取色器采样与最近通道映射联动
- [x] 3.4 实现 HSL 会话状态隔离与跨分组切换保值

## 4. 会话操作条与系统交互

- [x] 4.1 新建可复用 `EditorSessionOpsStrip` 组件并接入能力开关（undo/redo/compare）
- [x] 4.2 接入会话级 undo/redo 栈，限定仅作用于当前工具会话
- [x] 4.3 接入 compare 基线语义（进入会话时图像），支持按压或切换对比
- [x] 4.4 对齐局部与 HSL 的 `X / ✓` 提交/回滚语义，保证全局记录单步提交

## 5. 渲染、合成与导出

- [x] 5.1 实现局部径向渐弱模型（中心强、边缘弱）并支持多锚点稳定叠加
- [x] 5.2 实现局部15项参数实时预览管线，并抑制结构参数边缘光晕
- [x] 5.3 打通整体、局部、HSL 的预览叠加顺序与状态一致性
- [x] 5.4 在编辑完成时将整体/局部/HSL 结果落地导出到输出图，确保返回上层一致

## 6. 质量验证与回归

- [x] 6.1 验证局部锚点全流程（新增、拖拽、放大镜、查看全部、复制、删除）
- [x] 6.2 验证15项参数边界与精细调节（`-100/100`、步进连贯、线条与数值联动）
- [x] 6.3 验证 HSL 通道保值、取色映射、分组切换隔离与实时预览
- [x] 6.4 验证会话操作条 compare 行为与基线语义（已移除 undo/redo）
- [x] 6.5 运行 `flutter analyze` 并完成关键手测（进入工具、取消、确认、返回上层）

## 7. 规格文档同步

- [x] 7.1 将15项参数功能刷新到 `image-editor` 与 `pro-local-adjustment-panel` 规格
- [x] 7.2 将局部锚点新增规则（一次添加、上限10、逆时针负值环、拖拽重算策略）刷新到规格
