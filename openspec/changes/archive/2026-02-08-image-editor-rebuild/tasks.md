## 1. 外壳与三段式布局

- [x] 1.1 新建图片编辑器页面/路由，整页分为顶栏、中部、底栏三个区域，顶栏固定在上、底栏固定在下、中部占满剩余空间
- [x] 1.2 顶栏左侧实现返回按钮，中间实现图片序号（当前/总数，如 1/5），右侧实现「完成」按钮并使用 AppColors.primaryColor（蓝色）
- [x] 1.3 中部区域实现当前图片展示（支持缩放/平移以适配），无编辑时仅展示图片
- [x] 1.4 底栏实现横向编辑功能入口占位（图标+文案），预留旋转、裁剪、滤镜、专业修图、相框、文字、涂鸦、马赛克位

## 2. 编辑记录（步骤栈与 UI）

- [x] 2.1 定义编辑步骤数据模型（类型、参数、可选缩略图），实现步骤栈的增删与重算
- [x] 2.2 每次用户在某工具内点击「保存」时，将当前操作作为一步压入步骤栈并更新画布
- [x] 2.3 实现记录入口 UI（如顶栏或底栏按钮），打开后展示步骤列表
- [x] 2.4 实现「删除某步」：从步骤栈移除该步并重算其后步骤，刷新画布
- [x] 2.5 实现「重做某步」：进入该步对应工具与参数，用户可调整后再次保存并更新该步及后续

## 3. 底栏工具与选中态

- [x] 3.1 底栏实现旋转、裁剪、滤镜、专业修图、相框、文字、涂鸦、马赛克入口，使用设计系统图标与文案常量
- [x] 3.2 底栏选中某一项时高亮该入口，并触发显示对应操作面板（专业修图见第 5 组）
- [x] 3.3 实现操作面板容器：在底栏上方展示，可滑动或固定高度，包含「顶部分类、中间模板/内容、底部取消(叉)+保存(勾)+中间比例/强度」区域占位

## 4. 操作面板与具体工具（非专业修图）

- [x] 4.1 裁剪：展示可拖拽调整的裁剪框与九宫格辅助线，面板顶或中展示比例选项（自由、原图、正方形、3:2 等），底部取消/保存；保存时写入步骤栈
- [x] 4.2 滤镜：面板顶部分类（如推荐、画质、春天等），中间滤镜模板列表（缩略图+名称），底部取消/保存与强度滑块；保存时写入步骤栈
- [x] 4.3 旋转：面板或简单控件提供旋转角度/90° 等，底部取消/保存；保存时写入步骤栈
- [x] 4.4 相框、文字、涂鸦、马赛克：各实现最小可用操作面板（分类/模板 + 取消/保存 + 必要参数），保存时写入步骤栈

## 5. 专业修图子工具

- [x] 5.1 点击底栏「专业修图」进入子模式，子模式内提供曲线、白平衡、局部、修复、美丽光晕、色调对比度等入口
- [x] 5.2 各子工具使用与滤镜/裁剪一致的面板结构（参数或预设 + 取消/保存），至少实现曲线、白平衡、局部、修复、美丽光晕、色调对比度中两种以上，其余可占位或简化
- [x] 5.3 专业修图内每步保存同样写入主步骤栈，并支持在主记录中删除/重做

## 6. 与 create-flow 集成

- [x] 6.1 在 CreatePage 的图片（photo）Tab 中，将原有图片编辑区域替换为重建后的图片编辑器组件
- [x] 6.2 保证从创作入口选择「图片」后进入创作页并打开图片 Tab，完成/返回回调与草稿行为与现有流程一致
- [x] 6.3 若存在过往版本图片草稿格式，确定兼容或迁移策略并在文档中说明

## 7. 语义与收尾

- [x] 7.1 图片编辑器内全部颜色、间距、文案使用 AppColors、AppSpacing、UITextConstants，无硬编码
- [x] 7.2 运行 flutter analyze 无新增错误，完成按钮为蓝色已验收
