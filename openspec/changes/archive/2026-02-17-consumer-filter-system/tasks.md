## 1. 数据模型与常量建设

- [x] 1.1 新增消费者滤镜分类常量与文案（20 个分类：含推荐 + 去滤镜入口），统一接入 `UITextConstants`
- [x] 1.2 新增滤镜模板配置结构（`id/category/name/preview/defaultStrength/params`），并落地每类首批 3 个模板
- [x] 1.3 新增分类锚点映射数据（`category -> firstTemplateIndex`）用于分类与列表联动
- [x] 1.4 新增推荐分类聚合模型（最近使用去重排序 + 本地预置推荐补齐）
- [x] 1.5 为第二批模板（每类第 4~5 个）预留扩展字段与排序规则
- [x] 1.6 抽象配置协议与本地持久化结构（分类、模板、参数、默认强度、排序、启用态）
- [x] 1.7 预留云端更新检查接口与调度占位（遗留项，不实现网络拉取）

## 2. 面板 UI 重构（三段式）

- [x] 2.1 重构滤镜面板顶部为分类工具栏，左侧固定“去滤镜”按钮
- [x] 2.2 重构中部模板列表为“上图下名”卡片流，支持横向滑动与选中态
- [x] 2.3 重构底部中间控件为滤镜强度滑条（0~100），保留左右 `X/✓` 操作
- [x] 2.4 在纯黑背景下统一选中视觉语义（中性亮度区分，不使用蓝色主色）
- [x] 2.5 模板卡片增加缩略图 loading 态（转圈）与计算完成后的替换显示

## 3. 交互联动与状态管理

- [x] 3.1 实现点击分类定位到对应模板段（滚动到分类首模板）
- [x] 3.2 实现模板列表滑动反向更新分类高亮（双向联动）
- [x] 3.3 实现模板独立强度记忆：切换模板/回切后恢复各自强度值
- [x] 3.4 实现去滤镜行为：一键恢复 identity 模板并同步预览与滑条状态
- [x] 3.5 实现推荐分类“最近使用优先”逻辑，并在模板确认后写入最近使用记录
- [x] 3.6 最近使用不足时由本地预置推荐模板补齐推荐位

## 4. 滤镜渲染与导出一致性

- [x] 4.1 实现模板参数向量与统一强度插值（`mix(identity, template, strength)`）
- [x] 4.2 接入编辑器实时预览链路，保证当前图即时生效
- [x] 4.3 接入保存导出链路，保证“编辑器预览结果 == 返回上一层结果”
- [x] 4.4 接入历史步骤记录，保存时写入滤镜模板与强度参数，取消时回滚会话
- [x] 4.5 实现可视模板异步效果缩略图计算队列（可取消/降级），避免滑动卡顿
- [x] 4.6 实现本地资源配置加载、校验与版本缓存
- [x] 4.7 预留启动后定期检查更新的任务调度入口（仅占位，不发起网络请求）

## 5. 质量校验与体验调优

- [x] 5.1 补齐可回归用例：分类定位、滑动联动、去滤镜、模板强度记忆、推荐最近使用、保存一致性
- [x] 5.2 真机校验黑底视觉与可读性（分类、模板名、滑条、选中态）
- [x] 5.3 性能校验模板横向滚动（懒加载、缓存命中、帧率稳定）
- [x] 5.4 校验缩略图 loading 与替换行为（高频滑动、快速切图、并发任务）
- [x] 5.5 校验离线场景：无网络下可完整加载本地滤镜分类与模板
- [x] 5.6 运行 `flutter analyze` 并清理新增告警
