## 1. 专业修图分组重构

- [x] 1.1 将专业修图二级分组常量从“曝光/色彩/光影/质感”改为“基础/HSL/曲线”
- [x] 1.2 调整专业修图分组 Tab 渲染与选中态逻辑，确保默认选中“基础”
- [x] 1.3 将专业修图面板渲染改为按分组分发到独立 panel（基础/HSL/曲线）

## 2. 基础分组调节项列表

- [x] 2.1 新增基础调节项配置模型（id、图标、名称、默认值、范围）
- [x] 2.2 落地基础调节项横向滚动列表，单项垂直展示图标+名称+数值
- [x] 2.3 接入基础调节项选中状态管理，切换选中项时同步当前值展示

## 3. 统一调节线交互

- [x] 3.1 在底部 X/勾中间区域实现统一调节线控件，默认居中对应 0
- [x] 3.2 实现长按激活后左右拖动，更新当前选中项的 `-100..100` 数值
- [x] 3.3 实现调节线位置与当前选中项数值双向同步（含切换调节项后的回显）
- [x] 3.4 处理调节线手势与横向列表滑动手势冲突（分区或手势优先级）

## 4. HSL/曲线占位与隔离

- [x] 4.1 新增 HSL 独立面板占位实现（可切换、可取消/保存、无复杂参数）
- [x] 4.2 新增曲线独立面板占位实现（可切换、可取消/保存、无复杂参数）
- [x] 4.3 确保切换到 HSL/曲线后不会污染基础分组已调参数状态

## 5. 记录与确认链路

- [x] 5.1 基础分组点击勾时将当前参数写入记录步骤，点击 X 时回滚本次会话改动
- [x] 5.2 HSL/曲线占位态点击勾时无改动不写记录，避免生成无效步骤
- [x] 5.3 验证图片编辑页返回上层时仍返回最终编辑结果（与既有返回链路一致）

## 6. 质量验证

- [x] 6.1 运行 `flutter analyze` 并修复本次改动引入的问题
- [x] 6.2 进行交互回归：基础调节项选择、调节线拖动、分组切换、X/勾、记录记录
- [x] 6.3 人工验收深色主题与语义 token 使用，确认无硬编码红色与魔法数回归
