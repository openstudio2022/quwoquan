name: quwoquan_runtime_errors
description: Shared runtime failure contracts for quwoquan clients and services.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.11.0 <4.0.0'

dev_dependencies:
  test: ^1.31.0
