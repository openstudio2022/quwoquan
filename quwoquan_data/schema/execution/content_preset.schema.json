{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "内容 preset — quwoquan_data/control_plane/families/<presetId>.preset.yaml。execution spec 未显式声明的字段由 defaults deep-merge 补齐。",
  "type": "object",
  "properties": {
    "schemaVersion": { "const": "quwoquan.content.preset" },
    "presetId": { "type": "string", "description": "家族包内引用路径，如 content/travel/homepage/base" },
    "title": { "type": "string", "description": "人读 preset 名" },
    "defaults": {
      "type": "object",
      "description": "execution spec 可继承默认子集（content/acceptance/workflowPolicy 等；禁止 executionId 等运行身份字段）",
      "properties": {
        "content": { "type": "object" },
        "acceptance": { "type": "object" },
        "workflowPolicy": { "type": "object" },
        "queuePolicy": { "type": "object" }
      },
      "additionalProperties": false
    }
  },
  "required": ["schemaVersion", "presetId", "title", "defaults"],
  "additionalProperties": false
}
