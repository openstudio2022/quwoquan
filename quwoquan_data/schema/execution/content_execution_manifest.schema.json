{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "唯一内容执行工作包。每个 executionId 只允许一个可恢复的中间过程根。",
  "type": "object",
  "properties": {
    "executionId": {
      "type": "string",
      "pattern": "^20[0-9]{6}--[a-z][a-z0-9-]*-(homepage|article|image|video)-[a-z][a-z0-9-]*--[a-z0-9][a-z0-9-]*--(canary|m1|m2|m3|h10k)-[0-9]{3,}$"
    },
    "vertical": { "type": "string" },
    "contentType": { "enum": ["homepage", "article", "image", "video"] },
    "intent": { "type": "string" },
    "scope": { "type": "string" },
    "milestone": { "enum": ["canary", "m1", "m2", "m3", "h10k"] },
    "sequence": { "type": "integer", "minimum": 1 },
    "recipe": {
      "type": "object",
      "properties": {
        "ref": { "type": "string" },
        "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" }
      },
      "required": ["ref", "sha256"],
      "additionalProperties": false
    },
    "sourceDigest": {
      "type": "object",
      "properties": {
        "algorithm": { "const": "sha256" },
        "digest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
        "inputs": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "minItems": 1
        }
      },
      "required": ["algorithm", "digest", "inputs"],
      "additionalProperties": false
    },
    "modelBinding": {
      "type": "object",
      "required": [
        "provider",
        "authorModel",
        "authorModelFamily",
        "reviewerModel",
        "reviewerModelFamily"
      ],
      "additionalProperties": false,
      "properties": {
        "provider": {"const": "cursor_sdk"},
        "authorModel": {"type": "string", "minLength": 1},
        "authorModelFamily": {"type": "string", "minLength": 1},
        "reviewerModel": {"type": "string", "minLength": 1},
        "reviewerModelFamily": {"type": "string", "minLength": 1}
      }
    },
    "pricingRevision": {"type": "string", "minLength": 1},
    "rolloutContract": {
      "type": "object",
      "required": ["path", "sha256"],
      "additionalProperties": false,
      "properties": {
        "path": {"type": "string", "minLength": 1},
        "sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"}
      }
    },
    "resolvedParams": { "type": "object" },
    "selectionPolicy": { "const": "frozen" },
    "targetSetRef": { "const": "0.plan/target_set.json" },
    "targetSetSha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "retryOf": { "type": ["string", "null"] },
    "createdAt": { "type": "string" },
    "repoRoot": { "type": "string" }
  },
  "required": [
    "executionId", "vertical", "contentType", "intent",
    "scope", "milestone", "sequence", "recipe", "sourceDigest",
    "modelBinding", "pricingRevision", "rolloutContract",
    "resolvedParams", "selectionPolicy",
    "targetSetRef", "targetSetSha256", "retryOf",
    "createdAt", "repoRoot"
  ],
  "additionalProperties": false
}
