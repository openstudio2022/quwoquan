{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "quwoquan_data/schema/release/legacy_release_cohort_v1.schema.json",
  "title": "Supported legacy release cohort v1 (closed input schema)",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema",
    "producerBaselineRevision",
    "objectRefs",
    "milestone",
    "expectedCarrierCounts",
    "releaseClass"
  ],
  "properties": {
    "schema": {
      "const": "quwoquan_data.release_cohort"
    },
    "producerBaselineRevision": {
      "type": "string",
      "pattern": "^[0-9a-f]{40}$"
    },
    "objectRefs": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^(?!/)(?!.*(?:^|/)\\.\\.(?:/|$))[^\\u0000]+$"
      }
    },
    "milestone": {
      "enum": [
        "M1",
        "M10",
        "M100",
        "M1000",
        "M10000"
      ]
    },
    "releaseClass": {
      "enum": [
        "production",
        "research"
      ]
    },
    "expectedCarrierCounts": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "homepage",
        "article",
        "image",
        "video"
      ],
      "properties": {
        "homepage": {
          "type": "integer",
          "minimum": 0
        },
        "article": {
          "type": "integer",
          "minimum": 0
        },
        "image": {
          "type": "integer",
          "minimum": 0
        },
        "video": {
          "type": "integer",
          "minimum": 0
        }
      }
    }
  }
}
