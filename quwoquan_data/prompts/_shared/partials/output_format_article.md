<output_format>
- 唯一业务产物写到 task 指定的 output_path，文件名必须为 draft.article.md。
- 使用 Markdown；首行是与 target 一致的 H1，正文按 selected blueprint 使用必要的二级标题。
- blueprint 指定 figure/figuregroup 时，只按 retained evidence excerpts 中的精确 assetRef 与相对顺序放置；不新增资产引用。
- 不写 frontmatter、元数据、自检表、代码围栏或生产过程说明。
</output_format>
