<output_format>
- 唯一业务产物写到 task 指定的 output_path，文件名必须为 page.md。
- 使用带必要二级/三级标题的纯 Markdown；只写证据足以支撑的章节。
- blueprint 中已有图片占位意图时，按 retained evidence excerpts 给出的精确 id、位置与顺序保留；不新增资产引用。
- 不写 frontmatter、元数据、自检表、代码围栏或生产过程说明。
</output_format>
