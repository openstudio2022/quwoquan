# post_author article 载体 checkpoint prompt 变量契约。
# source_contract_block：由 .py 按 sourceUseMode 从 partials/author_source_adaptable.md
# 或 partials/author_source_factual.md 装配（render_partial），正文真相源仍在 partials。
system:
  required: []
  optional: []
task:
  required:
    [execution_id, content_ref, packet_path, prompt_path, writing_pack_path,
     draft_article_path, draft_meta_path, entity_name, source_contract_block]
  optional: []
