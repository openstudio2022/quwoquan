---
description: Dart 编码标准 + 设计系统 + 状态管理
globs: **/*.dart
---

# Dart 编码标准

---

## 1. 绝对禁止

### 1.1 硬编码视觉字面量

```dart
// ❌ 禁止
fontSize: 14.sp
EdgeInsets.all(16.w)
BorderRadius.circular(8)
Color(0xFF1877F2)
Container(width: 48.h, height: 48.h)
SizedBox(width: 44, height: 44)
leadingSize: 44
CupertinoListTile(leadingSize: 44, ...)

// ✅ 必须
style: AppTypography.sm
padding: EdgeInsets.all(AppSpacing.md)
borderRadius: BorderRadius.circular(AppSpacing.borderRadius)
color: AppColors.primaryColor
constraints: BoxConstraints(minWidth: AppSpacing.buttonHeight, minHeight: AppSpacing.buttonHeight)
```

### 1.2 相对路径 import

```dart
// ❌ 禁止
import '../../ui/discovery/pages/discovery_page.dart';
import '../components/media_post_card.dart';

// ✅ 必须（按领域 lib/ui/{domain}/，见 specs/01_APP_DIRECTORY_STRUCTURE_BY_DOMAIN）
import 'package:quwoquan_app/ui/discovery/pages/discovery_page.dart';
import 'package:quwoquan_app/components/media_post_card.dart';
```

### 1.3 产品名做包名/类名

```dart
// ❌ 禁止
lib/ui/assistant/pages/xiaoqu_home_page.dart   // 产品名做路径
if (msg['senderName'].toString().contains('小趣')) { }

// ✅ 必须（按领域 lib/ui/{domain}/）
lib/ui/assistant/pages/assistant_home_page.dart
if (msg['senderId'] == AppConceptConstants.assistantSenderId) { }
```

### 1.4 错误码与用户文案

错误码和用户可见文案由 `contracts/metadata/*/errors.yaml` 定义，经 codegen 生成 `*ErrorCode` 枚举。禁止硬编码 code 字符串或文案。

```dart
// ❌ 禁止
switch (e.code) {
  case 'INTEGRATION.USER.location_unavailable':
    return '暂时无法获取当前位置';
  ...
}
expect(actual.code, 'INTEGRATION.MIDDLEWARE.upstream_timeout');

// ✅ 必须（UI 展示）
IntegrationLocationErrorCode.fromCode(e.code).toDisplayMessage(context.l10n)

// ✅ 必须（测试断言）
expect(actual.code, IntegrationLocationErrorCode.upstreamTimeout.code)
```

---

## 2. 设计系统

### 2.1 颜色

| 用途 | API |
|------|-----|
| 主色 | `AppColors.primaryColor` / `primaryColorHover` / `primaryColorActive` |
| 次色 | `AppColors.secondaryColor` |
| 强调 | `AppColors.accentColor` |
| 功能 | `AppColors.success` / `warning` / `error` / `info` |
| 主题 | `AppColors.light.*` / `AppColors.dark.*` |
| 特殊 | `AppColors.special.linkColor` / `onlineColor` / `levelNColor` |

### 2.2 间距

| 层级 | API | 用途 |
|------|-----|------|
| 基础 | `AppSpacing.xs/sm/md/lg/xl` | 通用间距 |
| 语义 | `AppSpacing.semantic[DesignSemanticConstants.container]?[size] ?? AppSpacing.containerMd` | 容器/组内/组间 |
| 图标 | `AppSpacing.iconSmall/iconMedium/iconLarge` | 图标尺寸 |
| 布局 | `AppSpacing.bottomNavHeight/tabNavigationHeight/buttonHeight` | 固定高度 |
| 圆角 | `AppSpacing.smallBorderRadius/borderRadius/largeBorderRadius` | 圆角 |
| 触控/热区 | `AppSpacing.minInteractiveSize` | 可交互热区下限 44x44 |
| 图标按钮 sm | `AppSpacing.iconButtonMinSizeSm` | 图标按钮最小点击区域 44 |

**空值安全**：语义间距必须用 `?[]` + `??` 兜底。

### 2.3 文本与常量（i18n 强制）

- 所有用户可见**静态文本** → `UITextConstants.*`（禁止硬编码中文/英文字面量）
- 含参数的**模板文本**（计数、时间等）→ `context.l10n.*Template()`（定义在 `lib/l10n/app_*.arb`）
- **相对时间** → `context.l10n.justNow` / `minutesAgoTemplate` / `hoursAgoTemplate` / `daysAgoTemplate` / `monthDayTemplate`，禁止自行硬编码"刚刚""分钟前"等
- 内容类型 → `ContentTypeConstants.*`
- 概念名称 → `AppConceptConstants.*`
- 设计语义 → `DesignSemanticConstants.*`
- **门禁**：`verify_dart_semantic.py` 检测 `.dart` 文件中非 `UITextConstants`/`l10n` 的中文字面量

### 2.4 无障碍

- 可交互热区下限 44x44，主操作 48x48
- 深色背景文本/图标使用高对比（`AppColorsFunctional.getColor`）
- 文字对比度：正常文字 4.5:1（AA），大号文字 3:1

### 2.5 设置类全屏页（Inset 同源）

- **A 类**（分组列表/表单）：`SettingsInsetFormPageScaffold` + `SettingsInsetGroupedSection` / `SettingsInsetFormRow` / `SettingsInsetFormSectionDivider`（`lib/components/settings_form/`）。
- **B 类**（成员选择 + 内嵌搜索）：`SettingsInsetMemberPickerPageScaffold`（与 A **同源顶栏/灰底**）。
- **禁止**内页返回使用 `GlobalTopBarIconButton`；禁止新建 `pageBackground` + `selectionToolbarBackground` + 白顶栏作为未登记的第三套组合。
- 新增或移动受控页必须在同 PR 更新 [`scripts/settings_canonical_manifest.yaml`](scripts/settings_canonical_manifest.yaml)；例外页须在文件前部注释 `settings-canonical-exception: <理由> <CR-id>`（见 `specs/ux/page-layout-semantics.md` §4.3）。
- **门禁**：`python3 scripts/verify_settings_canonical.py`（`gate_repo.sh` app 阶段已接入）。

---

## 3. 状态管理（Riverpod）

### 3.1 State 类

- 必须不可变（`const` 构造函数）
- 必须提供 `copyWith` 方法
- 必须提供 `operator ==` 和 `hashCode`

### 3.2 StateNotifier

- 所有异步操作必须有 `try/catch` 错误处理
- 防止重复加载（`if (state.isLoading) return`）
- 错误信息使用 `UITextConstants` 或自定义异常类

### 3.3 Provider 使用

- `ref.watch` → 监听状态变化（build 中）
- `ref.read` → 执行操作（不监听）
- `ref.listen` → 副作用处理（错误提示等）
- `ref.watch(provider.select(...))` → 性能优化

### 3.4 异常层次

```
AppException (abstract)
├── NetworkException
├── ValidationException
├── AuthenticationException
├── BusinessException
└── NotFoundException
```

---

## 4. cloud/ 层约束

- `lib/cloud/models/` DTO 必须从 metadata `fields.yaml` codegen 生成（`DO NOT EDIT`）
- `lib/cloud/services/` Repository 接口必须与 `service.yaml` API 定义一致
- `lib/cloud/runtime/` 字段策略必须从 metadata 生成

---

## 5. PersonalAssistant 引擎契约约束

> 守护范围：`lib/personal_assistant/engine/`、`lib/personal_assistant/contracts/`。

### 5.1 输出契约字段必须类型化（禁止在引擎逻辑中直接写字段名字符串）

LLM 输出契约（`assistant_turn_v4`）的字段唯一定义于
`lib/personal_assistant/contracts/assistant_turn_contract.dart` 的 `AssistantTurnOutput` 类。

```dart
// ❌ 禁止：在引擎逻辑代码中直接写字段名字符串
final userMd = (parsed['userMarkdown'] as String?)?.trim() ?? '';
final decision = (parsed['decision'] as Map?)?.cast<String, dynamic>() ?? {};
final slotState = turnV2['slotState'];

// ✅ 必须：通过 AssistantTurnOutput.tryParse() 类型化访问
final turn = AssistantTurnOutput.tryParse(parsed);
final userMd = turn?.userMarkdown ?? '';
final decision = turn?.decision ?? const {};
final slotState = turn?.slotState ?? const {};
```

**唯一允许出现字段名字符串字面量的地方**：`AssistantTurnOutput.fromJson()` / `tryParse()` 方法内部。

### 5.2 完整契约字段清单（AssistantTurnOutput 必须覆盖）

| 字段 | 类型 | 说明 |
|------|------|------|
| `contractVersion` | `String` | 契约版本标识 |
| `decision` | `Map<String, dynamic>` | 含 `nextAction`、`confidence`、`reasoning` |
| `messageKind` | `String` | `progress`/`answer`/`ask_user`/`error`/`fallback` |
| `userMarkdown` | `String` | 面向用户的 Markdown 回答 |
| `result` | `Map<String, dynamic>` | 含 `text`、`summary`、`actionHints`、`interpretation` |
| `evidence` | `List<Map>` | 证据列表，每项含 `text`/`url` |
| `reasoningBasis` | `List<Map>` | 推理路径 |
| `selfCheck` | `Map<String, dynamic>` | 自检结果 |
| `diagnostics` | `Map<String, dynamic>` | 含 `emergedTags` |
| `modelSelfScore` | `Map<String, dynamic>` | 含 `score`、`reason` |
| `toolCalls` | `List<Map>` | 待执行工具调用序列；每项含 `toolName`/`name`、`arguments`、`toolCallId` |
| `slotState` | `Map<String, dynamic>` | 槽位状态 |
| `askUser` | `Map<String, dynamic>` | 追问信息 |
| `subagentPlan` | `List<Map>` | 含 `goal` |
| `missingContextSlots` | `List<String>` | 缺失上下文槽 |
| `fillGuidance` | `List<Map>` | 含 `guidance` |
| `followupPrompt` | `String` | 追问提示 |

### 5.3 版本管理：最多两个活跃版本

- **当前版本**：`assistant_turn_v4`（prompt 模板输出，代码内标准格式）
- **兼容遗留版本**：`assistant_turn_v3`（加载旧历史数据时识别，读后立即升级）
- **禁止**：同时存在三个及以上版本的识别路径（如 v2 || v3 || v4）
- **迁移要求**：`AssistantSessionManager.load()` 必须在读取完成后调用 `_migrateSessionsToCurrentVersion()`，将 content 中的 v2/v3 格式升级为 v4

```dart
// ❌ 禁止：三路版本判断
if (version == 'assistant_turn_v2' || version == 'assistant_turn_v3' || version == 'assistant_turn_v4')

// ✅ 必须：最多两版本（当前 + 一个遗留）
if (version == 'assistant_turn_v4' || version == 'assistant_turn_v3')
// 旧包裹格式兼容
final wrapped = parsed['assistant_turn_v2'];
```

### 5.4 修改契约字段的强制流程

```
1. 修改 prompt 模板（assets/personal_assistant/prompts/global/phase.output_contract.*.md）
2. 更新 assets/personal_assistant/prompts/_standards/output_contracts.json
3. 更新 AssistantTurnOutput 类（contracts/assistant_turn_contract.dart）
4. 检查 _ensureAssistantTurnEnvelopeText / session 迁移逻辑是否需同步
5. flutter test test/personal_assistant/
```

**禁止**：修改 prompt 模板字段后不同步更新 `AssistantTurnOutput`，或直接在引擎逻辑中加字符串 key 访问新字段。

### 5.5 遗留字段清理规范

- `userFacingMarkdown`：旧字段名，禁止在新代码中使用；已有引用须替换为 `userMarkdown`
- `assistant_turn_v2` 作为 `contractVersion` 值：禁止在新写入的 session 数据中使用；仅作为旧包裹格式解包的 key 保留

---

## 6. UI 层约束（按领域，见 specs/01_APP_DIRECTORY_STRUCTURE_BY_DOMAIN）

- `lib/ui/{domain}/` 必须包含 `pages/` 子目录（禁止在 `lib/features/` 下新建）
- UI 模块不允许直接 import 其他 UI 模块内部文件（通过 `core/` 或 `cloud/` 共享）
- Provider 必须使用 Riverpod 标准模式
- 更新特性树 `acceptance.yaml` 与 traceability 路径
- 选择器/创作等 Cupertino 页面遵循 iOS 语义 v1：Modal leading 统一 `CupertinoIcons.xmark`，Stack 子页使用 `CupertinoIcons.back/chevron_back`
- `CupertinoPageScaffold` 场景禁止混用 Material 交互组件（如 `Checkbox`、`SnackBar`）；选择态使用 Cupertino 或语义化自绘组件
- 禁止跨域借用文案 key（如图片编辑的“完成”用于圈子选择页）

---

## 7. 响应式设计

- 断点语义：`compact < 360` / `regular 360-599` / `expanded >= 600`
- 通过 `AppSpacing.responsiveValue` / `AppTypography.responsive` 控制差异
- 组内间距 `intraGroup*`，组间间距 `interGroup*`

---

## 8. 提交前检查

```bash
flutter analyze
python3 scripts/verify_dart_semantic.py
python3 scripts/verify_settings_canonical.py
```

检查项：所有颜色/间距/文本/类型使用语义标签，无硬编码，无相对路径 import，无产品名做标识符；同时检查 iOS 语义（行尾箭头、Cupertino 页面组件边界、selector leading）；设置类全屏页与 `settings_canonical_manifest.yaml` 一致（§2.5）。
