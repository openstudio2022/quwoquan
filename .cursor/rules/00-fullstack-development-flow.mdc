---
description: 端云一体开发宪法：一棵树、验收、测试与商用准入
alwaysApply: true
---

# 端云一体开发宪法

唯一主线：`specs/00_MASTER_DEVELOPMENT_FLOW.md`。

## 一棵树

所有增量必须归属：

```text
AppRoot -> L1_domain_service -> L2_business_capability -> L3_story
```

`Journey/Scenario` 只在应用根 registry 与 UAT 中表达；临时执行计划和会话 todo 不属于正式特性树文档。

## 入口检查

命令或直接对话开发开始前，必须能回答：

- AppRoot Journey/Scenario 是否受影响。
- `L1_domain_service / L2_business_capability / L3_story` 分别是什么。
- 验收意图：UAT / SIT / GWT / contract。
- 测试证据：T1 / T2 / T3 / T4。
- 是否触发 metadata-first、env-seed-first、mock 隔离、页面质量、runtime error 等横切规则。

缺任一关键项：`GATE_BLOCK`，先补规格、验收、设计或测试方案。

入口检查与自然语言路由的完整契约见 `docs/agent_context_contract.md`。用户直接输入“帮我修”“实现一下”“检查一下”时，不得跳过阶段识别、正向规格理解和执行前反思。

## 军规

- `spec-first`、`acceptance-first`、`test-first`。
- `metadata-first`：字段、错误码、path、operation、surface、route、decoder context 先改 metadata。
- `env-seed-first`：alpha/beta/gamma 数据先进入 metadata fixtures 与 seed manifest。
- `design-scoped`：设计只落在应用根、领域服务、业务能力层。
- `single-source`：不得维护第二套树、路由、错误码、surface、operation、mock 数据。
- 用户可见且可发布能力必须先冻结 SLO/KPI、权限、生命周期、灰度、回滚和观测。
- `/dev` 不得停在部分端、部分测试或无证据状态。
- 跨 App / Service / Data / Ops / Portal 的任务必须证明端到端链路闭环，不能只完成局部文件改动。

## 测试映射

- AppRoot UAT：主证据 `T4`，辅以 `T3`。
- 领域服务：主证据 `T1/T3`，必要时 `T4`。
- 业务能力 SIT：主证据 `T2/T3`。
- Story GWT/contract：主证据 `T1/T2`，涉及远端补 `T3`。

## 完成后复盘

每次完成非纯查询任务，最终说明或验收文档必须覆盖适用项：规格达成、测试证据、E2E 验证、产品/UX、运营观测、自动化/门禁、剩余风险。不适用项需说明原因。
