---
description: quwoquan 端云一体核心规则（DDD + 契约 + 元数据 + runtime + 全栈看护）
alwaysApply: true
---

# quwoquan 端云一体核心规则（SDD：Spec-Driven Development）

> 端侧不再单独维护规则和命令，所有开发均为端云一体化。
> 唯一主线见 `specs/00_MASTER_DEVELOPMENT_FLOW.md`。

## 核心原则

- `spec-first`：需求规格（spec + acceptance）先于设计，设计先于实现。
- `contracts-first`：先 contracts/metadata YAML，再 codegen，再业务逻辑。
- DDD 分层：domain → application → adapters → infrastructure（单向依赖）。
- 元数据驱动：字段策略由 `contracts/metadata/*` 声明，禁止硬编码。
- 横切统一 `runtime`，领域服务禁止自行实现横切（`01-arch-constraints §1.3`）。
- 验收先行：`acceptance.yaml` A1~An 须在实施前定义。
- 扩展遵从：25 个场景必须经 `/extend` 入口，metadata-first → codegen → 测试 → gate。
- 门禁拦截：`make gate` / `make gate-full` 全部通过才可合入。
- **原型例外**：`/try` 豁免特性树分解，其余约束完整遵从；验证成功后必须执行 `/land` 基线化。

## SDD 阶段卡点

```
Phase Gate → 阶段准入自检（PRD/Design/Dev/Deliver Gate）
G0  explore → prd       PRD Gate 通过
G1  prd → design        Design Gate + make verify-metadata + make codegen
G2  design → dev        Dev Gate + 每 task 后 make build + make test-contract
G3  dev → verify        make gate-full
G4  verify → commit     L1+L2 门禁 + 审计 + git commit + push
G5  commit → deploy     部署 integration + L3/L4 + 灰度 prod
```

## 规则集

| 编号 | 守护内容 |
|-----|---------|
| 00 | 本规则：端云一体核心（SDD 主线） |
| 01-arch-constraints | DDD 导入 + 数据库隔离 + runtime 统一 + codegen 保护 + 25 扩展场景 |
| 02-dart-coding | Dart 编码标准 + 设计系统 + 状态管理 |
| 03-testing | 端云契约测试标准 |

## SDD 命令集

| 命令 | 用途 |
|------|------|
| `/explore` | 探索思考，不写代码（SDD 入口） |
| `/prd` | 需求规格基线（spec + acceptance 草稿） |
| `/design` | 设计基线（design + tasks + metadata + codegen） |
| `/dev` | 实施特性（逐 task，每 task G2 卡点） |
| `/archive` | 仅归档（G3 + 回写特性树状态） |
| `/commit` | 归档 + 提交入库（= /archive + submit） |
| `/deploy` | 部署（integration → L3/L4 → prod 灰度） |
| `/deliver` | 全链路：dev + archive + commit 一气呵成 |
| `/verify` | 特性级漂移检测 + G3 |
| `/audit` | 代码库级结构健康度审计 |
| `/try` | 原型模式（豁免特性树，其余约束完整遵从） |
| `/land` | 原型落地基线化（try 验证成功后） |
| `/extend` | 实施阶段增量扩展（S01~S25） |
| `/prune` | 清理过期特性树节点 |

