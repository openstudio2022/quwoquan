---
description: quwoquan 全栈核心军规：分层、metadata、测试、发布
alwaysApply: true
---

# quwoquan 全栈核心军规

## 必守

- 一棵树：`AppRoot -> L1_domain_service -> L2_business_capability -> L3_story`。
- 命令和自然语言输入等价：用户未显式输入 slash command 时，也必须按 `docs/agent_context_contract.md` 识别阶段、区域、规格和三层测试证据。
- 正式文档：应用根/领域/能力用 `spec.md + design.md + acceptance.yaml`；Story 只用 `spec.md + acceptance.yaml`。
- 测试语言：验收意图为 UAT/SIT/GWT/contract，测试工程层只用 `local_contract`、`api_integration`、`user_acceptance`。
- DDD 依赖方向固定：`domain <- application <- adapters <- infrastructure`。
- `quwoquan_service/contracts/metadata/**` 是字段、错误码、path、operation、surface、route、decoder context 的唯一真相源。
- codegen 文件禁止手改；改行为先改 metadata，再 verify/codegen。
- UI、Repository、Router、埋点和文档不得维护第二套 IA、path、surface、operation 或 decoder 规则表。
- Mock 数据必须隔离；生产包默认 Remote，无 Mock/Remote 切换入口。
- 涉及页面数据、Repository、Remote API、人工 beta 或端云测试时，先补 fixture/seed manifest。
- 契约单轨：禁止协议版本信封（`schemaVersion` / `_vN` / 数字 schema）、wire 多键双读、客户端 JSON `_id` 出站/解码与正向 alias 兼容测试；门禁 `python3 quwoquan_ops/gate/verify_single_track_contracts.py` 为 GATE_BLOCK。
- 存储 `_id`（bson/Mongo seed）≠ wire `_id`；wire 只认业务键；projection `source: _id` 合法，codegen 不得把 source 当成 JSON 键。

## 阻断

- 旧树、树内临时计划文档、Story 设计文档回归。
- UI 直连 mock 或在 UI 复制业务假数据。
- 硬编码错误码、路径、surface、operation、route。
- 用户可见能力缺 SLO/KPI、权限、生命周期、灰度、回滚或观测。
- `implemented/completed` 但无测试证据。
- 跨 App / Service / Data / Ops 任意两个以上区域却没有端到端证据闭环。
- 协议版本分支、wire 双读、`--warn-only`/`mode=compat` 逃逸、正向「旧键仍可解析」测试。

## 交付

- `/explore`：定位树归属和风险。
- `/prd`：冻结规格与验收。
- `/design`：冻结应用根/领域/能力设计。
- `/dev`：按 metadata -> codegen -> 业务逻辑 -> 测试闭环。
- `/verify`：检查 UAT/SIT/GWT/contract 与三层测试证据。
- `/commit` / `/deploy`：以门禁、SLO、灰度与回滚演练为准。
