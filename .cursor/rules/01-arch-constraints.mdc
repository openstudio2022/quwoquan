---
description: 端云结构约束（DDD + runtime + mock/remote + codegen + 0→1 严格遵从）
alwaysApply: true
---

# 端云结构约束

---

## 1. Go 服务结构约束（0→1 严格遵从）

> 当前无任何服务实现，第一个服务必须严格遵从以下约束，作为后续所有服务的标杆。

### 1.1 服务目录结构（强制）

```
services/{service-name}/
├── cmd/api/main.go                    # 标准启动：Registry → Repository → HTTP
├── internal/
│   ├── domain/                        # 领域层（codegen + 手写领域逻辑）
│   │   ├── {entity}.go                # [codegen] Entity struct
│   │   ├── {entity}_repository.go     # [codegen] Repository interface
│   │   ├── {entity}_events.go         # [codegen] Event struct
│   │   └── {entity}_service.go        # [手写] 领域服务
│   ├── application/                   # 应用层（手写）
│   │   └── {usecase}_service.go       # 用例编排、事务边界
│   ├── adapters/                      # 适配器层
│   │   ├── http/
│   │   │   └── {entity}_handler.go    # [codegen 骨架 + 手写路由]
│   │   └── mq/
│   │       └── {event}_consumer.go    # [codegen 骨架 + 手写消费]
│   └── infrastructure/                # 基础设施层
│       ├── persistence/
│       │   └── {entity}_{storage}_repo.go  # [codegen] Repository 实现
│       ├── cache/
│       │   └── {entity}_cache.go      # [codegen] Redis 缓存中间件
│       └── migration/
│           └── {version}_{entity}.up.{sql|js}  # [codegen] DDL/索引
├── tests/
│   ├── testmain_test.go               # [codegen] 测试引擎启动/关闭
│   ├── fixture_test.go                # [codegen] Fixture 工厂
│   └── {entity}_contract_test.go      # [codegen 骨架 + 手写断言]
├── configs/config.yaml
├── go.mod
└── Makefile
```

**创建服务只能通过 `/qwq-extend new-service`，禁止手动 mkdir。**

### 1.2 DDD 导入约束

```
domain ← application ← adapters ← infrastructure（单向依赖）
```

| 层 | 允许 import | 禁止 import |
|----|-----------|------------|
| `domain/` | 标准库, `quwoquan_service/runtime/` 接口 | application, adapters, infrastructure, 数据库驱动 |
| `application/` | domain, 标准库, `quwoquan_service/runtime/` | adapters, infrastructure, 数据库驱动 |
| `adapters/` | domain, application, 标准库, `quwoquan_service/runtime/` | infrastructure 内部（仅通过 domain 接口间接） |
| `infrastructure/` | 全部内部层, `quwoquan_service/runtime/`, 数据库驱动 | — |

数据库驱动（`go.mongodb.org`, `jackc/pgx`, `go-redis`, `database/sql`）仅允许在 `infrastructure/` 和 `tests/`。

### 1.3 runtime 统一能力

| 能力 | 必须使用 | 禁止 |
|------|---------|------|
| 错误处理 | `runtime/errors.AppError` | 自定义 error struct 返回 HTTP |
| HTTP 服务 | `runtime/http.NewServer` + `runtime/observability` 中间件 | 裸 `net/http` 自建 server |
| 配置读取 | `runtime/config.RuntimeConfigProvider` | `os.Getenv` 读业务配置 |
| 消息发布 | `runtime/messaging.MessageEnvelope` | 自定义 MQ 序列化 |
| Repository | `runtime/repository.Repository[T]` 接口 | 自定义 DAO/ORM |
| 实体注册 | `runtime/registry.EntityRegistry` | 硬编码实体元数据 |
| 拦截链 | `runtime/interceptor` | 手写字段过滤/脱敏 |
| 可观测 | `runtime/observability` | 自建日志/指标/追踪 |
| 治理 | `runtime/governance` | 自实现熔断/限流 |

### 1.4 main.go 标准启动模板

```go
func main() {
    // 1. 加载配置
    cfg := config.MustLoad("configs/config.yaml")
    // 2. 初始化 Observability
    shutdown := observability.MustInit(cfg)
    defer shutdown()
    // 3. 加载 EntityRegistry（从 metadata）
    reg := registry.MustLoad(cfg.MetadataDir)
    // 4. 初始化 Repository（由 Registry 驱动存储路由）
    repos := repository.MustInitFromRegistry(reg, cfg)
    // 5. 构建 HTTP handler（注入 domain service + interceptor）
    handler := http.NewServer(cfg, reg, repos)
    // 6. 启动（含优雅关闭）
    http.ListenAndServe(handler, cfg)
}
```

禁止跳过 Registry/Repository/Interceptor 直接操作数据库。

### 1.5 codegen 保护

- `// Code generated ... DO NOT EDIT.` 标记的文件禁止手改
- `make gate` 通过 hash 比对检测
- 如需修改 codegen 产物的行为 → 修改 metadata YAML → 重新 `make codegen`

---

## 2. Dart 端侧结构约束

> 基于当前已有代码模式，将其固化为强制约束。

### 2.1 cloud/ 层目录结构（强制）

```
lib/cloud/
├── runtime/
│   ├── generated/                              # [codegen] 产物根目录，按业务域分子目录
│   │   └── {domain}/                          # 与 services/{domain}/ 保持一致
│   │       ├── post_base_dto.dart             # [手写] 同域 DTO 共享抽象基类
│   │       ├── {entity}_dto.g.dart            # [codegen] 类型化 DTO（DO NOT EDIT）
│   │       └── {domain}_metadata.g.dart       # [codegen] 运行时常量（DO NOT EDIT）
│   ├── cloud_runtime_config.dart              # 网关地址（dart-define 注入）
│   ├── cloud_request_headers.dart             # 请求头（追踪 + 鉴权）
│   ├── field_policy.dart                      # [codegen] 字段策略
│   └── error_codes.dart                       # [codegen] 错误码枚举
└── services/
    └── {domain}/                              # 与 generated/{domain}/ 保持一致
        ├── mock/
        │   └── {domain}_mock_data.dart        # canonical 字段 mock 数据
        └── {domain}_repository.dart           # Abstract + Mock + Remote
```

**content 域当前产物示例：**
```
lib/cloud/runtime/generated/content/
├── post_base_dto.dart          # [手写] 共享抽象基类（id/type/authorId/stats/createdAt）
├── photo_post_dto.g.dart       # [codegen] PhotoPostDto（width/height/imageUrls）
├── video_post_dto.g.dart       # [codegen] VideoPostDto（width/height/durationMs）
├── article_post_dto.g.dart     # [codegen] ArticlePostDto（title/body/coverUrl）
├── moment_post_dto.g.dart      # [codegen] MomentPostDto（body/imageUrls?/videoUrl?）
└── content_metadata.g.dart     # [codegen] 运行时常量
```

### 2.2 Repository 三层模式（强制）

每个云服务对应的 Repository 必须遵循当前已有的三层模式：

```dart
// ① Abstract 接口（与 service.yaml API 一一对应）
abstract class XxxRepository {
  Future<List<Map<String, dynamic>>> listItems({int limit = 20});
  Future<Map<String, dynamic>> getItem(String id);
  // ... 其他 service.yaml 中定义的 API
}

// ② Mock 实现（本地/原型数据，不发 HTTP）
class MockXxxRepository implements XxxRepository {
  @override
  Future<List<Map<String, dynamic>>> listItems({int limit = 20}) async {
    // 返回本地 mock 数据
  }
}

// ③ Remote 实现（调用云侧 API）
class RemoteXxxRepository implements XxxRepository {
  final http.Client _client;
  RemoteXxxRepository({http.Client? client}) : _client = client ?? http.Client();

  @override
  Future<List<Map<String, dynamic>>> listItems({int limit = 20}) async {
    final url = XxxApiMetadata.listItemsUri(limit: limit);
    final resp = await _client.get(
      url,
      headers: CloudRequestHeaders.forSurfaceOperation(
        surfaceId: XxxSurfaceIds.listPage,
        operationId: XxxOperationIds.listItems,
      ),
    );
    // 统一错误处理 ...
  }
}
```

**禁止行为**：
- Mock 实现禁止发 HTTP 请求
- Remote 实现必须使用 `CloudRuntimeConfig.gatewayBaseUrl`，禁止硬编码 URL
- Remote 实现必须使用 metadata/codegen 生成的 path builder、operation 常量、surface 常量；禁止硬编码 path、`pageId`、`surfaceId`、`operationId`
- Remote 请求头必须来自统一 header builder（过渡期可兼容 `forPage(pageId)`，但 `pageId` 也必须是 codegen 常量，禁止字符串字面量）
- Remote 响应解析必须统一处理 ErrorResponse（对应云侧 `runtime/errors`）

### 2.2.1 Operation / Surface / Route 元数据分层（强制）

客户端请求与导航相关标识必须按以下唯一真相源分层：

| 标识 | 唯一真相源 | 代码消费位置 |
|---|---|---|
| API `path_template` / `method` / `operation` | `service.yaml` | Repository / contract tests / codegen |
| `operation_id` / decoder context | `service.yaml`（或其 codegen 产物） | Request headers / `CloudResponseDecoder` |
| `surface_id` / `route_id` / `path_template` | `ui_config.yaml` 或 `ui_surfaces.yaml` | `app_router.dart` / navigation helpers / UI tests |

**禁止行为**：
- 在 codegen 中维护 operation→surface / route→page override map 作为第二真相源
- 在 Repository 中手写 `CloudResponseDecoder.context: '...'`
- 在 Router 中手写 `path: '/biz-path'`、`context.go('/biz-path')`
- 新增 operation / surface / route 时跳过 metadata 直接改业务代码

### 2.3 Mock/Remote 切换（强制 Provider 模式）

切换必须通过 `lib/core/providers/app_providers.dart` 的 Riverpod Provider：

```dart
// lib/core/services/app_content_repository.dart — 数据源模式
enum AppDataSourceMode { mock, remote }

// lib/core/providers/app_providers.dart — Repository 工厂
final xxxRepositoryProvider = Provider<XxxRepository>((ref) {
  final mode = ref.watch(appDataSourceModeProvider);
  return mode == AppDataSourceMode.remote
      ? RemoteXxxRepository()
      : MockXxxRepository();
});
```

**禁止行为**：
- 禁止 UI 直接 `new MockXxxRepository()` 或 `new RemoteXxxRepository()`
- 禁止 UI 内自行判断 mock/remote（必须通过 Provider 透明切换）
- 禁止在 UI 代码中 import `AppDataSourceMode`
- 新增 Repository → 必须同步在 `app_providers.dart` 注册 Provider

### 2.4 UI 层约束（按领域服务划分）

> **规划文档**：`specs/01_APP_DIRECTORY_STRUCTURE_BY_DOMAIN.md`  
> `lib/features/` 已废弃，**新建领域服务及页面必须在 `lib/ui/{domain}/` 下**，与 metadata 领域对齐。

```
lib/ui/{domain}/              # domain 与 contracts/metadata 一致
├── pages/          # 必须存在（页面入口 Widget）
├── providers/      # 状态管理（Riverpod Notifier/Provider）
├── widgets/        # 页面专属子组件
└── models/         # 仅页面可见的 ViewModel（非 cloud DTO）
```

**领域示例**：`content`（含 entry 创作入口）、`discovery`、`chat`、`user`、`circle`、`assistant`、`settings`、`welcome`。

**Discovery 域示例：**
```
lib/ui/discovery/
├── pages/
│   └── discovery_page.dart         # 发现页
├── providers/
│   └── discovery_feed_provider.dart
└── widgets/
    ├── photo_feed_grid.dart
    ├── video_feed_view.dart
    └── ...
```

**创作入口（content/entry）示例：**
```
lib/ui/content/entry/               # 创作入口，归属 content 域
├── pages/
│   ├── create_page.dart
│   └── publish_location_selector_page.dart
├── providers/
└── widgets/
```

| 约束 | 说明 |
|------|------|
| 目录必须在 `lib/ui/{domain}/` 下 | **禁止**在 `lib/features/` 下新建页面或领域模块 |
| 必须含 `pages/` | UI 模块入口 |
| 禁止 import 其他 UI 模块内部 | 共享通过 `lib/core/` 或 `lib/cloud/` |
| 必须通过 Provider 访问 Repository | `ref.read(xxxRepositoryProvider)` |
| 错误处理必须 fallback | catch 异常 → 降级到 mock 或 UI 提示 |
| 禁止在 UI 层直接操作 `Map<String, dynamic>` | 必须用 `PostBaseDto` 子类或 ViewModel 包装 |
| 页面路由与 surface 标识必须来自 metadata codegen | 禁止在 UI / Router 中硬编码业务 path、route_id、surface_id |

### 2.5 新增 Repository 端云对齐检查清单

新增一个云服务 Repository 时，以下必须全部完成：

```
☐ contracts/metadata/{entity}/service.yaml 已定义 API
☐ make codegen-app 已生成 DTO
☐ lib/cloud/services/{domain}/{domain}_repository.dart 已创建（三层模式）
☐ Abstract 接口方法与 service.yaml API 一一对应
☐ Remote 实现 URL 路径与 service.yaml routes 一致
☐ Remote 实现使用 CloudRuntimeConfig + CloudRequestHeaders
☐ Mock 实现使用本地数据，不发 HTTP
☐ lib/core/providers/app_providers.dart 已注册 Provider
☐ Provider 使用 appDataSourceModeProvider 切换
```

### 2.6 现有代码适配路径

当前已存在代码的迁移策略（历史存留，逐步重构）。**完整规划**见 `specs/01_APP_DIRECTORY_STRUCTURE_BY_DOMAIN.md`。

| 现有文件/目录 | 目标位置 | 处理方式 |
|-------------|---------|---------|
| `lib/features/create/` | `lib/ui/content/entry/` | 创作入口归属 content 域 |
| `lib/features/chat/` | `lib/ui/chat/` | 按领域迁移 |
| `lib/features/circles/` | `lib/ui/circle/` | 按领域迁移 |
| `lib/features/profile/` | `lib/ui/user/` | 与 ui/user 合并 |
| `lib/features/assistant/` | `lib/ui/assistant/` | 按领域迁移 |
| `lib/features/settings/` | `lib/ui/settings/` | 按领域迁移 |
| `lib/features/welcome/` | `lib/ui/welcome/` | 按领域迁移 |
| `cloud/runtime/generated/feed_item_dto.g.dart` | `cloud/runtime/generated/content/photo_post_dto.g.dart` 等 | 拆分为类型化 DTO，逐步替换 |
| `cloud/services/content/content_repository.dart` | 保留，返回类型由 `FeedItemDto` 改为 `PostBaseDto` 子类 | 分类型返回，逐类迁移 |
| `core/providers/app_providers.dart` | 保留，新增 Repository 追加注册 | 持续演进 |
| `core/services/app_content_repository.dart` | 保留 AppDataSourceMode，逐步迁移遗留 DataService | 持续演进 |

---

## 3. 端云一致性约束

### 3.1 接口一一对应

| 云侧 | 端侧 | 一致性来源 |
|------|------|-----------|
| `internal/domain/{entity}.go` struct | `cloud/models/{entity}_dto.dart` | `fields.yaml` |
| `internal/domain/{entity}_events.go` | `cloud/events/{entity}_events.dart` | `events.yaml` |
| `internal/adapters/http/{entity}_handler.go` routes | `cloud/services/{domain}/{domain}_repository.dart` Abstract 方法 | `service.yaml` |
| `runtime/errors` 错误码 | `cloud/runtime/error_codes.dart` | `contracts/error_codes.md` |
| `runtime/interceptor` 字段策略 | `cloud/runtime/field_policy.dart` | `fields.yaml` |

### 3.2 metadata↔代码同步

- Go struct / Dart DTO / OpenAPI / Migration 必须与 metadata YAML 一致
- `make verify` + `make gate` 自动校验
- 不一致 → `make gate` 失败

### 3.3 错误码 metadata 约束

错误码与用户可见文案**唯一定义于** `contracts/metadata/{domain}/{entity}/errors.yaml`，禁止在业务代码中硬编码。

| 层级 | 必须使用 | 禁止 |
|------|----------|------|
| **metadata** | `errors.yaml` 定义 code、l10n_key、user_message.zh/en、go_const、dart_const | 无 errors.yaml 就手写错误码/文案 |
| **云侧 Handler** | `generated.AppErrorFrom*(debugMessage)` 等 codegen 产出的方法 | 硬编码 user_message、code 字符串 |
| **端侧 UI/Repository** | `*ErrorCode.fromCode(e.code).toDisplayMessage(context.l10n)` | switch/case 硬编码 code 字符串或文案 |
| **测试** | 枚举 `.code`（如 `IntegrationLocationErrorCode.upstreamTimeout.code`） | 硬编码 `'INTEGRATION.MIDDLEWARE.upstream_timeout'` 等字符串 |

**顺序**：metadata → make verify-metadata → make codegen / make codegen-app → 业务逻辑。门禁 `verify_error_code_semantic.py` 检测端侧错误码字符串硬编码。

---

## 4. 扩展场景约束

**核心顺序**（每个扩展操作强制）：
```
metadata YAML → make verify → make codegen → 业务逻辑 → make test-contract → make gate
```

**禁止行为**：
- 不经 metadata 直接硬编码错误码或 user_message（必须 errors.yaml → codegen）
- 不经 `/qwq-extend` 或 metadata 直接创建 Go struct / Dart DTO / Repository
- 不经 codegen 手写 Repository 实现或 migration
- 手动创建 `services/{name}/` 目录（必须通过 `/qwq-extend new-service`）
- 在 domain 层 import 数据库驱动
- 在 application 层直接操作数据库
- 不经 EntityRegistry 注册就创建 Repository
- 绕过拦截链直接返回 SECRET/PII 字段
- 不经 `make verify` 就执行 `make codegen`
- 在 Feature 中直接实例化 Mock/Remote Repository
- Remote 实现中硬编码 URL 或自建 headers
- 在同一环境将一个 domain 归属到多个部署进程
- 允许 integration/prod 拓扑映射不一致
- 通过部署拓扑变更对外领域 API 路由契约

### 4.1 部署拓扑映射约束（新增）

部署映射唯一真相：`deploy/shared/process_domain_mapping.yaml`

结构：
```yaml
environments:
  dev:
    content-service:
      domains: [content]
  integration:
    quwoquan_service:
      domains: [content, chat, ...]
  prod:
    quwoquan_service:
      domains: [content, chat, ...]
```

强制规则：
- 同一环境中 domain 只能出现一次
- `integration` 与 `prod` 映射必须一致
- 进程名使用 `*-service` 或 `quwoquan_service`

**扩展入口**：`/qwq-extend <scenario>`，详见 `specs/runtime_extension_catalog.md`。
