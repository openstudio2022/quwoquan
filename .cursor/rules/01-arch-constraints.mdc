---
description: 端云结构约束（DDD + Object Facade/Data Ports + metadata/codegen）
alwaysApply: true
---

# 端云结构约束

---

## 1. Go 服务结构约束（0→1 严格遵从）

权威设计：

- `specs/feature-tree/runtime/system-architecture-and-engineering-guide/design.md`
- `specs/feature-tree/runtime/system-architecture-and-engineering-guide/acceptance.yaml`
- `quwoquan_service/contracts/metadata/DESIGN.md`

本规则只保留执行约束，不复制对象边界或 compiler 设计。

### 1.1 服务目录结构（强制）

```
services/{service-name}/
├── cmd/{entry}/main.go                # 显式 composition root
├── internal/
│   ├── domain/{bounded-context}/{object}/
│   │   ├── model/                     # aggregate / owned entity / value object
│   │   └── ports/                     # 对象专属 AggregateStore 等领域端口
│   ├── application/{bounded-context}/{object}/
│   │   ├── *_command_facade.go        # command facet
│   │   ├── *_query_facade.go          # query facet
│   │   ├── *_reader.go                # 业务命名 Reader port
│   │   └── *_slice.go                 # typed Slice
│   ├── adapters/
│   │   ├── http/
│   │   │   └── *_handler.go           # generated operation → Facade
│   │   └── mq/
│   │       └── *_consumer.go          # typed event → application
│   └── infrastructure/
│       ├── {bounded-context}/{object}/persistence/ # 对象 Store/Reader 的具体实现
│       ├── {bounded-context}/{object}/projection/  # projector/checkpoint/rebuild
│       ├── {bounded-context}/{object}/cache/       # named Reader 的缓存实现
│       ├── {bounded-context}/{object}/external/    # 第三方或跨域 adapter
│       └── migration/                 # generated DDL/索引
├── tests/
│   ├── local_contract/
│   └── api_integration/
└── configs/
```

服务统一使用根 `quwoquan_service/go.mod`。创建服务经 `/extend new-service`，
并实际调用 `make new-service SERVICE=<name>-service PORT=<port>`；禁止手工复制旧服务骨架。

### 1.2 DDD 导入约束

```
domain ← application ← adapters ← infrastructure（单向依赖）
```

| 层 | 允许 import | 禁止 import |
|----|-----------|------------|
| `domain/` | 标准库, `quwoquan_service/runtime/` 跨域机制接口 | application, adapters, infrastructure, 数据库驱动 |
| `application/` | domain, 标准库, `quwoquan_service/runtime/` 跨域机制接口 | adapters, infrastructure, 数据库驱动 |
| `adapters/` | domain, application, 标准库, `quwoquan_service/runtime/` 跨域机制接口 | infrastructure 内部（仅通过对象专属 port 间接） |
| `infrastructure/` | 全部内部层, `quwoquan_service/runtime/`, 数据库驱动 | — |

数据库驱动（`go.mongodb.org`, `jackc/pgx`, `go-redis`, `database/sql`）仅允许在 `infrastructure/` 和 `tests/`。

### 1.2.1 限界上下文与子对象访问

- 每个 domain 的唯一 `business_object_map.yaml` 必须完整登记 stable context id、object、
  owner、access policy 与 relationship；未登记对象不允许实现或暴露 operation。
- 跨上下文写入只调用目标对象 public Command Facade，读取只调用 named Reader/typed
  Slice，异步协作只消费 public event；禁止 import 目标内部包、直连其 DB/cache/index。
- `application.aggregate_owner` 只允许指向 `aggregate_root`；append-only fact 的 POST
  command 必须使用与其互斥的 `application.append_sink`，禁止把事实伪装为聚合根。
- `owned_entity/value_object` 只能以 `aggregateId + childId` 经聚合根行为访问；禁止子对象
  Repository/Store/Facade、跨上下文 relationship 或直接 persistence 查询。
- 若子对象需要独立命令、版本、并发控制、查询 SLO 或生命周期，必须提升为 aggregate
  root 并通过 typed reference/event 协作，不能为了兼容旧路径保留双重身份。
- Registry/ContractGraph 只允许单轨当前 schema：禁止 `version`、`schemaVersion`、
  `registryRevision`、`_schemas/v*`、legacy parser、migration adapter 和版本选择分支；
  聚合并发 `expectedVersion`、事件顺序与正式 `/v1` 资源路径不属于兼容版本。

### 1.3 runtime 统一能力

| 能力 | 必须使用 | 禁止 |
|------|---------|------|
| 错误处理 | `runtime/errors.AppError` | 自定义 error struct 返回 HTTP |
| HTTP 服务 | `runtime/http.NewServer` + `runtime/observability` 中间件 | 裸 `net/http` 自建 server |
| 配置读取 | `runtime/config.RuntimeConfigProvider` | `os.Getenv` 读业务配置 |
| 消息发布 | `runtime/messaging.MessageEnvelope` | 自定义 MQ 序列化 |
| 应用接口 | 对象专属 command/query Facade | 跨对象基础 Facade、通用 CRUD service |
| 数据端口 | 对象专属 `AggregateStore` / named Reader / typed Slice | 泛型 Store/Reader、动态 Filter/Map |
| 契约编译 | 构建期唯一 `ContractGraph` + generated descriptor | 服务启动扫描 metadata、运行时对象注册表 |
| 字段策略 | metadata/codegen 产出的字段策略 + 对象 policy | 通用反射拦截链、手写 SECRET/PII 旁路 |
| 可观测 | `runtime/observability` | 自建日志/指标/追踪 |
| 治理 | `runtime/governance` | 自实现熔断/限流 |

### 1.4 composition root（强制）

`cmd/{entry}/main.go` 必须按固定顺序显式装配：

1. typed config、observability 与 platform client。
2. 每个业务对象的具体 Store/Reader adapter；adapter 类型由代码显式选择，不由
   metadata 在运行期决定。
3. 对象 command/query Facade；command 绑定 aggregate owner，query 绑定 named
   Reader/Slice 且不加载 aggregate。
4. generated operation descriptor 与 transport handler。
5. health、优雅关闭和 fail-fast 依赖检查。

禁止 domain/application/adapters 直接操作数据库；禁止恢复跨对象通用数据访问层、
运行时 metadata 扫描或反射拦截器。数据库驱动只存在于
`quwoquan_service/internal/platform/**` 和服务 `internal/infrastructure/**`。

### 1.5 codegen 保护

- `// Code generated ... DO NOT EDIT.` 标记的文件禁止手改
- `make gate` 通过 hash 比对检测
- 如需修改 codegen 产物的行为 → 修改 metadata YAML → 重新 `make codegen`

---

## 2. Dart 端侧结构约束

权威设计仍为本规则开头列出的 L2 design/acceptance 与 metadata DESIGN。本节只定义
依赖方向和执行约束，不复制对象清单、operation 或 UI IA。

### 2.1 cloud/ 层目录结构（强制）

```
packages/quwoquan_cloud_contracts/
└── lib/src/
    ├── generated/{domain}/                    # operation/command/result/slice/error
    └── shared/                                # context/pagination/runtime_failure

packages/quwoquan_cloud_mock/
└── lib/src/{domain}/                          # alpha/test adapter + fixture bundle

lib/cloud/
├── runtime/
│   ├── auth/
│   ├── config/
│   ├── context/
│   ├── codec/
│   ├── errors/
│   ├── observability/
│   └── transport/
lib/application/{bounded-context}/{object}/    # coordinator + typed application port
lib/cloud/remote/{bounded-context}/{object}/   # generated client 的薄 adapter/mapper
lib/infrastructure/local/{bounded-context}/{object}/
                                                # cache/outbox/local store
lib/core/di/                                    # production Remote-only composition
```

- `quwoquan_cloud_contracts` 必须是 pure Dart，零 App/Flutter/Riverpod/平台 SDK 反向依赖。
- `quwoquan_cloud_mock` 只允许 alpha runner 和 test target 引用，禁止进入 production
  composition、kernel/AOT 可达图或 SBOM。
- Cloud 业务层不直接依赖 UI、Router、平台插件、Hive、SharedPreferences 或 `dart:io`；
  本地能力经 `core/platform` port 注入。
- `generated/` 只放带 `DO NOT EDIT` 标记的 codegen 产物；共享基类、mapper、fixture、
  ViewData 和业务 policy 禁止伪装成 generated。

### 2.2 类型化 Facet 与 generated client（强制）

App 接口按业务对象和用例拆分，不按服务进程创建聚合 Repository。command 只绑定一个
aggregate owner，query 只绑定一个具名 Reader/Slice。单个 Facet 不超过 10 个方法。

```dart
abstract interface class PostCommandWriter {
  Future<CreatePostResult> createPost(
    OperationInvocationContext context,
    CreatePostCommand command,
  );
}

abstract interface class DiscoveryFeedQuery {
  Future<DiscoveryFeedSlice> loadFeed(
    OperationInvocationContext context,
    DiscoveryFeedQueryInput query,
  );
}

final class RemotePostCommandWriter implements PostCommandWriter {
  RemotePostCommandWriter(this._client);

  final ContentGeneratedOperationClient _client;

  // 仅做 typed command/result 映射；path/auth/error/retry/deadline 来自 descriptor。
}
```

**禁止行为**：
- 业务 Facet/Provider/UI 使用 `Map<String, dynamic>`、`Object? body` 或调用方猜测
  `TResponse`。
- App 业务代码传入 `operationId`、path、decoder context、auth header 或 retry policy。
- Remote 自建 `CloudHttpClient()`、拼 URI、使用 `forPage` 或绕过 generated executor。
- 以服务进程或前台大页面为边界创建几十方法的 Repository。
- command 直接修改公开字段，或 query 为读取详情而加载 aggregate。

### 2.2.1 Operation / Surface / Route 元数据分层（强制）

客户端请求与导航相关标识必须按以下唯一真相源分层：

| 标识 | 唯一真相源 | 代码消费位置 |
|---|---|---|
| API `path_template` / `method` / `operation` | `service.yaml` | generated descriptor/client、contract tests |
| `operation_id` / decoder context | `service.yaml`（或其 codegen 产物） | generated descriptor / executor |
| `surface_id` / `route_id` / `path_template` | `ui_config.yaml` 或 `ui_surfaces.yaml` | Router / generated membership / UI tests |

**禁止行为**：
- 在 codegen 中维护 operation→surface / route→page override map 作为第二真相源
- 在 Remote adapter 中手写 `CloudResponseDecoder.context: '...'`
- 在 Router 中手写 `path: '/biz-path'`、`context.go('/biz-path')`
- 新增 operation / surface / route 时跳过 metadata 直接改业务代码

### 2.3 production / alpha 物理装配（强制）

production 与 alpha 使用不同 composition root 和依赖图，不在同一生产二进制内通过枚举、
Provider 或远端配置切换。

```dart
// production root
final postCommandWriterProvider = Provider<PostCommandWriter>(
  (ref) => RemotePostCommandWriter(ref.watch(contentGeneratedClientProvider)),
);

// alpha runner/test target 位于独立 package/entrypoint，才允许装配 mock adapter。
```

**禁止行为**：
- production 入口、Provider、业务代码引用 `AppDataSourceMode.mock`、Mock、fixture 或 alpha URL。
- Remote 失败后返回 fixture、Mock、空集合或本地合成成功。
- 缺 endpoint/auth/config/真实依赖时继续启动；beta/gamma/prod 必须 fail-fast。
- alpha 在设备运行时读取仓库相对路径；fixture 必须由 metadata seed manifest 构建为
  带 source hash 的 typed bundle。

### 2.4 UI 层约束（按领域服务划分）

> **规划文档**：`specs/01_APP_DIRECTORY_STRUCTURE_BY_DOMAIN.md`  
> `lib/features/` 已废弃，**新建领域服务及页面必须在 `lib/ui/{domain}/` 下**，与 metadata 领域对齐。

```
lib/ui/{domain}/              # domain 与 quwoquan_service/contracts/metadata 一致
├── pages/          # 必须存在（页面入口 Widget）
├── providers/      # 状态管理（Riverpod Notifier/Provider）
├── widgets/        # 页面专属子组件
└── models/         # 仅页面可见的 ViewModel（非 cloud DTO）
```

**领域示例**：`content`（含 entry 创作入口）、`discovery`、`chat`、`user`、`circle`、`assistant`、`settings`、`welcome`。

**Discovery 域示例：**
```
lib/ui/discovery/
├── pages/
│   └── discovery_page.dart         # 发现页
├── providers/
│   └── discovery_feed_provider.dart
└── widgets/
    ├── photo_feed_grid.dart
    ├── video_feed_view.dart
    └── ...
```

**创作入口（content/entry）示例：**
```
lib/ui/content/entry/               # 创作入口，归属 content 域
├── pages/
│   ├── create_page.dart
│   └── publish_location_selector_page.dart
├── providers/
└── widgets/
```

| 约束 | 说明 |
|------|------|
| 目录必须在 `lib/ui/{domain}/` 下 | **禁止**在 `lib/features/` 下新建页面或领域模块 |
| 必须含 `pages/` | UI 模块入口 |
| 禁止 import 其他 UI 模块内部 | 共享通过 `lib/core/` 或 `lib/cloud/` |
| 必须通过 Provider 访问 typed Facet | 页面只注入所需 command/query capability |
| 错误处理必须结构化 | `RuntimeFailure` + `RuntimeRecoveryPolicy`，不得回退 Mock |
| 禁止在 UI 层直接操作动态 wire | 必须使用 typed Slice/ViewData |
| 页面路由与 surface 标识必须来自 metadata codegen | 禁止在 UI / Router 中硬编码业务 path、route_id、surface_id |
| **横向质量矩阵** | 新增/搬迁页面文件（见 `quwoquan_app/scripts/runtime/verify_page_matrix_scan_complete.py` 扫描基线）须更新 [`specs/feature-tree/runtime/runtime-client-foundation/page-horizontal-quality-matrix.md`](specs/feature-tree/runtime/runtime-client-foundation/page-horizontal-quality-matrix.md)，并勾选 [`specs/gates/page_horizontal_quality_pr_checklist.md`](specs/gates/page_horizontal_quality_pr_checklist.md)；`make verify-app-page-horizontal-quality` 与 `make gate` 含对应 `verify_page_*` 脚本（详见 [.cursor/rules/09-page-horizontal-quality.mdc](09-page-horizontal-quality.mdc)） |

### 2.5 对象 operation 端云对齐检查清单

新增或迁移一个对象 operation 时，以下必须全部完成：

```
☐ metadata 已声明 canonical object、command owner 或 Reader/Slice
☐ auth/actor/error/idempotency/deadline/retry/telemetry 合同完整
☐ ContractGraph commercial validate 通过并由同一 hash 生成 Go/Dart/OpenAPI
☐ Go Facade/Data Port/production wiring 与 generated descriptor 对齐
☐ Dart typed client + 细粒度 Facet + production Remote adapter 对齐
☐ alpha fixture 来自 canonical seed manifest，production 依赖图不可达 Mock
☐ local_contract / api_integration / user_acceptance 反向绑定
☐ 同一变更删除旧 Repository/DTO/decoder/alias/fallback，不保留双轨
```

### 2.6 现有代码适配路径

存量按对象 packet 原子迁移，不保留错误实现作为长期兼容层。

| 现有文件/目录 | 目标位置 | 处理方式 |
|-------------|---------|---------|
| `lib/features/create/` | `lib/ui/content/entry/` | 创作入口归属 content 域 |
| `lib/features/chat/` | `lib/ui/chat/` | 按领域迁移 |
| `lib/features/circles/` | `lib/ui/circle/` | 按领域迁移 |
| `lib/features/profile/` | `lib/ui/user/` | 与 ui/user 合并 |
| `lib/features/assistant/` | `lib/ui/assistant/` | 按领域迁移 |
| `lib/features/settings/` | `lib/ui/settings/` | 按领域迁移 |
| `lib/features/welcome/` | `lib/ui/welcome/` | 按领域迁移 |
| 手写/混合 `runtime/generated/**` | pure contracts generated 或领域 mapper/ViewData | 补 metadata 后重生或迁出 |
| 聚合 `ContentRepository` 等 | 对象级 command/query Facet | 消费者原子切换后删除 |
| `core/providers/app_providers.dart` | 对象级 provider/production root | 只装配 typed Remote capability |
| `AppContentRepository` / `AppDataSourceMode` | 无 | 各 owner 迁完后删除 |

---

## 3. 端云一致性约束

### 3.1 接口一一对应

| 云侧 | 端侧 | 一致性来源 |
|------|------|-----------|
| 对象 aggregate/command/result/Slice | pure Dart generated command/result/Slice | ContractGraph + `fields.yaml` |
| typed domain event/outbox envelope | generated event contract 或 projection input | `events.yaml` + ContractGraph |
| generated operation descriptor → Facade | generated operation client → object Facet | `service.yaml` |
| `runtime/errors` 错误码 | App 端 codegen 错误枚举 / runtime mapper | `quwoquan_service/contracts/metadata/{domain}/{entity}/errors.yaml` |
| generated privacy/field descriptor | generated decoder/redaction policy | `fields.yaml` |

### 3.2 metadata↔代码同步

- Go struct / Dart DTO / OpenAPI / Migration 必须与 metadata YAML 一致
- `make verify` + `make gate` 自动校验
- 不一致 → `make gate` 失败

### 3.3 错误码 metadata 约束

错误码与用户可见文案**唯一定义于** `quwoquan_service/contracts/metadata/{domain}/{entity}/errors.yaml`，禁止在业务代码中硬编码。

| 层级 | 必须使用 | 禁止 |
|------|----------|------|
| **metadata** | `errors.yaml` 定义 code、l10n_key、user_message.zh/en、go_const、dart_const | 无 errors.yaml 就手写错误码/文案 |
| **云侧 Handler** | `generated.AppErrorFrom*(debugMessage)` 等 codegen 产出的方法 | 硬编码 user_message、code 字符串 |
| **端侧 UI/Facet** | `*ErrorCode.fromCode(e.code).toDisplayMessage(context.l10n)` | switch/case 硬编码 code 字符串或文案 |
| **测试** | 枚举 `.code`（如 `IntegrationLocationErrorCode.upstreamTimeout.code`） | 硬编码 `'INTEGRATION.MIDDLEWARE.upstream_timeout'` 等字符串 |

**顺序**：metadata → make verify-metadata → make codegen / make codegen-app → 业务逻辑。门禁 `verify_error_code_semantic.py` 检测端侧错误码字符串硬编码。

---

## 4. 扩展场景约束

**核心顺序**（每个扩展操作强制）：
```
metadata
  → qwq-contract commercial validate
  → ContractGraph/codegen
  → Object Application Facade
  → Object Data Ports
  → service infrastructure adapters
  → local_contract / api_integration / user_acceptance
  → alpha / beta / gamma / prod
```

**禁止行为**：
- 不经 metadata 直接硬编码错误码或 user_message（必须 errors.yaml → codegen）
- 不经 `/extend` 与 metadata 直接创建 Go struct / Dart DTO / 对象接口
- 不经 metadata/codegen 手写 migration；Store/Reader adapter 必须实现对象专属 port
- 手动创建 `services/{name}/` 目录（必须通过 `make new-service`）
- 在 domain 层 import 数据库驱动
- 在 application 层直接操作数据库
- 恢复跨对象泛型 CRUD、运行时对象注册、动态存储选厂或 metadata 文件扫描
- 绕过 generated 字段策略直接返回 SECRET/PII 字段
- 不经 `make verify` 就执行 `make codegen`
- 在 Feature 中直接实例化 Mock/Remote adapter
- Remote adapter 中硬编码 URL、自建 headers 或绕过 generated executor
- 在同一环境将一个 domain 归属到多个部署进程
- 允许 integration/prod 拓扑映射不一致
- 通过部署拓扑变更对外领域 API 路由契约

### 4.1 部署拓扑映射约束（新增）

部署映射唯一真相：`quwoquan_ops/environments/process_domain_mapping.yaml`

结构：
```yaml
environments:
  dev:
    content-service:
      domains: [content]
  integration:
    quwoquan_service:
      domains: [content, chat, ...]
  prod:
    quwoquan_service:
      domains: [content, chat, ...]
```

强制规则：
- 同一环境中 domain 只能出现一次
- `integration` 与 `prod` 映射必须一致
- 进程名使用 `*-service` 或 `quwoquan_service`

**扩展入口**：`/extend <scenario>`，详见 `specs/runtime_extension_catalog.md`。

---

## 5. 脚本目录约束

### 5.1 禁止根目录 `scripts/` 平铺

仓库根目录 **不再保留** `scripts/` 目录。所有脚本必须归入对应的领域项目：

| 脚本归属 | 目录 | 示例 |
|---|---|---|
| App 端客户端 | `quwoquan_app/scripts/<domain>/` | `quwoquan_app/scripts/runtime/`、`quwoquan_app/scripts/content/` |
| 云侧服务 | `quwoquan_service/scripts/<domain>/` | `quwoquan_service/scripts/deploy/`、`quwoquan_service/scripts/runtime/observability/` |
| 数据工程 | `quwoquan_data/scripts/<domain>/` | `quwoquan_data/scripts/verify/`、`quwoquan_data/scripts/e2e/` |
| 通用运维/CI/部署 | `quwoquan_ops/<domain>/` | `quwoquan_ops/gate/`、`quwoquan_ops/ci/`、`quwoquan_ops/cli/` |

**禁止行为**：
- 禁止在仓库根目录创建 `scripts/` 目录
- 禁止在 `quwoquan_ops/` 下新增 `scripts/` 子目录（`quwoquan_ops` 本身即脚本顶层）
- 新增脚本必须归入上表对应领域子目录；不确定归属时先在 PR 中说明

### 5.2 门禁

- `quwoquan_ops/gate/scaffold/verify_engineering_directory.sh` 通过 `engineering_directory_manifest.yaml` 校验必要目录，`scripts` 已从 `required_directories` 中移除、替换为 `quwoquan_ops`。
- 如需添加仓库级脚本结构检查，在 `quwoquan_ops/gate/gate_repo.sh` 中增加 `[ ! -d scripts ] || { echo "FAIL: scripts/ 禁止平铺"; exit 1; }` 等价校验。

### 5.3 数据工程 CLI-first + Skill 范式（强制）

> 适用 `quwoquan_data/**` 的内容生产/冷启动/校验能力。详见 [`.cursor/skills/quwoquan-data-content/SKILL.md`](.cursor/skills/quwoquan-data-content/SKILL.md)。

所有新增数据工程能力必须遵循统一三段式范式：

```
[CLI prepare]  ->  [Agent semantic]  ->  [CLI validate + gate]
```

| 约束 | 要求 |
|---|---|
| 唯一入口 | 全部经 `qwq-data`（=`python3 quwoquan_data/scripts/cli.py <command>`）；脚本只做 IO 准备/拉取/落盘/打分/校验，语义加工在 Agent 会话内完成 |
| 能力实现 | 新能力 = `<command>/handler.py`（`register_parser` + `handle_*`）+ 可选 `<command>/gate.py`；可复用逻辑沉到 `core/` 库 |
| Skill 暴露 | `SKILL.md` 只暴露 CLI 命令，禁止指向单场景孤立可执行脚本 |
| 真相源 | 路径/错误码/字段/叙事契约/imagePolicy 先改 metadata/blueprint/schema，再跑 `template lint` 与业务逻辑 |
| Stage 闭环 | 每个 stage 必落 stage result + gate report；失败写 repair report，按 `fallbackStage` 回退重跑直至全绿 |

**禁止行为**：
- 禁止在 `quwoquan_data/scripts/**` 新增可直接 `__main__` 运行的业务入口脚本（旧脚本只能保留薄壳委托 `qwq-data`，并逐步收敛）
- 禁止为单个场景（如 `chuanxi`）编写专用脚本；能力必须泛化为 CLI 子命令 + 模板/蓝图驱动
- 禁止 Skill 文档绕过 CLI 直接调用内部模块

**门禁**：
- `python3 quwoquan_data/scripts/verify/verify_cli_first.py`（业务能力只允许经 `scripts/cli.py` 进入；`scripts/verify/*.py` 仅是工程门禁，不维护入口 allowlist）
- `python3 quwoquan_data/scripts/cli.py verify all`（唯一仓内 Data gate；环境 release 另以 `ship` 和 two-province release gate 显式验证）
