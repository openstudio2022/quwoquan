---
description: 端云一体化测试治理（三层测试 + UAT/SIT/GWT/contract）
globs: **/*_test.dart,**/test/**/*.dart,**/*_test.go,**/tests/**/*.go,**/acceptance.yaml
---

# 端云一体化测试治理

唯一规范：`specs/03_TESTING_STRATEGY.md`。

测试治理分为两套语言：

- 验收表达：`UAT`、`SIT`、`GWT`、`contract`
- 测试工程层：`local_contract`、`api_integration`、`user_acceptance`

禁止：

- 用 `L3/L4` 表示测试层。
- 新增 `T1/T2/T3/T4` 作为验收证据前缀、目录名或工程层。
- 用四层特性树替代测试分层。

---

## 1. 验收意图

| 验收 | 归属 | 语义 |
|---|---|---|
| `UAT` | 应用根 Journey / Scenario | 用户需求和端到端价值是否成立 |
| `SIT` | `L2_business_capability` | 多 Story 组合、状态机、数据一致性、端云协同是否成立 |
| `GWT` | `L3_story` | 最小价值点在 Given / When / Then 下是否成立 |
| `contract` | `L3_story` 或 metadata | API、DTO、错误码、route/surface/operation 是否一致 |

---

## 2. 三层测试

| 测试层 | 环境 | 典型对象 |
|---|---|---|
| `local_contract` | 本地 / CI / alpha fixture | metadata、DTO、错误码、Repository、Provider、Widget、领域规则、单服务 handler |
| `api_integration` | beta / gamma / prod（只读/幂等） | gateway/API、真实存储、事件/outbox、RemoteRepository、跨服务一致性 |
| `user_acceptance` | gamma_local / prod rollout stage / 设备矩阵 | 用户旅程、页面功能、UI/UX、页面生命周期、权限、弱网、SLO、灰度验收 |

规则：

- 测试层只表达验证方式。
- 特性树层级只表达交付对象和责任归属。
- `UAT/SIT/GWT/contract` 必须映射到至少一个三层测试证据。
- “superpowers”只表达产品级用户能力，不是测试层，也不是特性树新层级。

---

## 3. 与特性树关系

### 应用根

- 必须声明 UAT。
- 主要证据为 `user_acceptance`，辅以 `api_integration`。
- 用 `journey_scenario_registry.yaml` 绑定 Journey / Scenario 与领域服务、业务能力、Story。
- AppRoot UAT 用用户验收用例表达，不使用 GWT。

### `L1_domain_service`

- 必须验证领域边界、依赖、权限、生命周期、观测和发布 guardrails。
- 主要证据为 `api_integration` 与 `local_contract`，必要时补 `user_acceptance`。

### `L2_business_capability`

- 必须声明 SIT。
- 主要证据为 `api_integration`，支撑证据为 `local_contract`；面向用户的能力补 `user_acceptance`。
- 能力级设计负责说明状态机、策略、数据流和跨 Story 协作。

### `L3_story`

- 必须声明 GWT 和必要的 contract。
- 主要证据为 `local_contract`，涉及远端边界时补 `api_integration`，涉及页面/旅程时补 `user_acceptance`。
- Story 不设 `design.md`；需要设计时上收到业务能力层。

---

## 4. TDD / ATDD 规则

所有 `/dev`、`/deliver`、`/try` 必须遵循：

```text
先定义 acceptance
→ 再确定 UAT/SIT/GWT/contract
→ 再确定 local_contract/api_integration/user_acceptance 证据
→ 先写失败测试或替代验证说明
→ 写最小实现
→ 重构
→ 回填测试证据
```

强制要求：

- 每个当前交付任务开始前，必须能指出对应验收项和测试层。
- 每个会话 todo 必须回链到 Story 或能力验收项。
- 高风险改动必须有明确的先失败再转绿证据。
- 无法先写测试时，必须在对应 design 或 acceptance 中说明替代验证手段。

---

## 5. 门禁绑定

开发期 `make gate` 应覆盖：

- `local_contract`
- `verify-test-specs`
- `verify-test-directory-layout`
- 必要的本地 `api_integration`

按 profile 收口：

- `make gate` 只覆盖无环境依赖的 `local_contract`、规格、契约和静态非功能质量。
- `make gate-smoke` 覆盖 Alpha 的 mock/contract 投影。
- `make gate-integration ENV=beta|gamma` 覆盖真实内容数据平面的 `api_integration`、媒体、幂等与回滚/replay；不得要求 SLS 或 product-ops。
- `make gate-release ENV=gamma|prod` 覆盖 Gamma 真机 UAT、trace/SLO/SLS，以及 Prod 的 `rollout_stage=gray_initial` 条件具备时的低风险发布验收。

发布前必须补齐：

- `api_integration`
- `user_acceptance`
- SLO、告警、灰度、回滚证据

非功能质量不新增第四层测试，统一写入 `quality_facet`：

- 异常与恢复：`reliability + experience + observability`
- 性能：`performance`
- 安全与隐私：`security`
- 埋点、日志、指标、trace、audit：`observability`
- 健康检查、降级、重试、回滚：`availability + reliability`
- 读写一致、事件/outbox、导入发布、推荐归因：`data_consistency`

任何适用的非功能维度必须在 `acceptance.yaml` 的 `quality_facets` 与 `test_evidence` 中声明；缺证据时返回 `GATE_BLOCK`。

---

## 6. 环境数据驱动测试

新增或修改业务对象页面、Repository、Remote API、MockRepository 时，必须遵守：

- alpha/beta/gamma 的测试数据来自 `quwoquan_service/contracts/metadata/**/test_fixtures`。
- 端侧 alpha 使用 contract-seeded MockRepository，不访问云服务。
- 端侧 beta/gamma 使用 RemoteRepository，经 gateway/API 读取云侧 seed 数据，禁止读取 Dart mock 列表。
- 云侧 alpha/beta/gamma 必须通过 seed manifest 声明 `seedRefs`、`resetScope`、`targetStore` 与 `verifiedEndpoints`。
- 人工 beta 预置数据必须进入 `app_beta_seed_manifest.json`。
- 生产 App 只有一个包；不存在 `app-prod-gray`。

---

## 7. acceptance.yaml 要求

验收项必须包含：

```yaml
quality_facets: [functional, reliability, observability]
test_object: application_service
slo_refs:
  - specs or SLO document path
observability_refs:
  - metric, log, trace or dashboard reference
security_refs: []
test_evidence:
  primary:
    - layer: local_contract
      suite: suite_id
      cases:
        - local_contract.domain.capability.story.case
      envs: [local]
      test_object: application_service
      quality_facet: functional
  supporting:
    - layer: api_integration
      suite: suite_id
      cases:
        - api_integration.domain.operation.case
      envs: [beta, gamma]
      test_object: api_operation
      quality_facet: contract
    - layer: user_acceptance
      suite: suite_id
      cases:
        - user_acceptance.domain.capability.story.case
      envs: [gamma_local, prod]
      rollout_stages: [gray_initial]
      test_object: page
      quality_facet: experience
tests:
  planned: []
  recorded: []
```

禁止：

- `implemented` / `completed` 但 `tests.recorded` 为空。
- 用 `L3/L4` 或 `T1~T4` 作为测试层标识。
- 将 UAT、SIT、GWT、contract 和三层测试混为一层。
- 在 Story 层新增 `design.md` 来绕过能力级设计。
- 在验收项里新增旧字段 `evidence`。

## 8. 工程目录

新增测试目录：

```text
quwoquan_app/test/local_contract/<test_object>/<domain>/<...>/<case>__<facet>__local_contract_test.dart
quwoquan_app/test/api_integration/<test_object>/<domain>/<case>__<facet>__api_integration_test.dart
quwoquan_app/test/user_acceptance/pages/<owner>/<surface_id>/<surface_id>_page__user_acceptance_test.dart
quwoquan_service/services/<service>/tests/local_contract/<case>__<facet>__local_contract_test.go
quwoquan_service/services/<service>/tests/api_integration/<case>__<facet>__api_integration_test.go
quwoquan_data/tests/<layer>/<domain>/test_<case>__<facet>__<layer>_test.py
quwoquan_ops/tests/local_contract/test_<case>__<facet>__local_contract_test.py
quwoquan_ops/tests/acceptance/user_acceptance/test_<case>__<facet>__user_acceptance_test.py
```

存量无 facet 文件由统计器推导；新增或重命名测试必须显式携带 facet。存量目录只作为迁移映射，不作为新增标准，并由 `python3 quwoquan_ops/gate/scaffold/verify_test_directory_inventory.py` 阻断新增 legacy 路径。
