---
description: 四环境 Remote、canonical release 与 test-only double 的物理隔离
alwaysApply: true
---

# Mock / Fixture 物理隔离

## 唯一装配模型

- production composition 只依赖 `quwoquan_cloud_contracts`、generated client、Remote
  adapter 与真实 platform/local infrastructure；缺 endpoint、auth、Store 或平台能力时
  fail-fast。
- alpha/beta/gamma/prod 全部使用同一个 production Remote composition；环境只决定
  runtime package、endpoint、容量与发布阶段，不决定 App 内数据源。
- App 运行时、runner、`pubspec`、composition root、kernel/AOT 可达图与 SBOM 均不得
  引用 mock package、fixture bundle 或 Mock/Remote 切换器。App 只有一个 `prod` 包，
  灰度只是 prod rollout stage，不存在 `prod-gray` 数据源或包。
- 第一方业务数据只能由 `quwoquan_data` canonical immutable release 经环境 importer
  激活；T3/UAT、环境启动器和 bootstrap 禁止通过 API、数据库或仓库 fixture 自建待验证数据。

## 业务代码边界

- `lib/**`、环境 runner、设备启动器和 UAT support 禁止 import Mock、fixture、prototype、
  alpha runner 或 `quwoquan_cloud_mock`。
- 页面和 Provider 只依赖对象级 `*CommandWriter/*Query` typed port；禁止依赖聚合
  Repository、`AppDataSourceMode`、`appDataSourceModeProvider` 或运行时 mock/remote 开关。
- Remote adapter 只映射 generated typed client；禁止失败后返回 fixture、空集合、
  Mock 结果、本地合成成功或吞掉 `RuntimeFailure`。
- 禁止在 UI、Provider、脚本或 production `lib/**` 新增第二套业务列表、域名 const 数据、
  `Mock*`、`Stub*`、`Noop*` 或测试专用 factory。
- 禁止新增或扩大 mock/fixture/test-only allowlist；清理存量时必须同步删除对应豁免。

## 环境与测试

- `local_contract` 只能在测试树内使用对象级最小 typed in-memory double，验证 generated
  contracts、decoder、mapper、安全负例与恢复语义；禁止建立可被环境 App 引用的聚合 mock package。
- `api_integration` 必须经 generated Dart Remote 访问预制真实环境；裸 HTTP、自 seed、
  Memory adapter 或动态 skip 不计证据。
- `user_acceptance` 必须启动真实页面和 production Remote composition；路径存在性或
  fixture-only Journey 不计通过。
- alpha/beta/gamma/prod 的 App 可见业务对象必须绑定 canonical `releaseId` 与 import
  receipt；环境 package/manifest 禁止 `test_fixtures`、`seedRefs`、`requiresSeedReset`
  与默认 secret。
- 非 Prod 的第三方 Provider 可在服务防腐层使用登记的 sandbox/local substitute；缺能力
  必须返回结构化 unavailable，禁止向 App 伪造第一方业务成功。

## 门禁

- `python3 quwoquan_app/scripts/env/verify_ui_mock_isolation.py`
- `make verify-app-mock-isolation`
- `make verify-app-cloud-package-boundaries`
- `make verify-production-wiring-purity`
- 四环境 dependency graph、kernel/AOT reachability 与 SBOM 的 Mock/fixture/Noop 数量必须为 0。
