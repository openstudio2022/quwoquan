---
description: production Remote 与 alpha/test fixture 的物理依赖隔离
alwaysApply: true
---

# Mock / Fixture 物理隔离

## 唯一装配模型

- production composition 只依赖 `quwoquan_cloud_contracts`、generated client、Remote
  adapter 与真实 platform/local infrastructure；缺 endpoint、auth、Store 或平台能力时
  fail-fast。
- alpha/test 只能由 `packages/quwoquan_cloud_mock` 与 `runners/alpha` 注入 mock adapter；
  production `pubspec`、composition root、kernel/AOT 可达图与 SBOM 不得引用 mock package。
- beta/gamma/prod 只使用 Remote composition。App 只有一个 `prod` 包，灰度只是 prod
  rollout stage，不存在 `prod-gray` 数据源或包。
- alpha fixture 必须由 metadata seed manifest 在构建期生成 immutable typed bundle，并
  带 source/Graph hash；设备运行时禁止读取仓库文件或相对路径。

## 业务代码边界

- `lib/ui/**`、`lib/app/**`、`lib/application/**`、`lib/core/**` 禁止 import Mock、fixture、
  prototype、alpha runner 或 `quwoquan_cloud_mock`。
- 页面和 Provider 只依赖对象级 `*CommandWriter/*Query` typed port；禁止依赖聚合
  Repository、`AppDataSourceMode`、`appDataSourceModeProvider` 或运行时 mock/remote 开关。
- Remote adapter 只映射 generated typed client；禁止失败后返回 fixture、空集合、
  Mock 结果、本地合成成功或吞掉 `RuntimeFailure`。
- 禁止在 UI、Provider、脚本或 production `lib/**` 新增第二套业务列表、域名 const 数据、
  `Mock*`、`Stub*`、`Noop*` 或测试专用 factory。
- 禁止新增或扩大 mock/fixture/test-only allowlist；清理存量时必须同步删除对应豁免。

## 环境与测试

- `local_contract` 使用 pure contracts + mock package 验证 typed parity、安全负例和恢复语义。
- `api_integration` 必须经 generated Dart Remote 访问预制真实环境；裸 HTTP、自 seed、
  Memory adapter 或动态 skip 不计证据。
- `user_acceptance` 必须启动真实页面和 production Remote composition；路径存在性或
  fixture-only Journey 不计通过。
- alpha/beta/gamma/prod seed 只来自各自 metadata manifest；production manifest 禁止
  `test_fixtures`、`seedRefs`、`requiresSeedReset` 与默认 secret。

## 门禁

- `python3 quwoquan_app/scripts/env/verify_ui_mock_isolation.py`
- `make verify-app-mock-isolation`
- `make verify-app-cloud-package-boundaries`
- `make verify-production-wiring-purity`
- production dependency graph、kernel/AOT reachability 与 SBOM 的 Mock/fixture/Noop 数量必须为 0。
