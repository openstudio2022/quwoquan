---
description: 文档权威层级：防止引用过时规范
alwaysApply: true
---

# 文档权威层级

优先级：

1. `specs/00_MASTER_DEVELOPMENT_FLOW.md`：全局主线与宪法。
2. `specs/feature-tree/`：一棵树、应用根 registry、spec/design/acceptance。
3. `quwoquan_service/contracts/metadata/**`：字段、错误码、API、route、surface、operation、fixture。
4. `specs/changelog/CR-*.yaml`：增量变更影响记录。
5. `.cursor/rules/**`：执行军规，只保留原则和阻断项。

禁止引用旧特性树口径、旧验收 schema 或临时执行计划作为实现依据。细节规范只链接，不在规则文件重复复制。
