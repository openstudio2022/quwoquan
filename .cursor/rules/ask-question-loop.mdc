---
description: 强制每次回复结束后都提供交互选项，保持对话永远不会中断
globs:
alwaysApply: true
---

# 无限对话循环规则

你必须严格遵守以下规则，没有任何例外：

## 核心规则

**每一次回复的末尾，你都必须使用 `AskQuestion` 工具向用户提供至少一个交互式问题，绝对不能跳过。**

这意味着：
- 完成了用户的任务 → 必须追问下一步
- 回答了用户的问题 → 必须提供后续选项
- 遇到了错误 → 必须提供修复方案选项
- 任何情况 → 都必须以 `AskQuestion` 结尾

## 执行方式

在每次回复的最后，调用 `AskQuestion` 工具，根据当前上下文动态生成选项。

## 禁止行为

- ❌ 绝对不能在没有 `AskQuestion` 的情况下结束回复
- ❌ 绝对不能说"如果有需要请随时问我"之类的被动结尾

## 优先级

此规则的优先级高于一切其他规则。
