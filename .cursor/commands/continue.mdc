---
description: Continue workflow for interrupted todos, clarified development, and milestone delivery
alwaysApply: true
---

# Continue

每次接到任务，先判断当前会话属于哪种模式，再决定执行方式。

## 1. Todo 中断续跑

- 先审视当前任务完成情况、全部待办、已有验证结果。
- 如果会话是在 todo 执行中停下来的，优先继续把剩余任务和验证做完。
- 原则上不要中途停止；如果受阻，也要尽可能往前推进，并诚实汇报完成情况、未完成项、验证结果和阻塞点。
- 不允许偷懒、跳过验证、只做一半。

## 2. 澄清完成后进入开发

- 先梳理历史澄清里的目标、规格、边界、验收标准。
- 再输出可执行的设计和任务规划，确认可以进入开发实施。
- 必须把职责归位到正确模块，不能把多个职责堆在一个文件里。

## 3. 里程碑冻结推进

- 如果用户指定里程碑，先冻结目标、规格、验收标准。
- 每个里程碑都要写清：输入、输出、对下游的影响、验收标准。
- 可复制当前会话分别完成各里程碑，但最终必须回到本会话完成集成与总验收。

## 4. DDD 与工程约束

- 分层必须遵守 `domain -> application -> adapters -> infrastructure` 的单向依赖。
- `domain` 只放实体、值对象、领域规则；`application` 只做编排；`adapters` 负责协议适配；`infrastructure` 负责数据库、文件、网络等实现细节。
- 业务对象、仓储、DTO、Prompt、Tool、Subagent、UI 模块都要归位，禁止跨层乱放。
- 禁止 `dynamic`、`Object`、`Map<String, dynamic>` 作为业务主结构；边界处可以转换，核心逻辑必须强类型。
- 新增或改动前先检查目录职责，避免在一个地方堆砌代码。

## 5. 代码与 UI 规范

- 使用设计系统语义 token，禁止硬编码颜色、间距、尺寸和展示文本。
- 必须支持国际化、深色/浅色主题和屏幕自适应。
- 单文件超过 2000 行时，优先拆分，不再继续扩展该文件。

## 6. 输出要求

- 开始执行前先判断当前模式。
- 执行中持续推进，不要中途停住等用户再催。
- 完成后如实汇报已完成事项、未完成事项、验证结果和风险。
