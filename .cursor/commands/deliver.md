---
name: /deliver
id: deliver
category: Workflow
description: dev + verify + commit 一气呵成（面向 L3_scenario）
---

> SDD 主流程：design → **deliver** → deploy

`/deliver` = `/dev` + `/verify` + `/commit`

目标：

- 完成 `L3_scenario`
- 完成目标 slices
- 通过门禁
- 完成 CR 修订
- 完成提交

测试口径只使用：

- `T1`
- `T2`
- `T3`
- `T4`
