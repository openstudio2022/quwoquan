# /try

目标：低成本试验。

豁免：可暂不创建正式特性树节点。

不豁免：metadata、mock 隔离、runtime error、生产纯净、安全隐私、测试证据。

成功后进入 `/land`，补齐一棵树归属与验收。

自然语言等价触发：用户直接描述与本命令目标相同的需求时，也按 `/try` 语义执行；执行前仍需按 `根 AGENTS.md` 完成 Spec Entry / Pre-work Reflection，完成后按 Exit Review 收口。

协议补充：执行前按 `根 AGENTS.md` 完成 Spec Entry / Pre-work Reflection；完成后按 Exit Review 输出证据、门禁结果与剩余风险。
