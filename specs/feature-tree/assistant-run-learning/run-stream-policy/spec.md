# L2 特性：run-stream-policy

## 功能说明
- 规范助手 Run/Stream 主链路的协议、策略模板与域路由行为。
- 保证端侧请求在同步与流式路径下均有稳定、可追踪、可降级的执行结果。

## 约束
- Run/Stream 的输入输出字段必须与端侧协议一致，禁止服务端私有字段外泄。
- 策略模板与域路由必须版本化，可灰度、可回滚。

## 验收标准
- A1：同步与流式运行都可完成且输出结构稳定。
- A7：契约与 metadata 一致，兼容性测试通过。
- A8：mock/contract/integration 自动化覆盖完成。
