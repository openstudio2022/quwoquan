version: 1
feature: "codegen-test-and-ci-integration"
level: "L5_cross_cutting"
template: "A1-A8"
tree_context:
  feature_level: "L5_cross_cutting"
  feature_path: "capability.runtime.feature.runtime-testinfra.subfeature.test-engine-and-fixture-framework.object.embedded-pg-testcontainers-miniredis.cross.codegen-test-and-ci-integration"
  parent_path: "capability.runtime.feature.runtime-testinfra.subfeature.test-engine-and-fixture-framework.object.embedded-pg-testcontainers-miniredis"
  acceptance_inherits_from: "capability.runtime.feature.runtime-testinfra"
level_acceptance:
  focus_groups:
    - A7_contract_metadata_consistency
    - A8_test_automation
  notes: "codegen 集成与 CI 流水线。"
global_acceptance:
  A1_functional:
    scenarios:
      - given: "Post 聚合 codegen 完成"
        when: "执行 make test-contract"
        then: "Post 契约测试运行，使用 testinfra 引擎"
      - given: "PR 提交"
        when: "CI 触发"
        then: "make test-contract 自动运行"
  A2_experience:
    - "make test-contract 一键运行全部契约测试"
  A3_service_governance:
    - "CI 契约测试超时 < 5min"
  A4_observability:
    - "CI 输出测试阶段耗时"
  A5_product_ops:
    - "N/A"
  A6_security_privacy:
    - "测试数据不包含真实用户数据"
  A7_contract_metadata_consistency:
    - "codegen 生成的 TestMain/fixture 与 test_infra.yaml、fields.yaml 一致"
  A8_test_automation:
    - "make test-contract 一键运行"
    - "CI 集成：每次 PR 自动运行契约测试"
tests:
  unit:
    - "tools/codegen/tests/test_scaffold_template_test.go"
  contract:
    - "quwoquan_service/.../contract_test.go (generated)"
execution:
  local_gate: "make gate"
  full_gate: "make gate-full"
