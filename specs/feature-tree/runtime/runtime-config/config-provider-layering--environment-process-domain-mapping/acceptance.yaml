version: 2
node:
  id: config-provider-layering--environment-process-domain-mapping
  level: L3_story
  path: specs/feature-tree/runtime/runtime-config/config-provider-layering--environment-process-domain-mapping
  title: config provider layering  environment process domain mapping
  parent_domain_service: runtime
  parent_business_capability: runtime-config
scope:
  summary: config provider layering  environment process domain mapping 验收范围。
  in_scope: []
  out_of_scope: []
gwt_acceptance:
  GWT1:
    title: config provider layering  environment process domain mapping GWT
    given:
    - 前置条件明确。
    - APP_ENV 与 CONFIG_VERSION 均已绑定到环境、进程和 domain 映射。
    when:
    - 用户或系统动作发生。
    then:
    - 可观察结果符合预期。
    done_when:
    - 最小价值点闭环。
    - APP_ENV 与 CONFIG_VERSION 能证明环境、进程和 domain 映射无漂移。
    edge_cases: []
    contract_refs:
    - APP_ENV
    - CONFIG_VERSION
    performance_points: []
    tests:
      planned: []
      recorded: []
    status: pending
    test_evidence:
      primary:
      - layer: local_contract
        execution_profile: baseline
        suite: contract
        cases:
        - local_contract.runtime.runtime_config.config_provider_layering_environment_process_domain_mapping.contract
        envs: &id001
        - local
      - layer: local_contract
        execution_profile: baseline
        suite: gwt1_local_contract
        cases:
        - local_contract.runtime.runtime_config.config_provider_layering_environment_process_domain_mapping.gwt1_local_contract
        envs: *id001
      supporting: []
    notes: []
execution:
  local_gate:
  - bash quwoquan_ops/gate/scaffold/verify_feature_tree_refactor.sh
  - bash quwoquan_ops/gate/scaffold/verify_acceptance_standard.sh
  profile_gates:
  - make gate
