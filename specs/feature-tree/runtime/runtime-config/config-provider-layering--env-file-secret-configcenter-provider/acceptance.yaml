version: 2
node:
  id: config-provider-layering--env-file-secret-configcenter-provider
  level: L3_story
  path: specs/feature-tree/runtime/runtime-config/config-provider-layering--env-file-secret-configcenter-provider
  title: config provider layering  env file secret configcenter provider
  parent_domain_service: runtime
  parent_business_capability: runtime-config
scope:
  summary: config provider layering  env file secret configcenter provider 验收范围。
  in_scope: []
  out_of_scope: []
gwt_acceptance:
  GWT1:
    title: config provider layering  env file secret configcenter provider GWT
    given:
    - 前置条件明确。
    when:
    - 用户或系统动作发生。
    then:
    - 可观察结果符合预期。
    done_when:
    - 最小价值点闭环。
    edge_cases: []
    contract_refs: []
    performance_points: []
    tests:
      planned: []
      recorded: []
    status: pending
    test_evidence:
      primary:
      - layer: local_contract
        suite: contract
        cases:
        - local_contract.runtime.runtime_config.config_provider_layering_env_file_secret_configcenter_provider.contract
        envs: &id001
        - local
      - layer: local_contract
        suite: gwt1_local_contract
        cases:
        - local_contract.runtime.runtime_config.config_provider_layering_env_file_secret_configcenter_provider.gwt1_local_contract
        envs: *id001
      supporting: []
    notes: []
execution:
  local_gate:
  - bash quwoquan_ops/gate/scaffold/verify_feature_tree_refactor.sh
  - bash quwoquan_ops/gate/scaffold/verify_acceptance_standard.sh
  full_gate:
  - make gate
