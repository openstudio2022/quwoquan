# 设计：multi-environment-wave-deployment

与 [environment_matrix.md](../../../../../deploy/shared/environment_matrix.md) 为同一口径；`process_domain_mapping` 的 `integration`≈`prod` 拓扑约束不变。波次半自动/审批见 `deploy-prod-gray` 与 [gray_rollout_stages.yaml](../../../../../deploy/shared/gray_rollout_stages.yaml)。
