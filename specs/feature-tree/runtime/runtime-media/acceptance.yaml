feature: runtime-media
level: "L2_feature"
archived: true
archived_at: '2026-03-07T18:20:24Z'
execution:
  local_gate: make build && make test-contract
  full_gate: make gate-full
level_acceptance:
  A1:
    criteria: MediaStore 接口定义完整：InitUpload/CompleteUpload/AbortUpload/GetAsset/BatchGetAssets
      五个方法均有签名、入参和返回类型，且遵循 runtime 模块模式（接口 + 实现分离）
    status: implemented
    linked_tasks:
    - G1
    - G5
    test_layers:
      T1: required
      T2: required
      T3: optional
      T4: optional
    tests:
    - file: quwoquan_service/runtime/media/media_test.go
      layer: T1
      description: MockMediaStore 单测：Init/Complete/Abort/Get/SignURL 正常+异常路径
  A2:
    criteria: OSS presigned URL 上传端到端可用：InitUpload 返回有效 presigned URL，客户端 PUT 文件后
      CompleteUpload 返回 cdnUrl，GetAsset 可查到 status=ready 的 MediaAsset
    status: implemented
    linked_tasks:
    - G3
    - G5
    test_layers:
      T1: required
      T2: required
      T3: required
      T4: optional
    tests:
    - file: quwoquan_service/runtime/media/media_test.go
      layer: T1
      description: MockMediaStore 端到端流程验证：Init→Complete→Get
  A3:
    criteria: CDN URL 签名正确：生成的 URL 含签名参数，过期前可访问，过期后返回 403；过期时间可通过 RuntimeConfigProvider
      配置
    status: implemented
    linked_tasks:
    - G4
    test_layers:
      T1: required
      T2: required
      T3: required
      T4: optional
    tests:
    - file: quwoquan_service/runtime/media/media_test.go
      layer: T1
      description: MockMediaStore SignURL 验证
  A4:
    criteria: 上传策略引擎按 Category×MediaType 生效：messaging/audio 限制 5MB/120s，messaging/image
      限制 10MB，content/video 限制 500MB/300s；超出限制返回 ModuleOSS.USER.policy_violation 错误
    status: implemented
    linked_tasks:
    - G2
    test_layers:
      T1: required
      T2: required
      T3: optional
      T4: optional
    tests:
    - file: quwoquan_service/runtime/media/media_test.go
      layer: T1
      description: ValidateUpload 策略引擎单测：各 Category 组合、超限拦截、未知 Category
    - file: quwoquan_app/test/cloud/media/contract/upload_policy_contract_test.dart
      layer: T1
      description: 端侧 UploadPolicy 契约测试：各 category 校验、超限拦截
  A5:
    criteria: 弱网场景上传可靠性：上传失败自动重试最多 3 次（指数退避 1s→2s→4s），presigned URL 过期后自动重新 InitUpload，断网期间任务入本地队列、网络恢复后自动恢复上传
    status: implemented
    linked_tasks:
    - D3
    - D5
    test_layers:
      T1: optional
      T2: required
      T3: required
      T4: required
    tests:
    - file: quwoquan_app/lib/cloud/media/media_upload_manager.dart
      layer: T2
      description: MediaUploadManager 实现含重试/离线队列/网络恢复逻辑（T2 单测搁置）
  A6:
    criteria: 可观测性完整：每次 InitUpload/CompleteUpload/AbortUpload 操作均有 IOAccessLogger
      日志（含 mediaId、category、mediaType、fileSizeBytes、latencyMs）；上传成功率 SLI 指标可通过 Prometheus
      查询
    status: implemented
    linked_tasks:
    - G7
    test_layers:
      T1: required
      T2: required
      T3: optional
      T4: optional
    tests:
    - file: quwoquan_service/runtime/media/oss_adapter.go
      layer: T1
      description: OSSMediaStore 实现含 observability span 和日志
  A7:
    criteria: content-service 平滑迁移：PostService 中原有的 mediaAssets/uploadSession 内存 map
      替换为 MediaStore 接口调用，现有 6 个媒体相关 API 路由不变、行为不变，契约测试全部通过
    status: deferred
    deferred_reason: content-service codegen 存在预存 YAML 解析问题（storage.yaml），待修复后执行迁移
    linked_tasks:
    - G6
    test_layers:
      T1: required
      T2: required
      T3: required
      T4: optional
    tests: []
  A8:
    criteria: 端侧 MediaUploadManager 实现：支持队列管理（并发最多 3 个）、自动重试、离线队列（Hive 持久化）、网络恢复自动恢复；MediaDownloadCache
      支持本地缓存（LRU 策略，默认 200MB）
    status: implemented
    linked_tasks:
    - D3
    - D4
    - D5
    test_layers:
      T1: optional
      T2: required
      T3: required
      T4: required
    tests:
    - file: quwoquan_app/test/cloud/media/contract/media_asset_dto_contract_test.dart
      layer: T1
      description: MediaAssetDto 契约测试：序列化/反序列化/可选字段
