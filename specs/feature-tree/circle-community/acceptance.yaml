feature: circle-community
level: L1_domain
archived: false
level_acceptance:
  - id: A1
    description: >
      端侧目录完成从 features/circles/ 到 ui/circle/ 的迁移：
      所有路由和 Provider 更新完毕，项目内无任何 import 引用 features/circles/ 路径，
      flutter analyze 无新增错误。
    status: pending
    tests:
      - path: quwoquan_app/lib/ui/circle/pages/circles_page.dart
      - path: quwoquan_app/lib/ui/circle/pages/circle_detail_page.dart
      - path: quwoquan_app/lib/ui/circle/pages/circle_stats_page.dart

  - id: A2
    description: >
      CircleRepository 三层模式（Abstract + Mock + Remote）创建完成，
      已注册到 app_providers.dart 的 circleRepositoryProvider，
      通过 appDataSourceModeProvider 切换 mock/remote，
      AppContentRepository 中的 circles* 接口已移除。
    status: pending
    tests:
      - path: quwoquan_app/lib/cloud/services/circle/circle_repository.dart
      - path: quwoquan_app/lib/core/providers/app_providers.dart

  - id: A3
    description: >
      统一领域标签体系建立：圈子频道配置与个人助理 domain_routing_catalog.json
      共享同一套领域 ID（至少 travel/local_life/health_wellness/tech/education 5 个领域对齐），
      圈子频道数据由领域标签驱动而非硬编码 Map。
    status: pending
    tests: []

  - id: A4
    description: >
      圈子详情页重构为板块式结构，至少包含 4 个功能区：
      作品发布区（支持瀑布流+列表视图切换）、群聊入口、存储空间入口、互动区。
      各板块独立加载，单板块加载失败不影响其他板块渲染。
    status: pending
    tests:
      - path: quwoquan_app/lib/ui/circle/pages/circle_detail_page.dart

  - id: A5
    description: >
      圈子存储空间支持基础 CRUD 操作：创建文件夹、上传文件（≤50MB）、
      下载文件、删除文件、浏览文件列表；权限控制生效
      （圈主完全控制、管理员可管理、成员可上传下载、访客只读）。
    status: pending
    tests: []

  - id: A6
    description: >
      圈子群聊集成可用：圈子详情页显示群聊入口，点击可跳转到对应 IM 会话；
      加入圈子触发 CircleMemberJoined 事件后用户自动进群，
      退出圈子触发 CircleMemberLeft 事件后用户自动退群（可在圈子设置中关闭）。
    status: pending
    tests: []

  - id: A7
    description: >
      圈子详情页所有硬编码视觉字面量清零：
      字号使用 AppTypography.*、间距使用 AppSpacing.*、颜色使用 AppColors/AppColorsFunctional、
      圆角使用 AppSpacing.*BorderRadius，
      python3 scripts/verify_dart_semantic.py 对 ui/circle/ 目录零新增违规。
    status: pending
    tests:
      - path: quwoquan_app/lib/ui/circle/pages/circle_detail_page.dart

  - id: A8
    description: >
      云侧 circle-service 核心 API 实现并通过契约测试：
      ListCircles、CreateCircle、GetCircle、JoinCircle、LeaveCircle、
      GetCircleFeed、GetCircleStats 7 个端点与 service.yaml 契约一致，
      make test-contract 通过。
    status: pending
    tests: []

  - id: A9
    description: >
      圈子主页同频发现推荐可用：基于用户兴趣画像（领域标签权重）
      推荐排序圈子列表，推荐结果与用户近 7 天活跃领域相关度 ≥ 60%
      （通过离线评测验证）。
    status: pending
    tests: []
