# Tasks: canonical-search-contract

## 当前交付任务

- [x] T1: 新增 shared metadata，生成 `SearchRequest / SearchResponse / SearchMode` 等 canonical schema → A1
- [x] T2: 让搜索页、业务 facade 与 assistant search tool 共用同一 query-first contract → A1
- [ ] T3: 补齐 contract 级分页、排序和更多多轮检索回归验证 → A1

## 未来演进任务

- [ ] F1: 在不引入复杂 DSL 的前提下扩展更细粒度的排序和召回提示字段
