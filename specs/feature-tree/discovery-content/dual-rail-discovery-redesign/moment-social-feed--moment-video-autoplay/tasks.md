# moment-video-autoplay 任务清单

## 当前交付任务
- [ ] **Mv1** 提取 `VideoPlaybackMixin`（`VisibilityDetector` + 音量淡入逻辑），供 `VideoAutoPlayItem` 和 `MomentVideoCard` 复用
- [ ] **Mv2** 新建 `MomentVideoCard`（内嵌卡片，`BoxFit.cover`，缩略图占位，`VideoPlaybackMixin`）
- [ ] **Mv3** 点击 → `Navigator.push` 简单全屏播放器（`MomentVideoFullScreen`），关闭按钮
- [ ] **T1** Widget test：视口 ≥ 60% 触发播放；< 60% 暂停

## 搁置任务（带规划）
暂无。

## 未来演进任务
暂无。
