# L3 Story：作品统一内容流 (`works-unified-feed`)

> 所属能力：[`dual-rail-discovery-redesign`](../spec.md)

> Journey / Scenario：[`JNY-003 / SCN-007`](../../../spec.md#scn-007)

> 设计归属：[L2 DEC-001](../design.md#dec-001)

## 1. 用户价值

作为浏览作品的用户，
我希望在同一内容流查看图片、视频和文章并进入各自统一浏览落点，
从而以一致方式发现不同形态的作品。

## 2. 范围与非目标

### In Scope

- “作品统一内容流”的输入、可观察主路径、失败语义以及与父能力的交接。

### Out of Scope

- 父能力中由其他 Story 独立拥有的行为、能力级架构决定和实现任务。

## 3. 行为要求

<a id="req-001"></a>
### REQ-001 作品统一内容流

- 端点必须先在 `service.yaml` 声明，`make verify` → `make codegen` 后方可编写 Repository。

<a id="req-002"></a>
### REQ-002 端点必须先在 service.yaml 声明，make verify → make codegen 后方可编写 Repository

- 端点必须先在 `service.yaml` 声明，`make verify` → `make codegen` 后方可编写 Repository。
- Remote 实现必须使用 `CloudRuntimeConfig.gatewayBaseUrl`，禁止硬编码 URL。
- Mock 实现必须覆盖三类 DTO（至少各 2 条混排样本）

<a id="req-003"></a>
### REQ-003 首页双行顶栏为大搜索框加带字小趣，下方全宽一级 Tab

- 安全区只计算一次；上方搜索框占满剩余宽度，小趣紧随其右侧并显示现有「找小趣」标签；下方一级 Tab 独立占满内容宽度。
- 保留搜索 scope=`all`、来源上下文、返回位置、主题和不小于 44pt 热区；Tab 居中/锚定阈值按实际宽度重算。不恢复过期页面、路由或旧助手半屏。
- 首页状态栏安全区与搜索行共用连续品牌蓝底，覆盖屏幕顶端至搜索行下沿；状态栏图标为浅色，小趣标签在蓝底保持可读。一级 Tab 使用独立中性表面，正文不铺蓝；其他页面的默认搜索样式不变。

<a id="req-004"></a>
### REQ-004 首页正文末行全文入口

- 普通 post 超过折叠行数时，在真实可见末行尾部内联「…全文」，点击只原地展开；展开后内联「收起」恢复折叠，不导航。
- 文章摘要超过三行时，在真实第三行尾部内联「…全文」；点击只打开当前文章阅读器一次，与文章整卡使用同一打开动作，不触发普通 post 展开。
- 两类入口共用字素边界截断与真实排版测量，遵循当前 TextScaler、文本方向及 locale，不切断 emoji/组合字符；短文不显示多余入口。入口整体不得断行，其独立最小 44pt 热区参与行内排版，不用覆盖正文的定位热区或另起一行补入口。

## 4. 契约引用

- 父能力公开契约：[`L2 spec`](../spec.md)。

## 5. 验收场景

<a id="gwt-001"></a>
### GWT-001 作品统一内容流

- GIVEN 内容创作者或浏览者具备有效身份，且父能力声明的输入与上游事实成立。
- WHEN 参与者执行“作品统一内容流”对应的公开行为。
- THEN 端点必须先在 `service.yaml` 声明，`make verify` → `make codegen` 后方可编写 Repository。
- AND 失败时返回 canonical failure，且不产生伪成功事实。

<a id="gwt-002"></a>
### GWT-002 首页双行搜索与小趣头部

- GIVEN 用户打开首页。
- WHEN 顶栏完成布局。
- THEN 上方出现大搜索框与带字小趣，下方一级 Tab 占满内容宽度；搜索与小趣入口可进入当前搜索/助手旅程。
- AND 深浅主题、文字放大、窄屏与平板下热区不小于 44pt；Tab 锚定与反向恢复仍成立。
- AND 安全区与搜索行蓝底连续、恰在独立中性 Tab 上沿终止，状态栏为浅色前景，小趣标签保持可读；正文及全站其他搜索入口底色不变。

<a id="gwt-003"></a>
### GWT-003 首页全文入口保留末行与不同动作

- GIVEN 首页展示短文、长普通 post 与长文章摘要，包含 emoji/组合字符，使用窄屏、放大文字或 RTL 排版。
- WHEN 文本按当前宽度布局。
- THEN 短文不出现全文；两种长文的完整「…全文」位于折叠末行且不覆盖正文，截断前缀保持完整字素，测量与渲染使用相同文字缩放和方向。
- WHEN 点击普通 post 全文、再点击收起，或点击文章全文。
- THEN 普通 post 只原地展开/收起且不导航；文章全文只打开当前阅读器一次，点击整卡仍走同一打开动作；各入口热区至少 44pt 且不侵占相邻正文。

## 6. 依赖

- 前置要求：[`dual-rail-discovery-redesign`](../spec.md) 的范围、要求与 SIT。
- 下游结果：本 Story 声明的 GWT 可观察结果。
- 父级设计：[L2 DEC-001](../design.md#dec-001)

## 7. 开放事项

<a id="open-001"></a>
### OPEN-001 作品统一内容流 验收证据

- 类型：`capability_gap`
- 优先级：`P1`
- 准出影响：`track`
- 影响或价值：尚缺少能够证明“作品统一内容流”已满足当前规格的真实测试证据。
- 完成判定：`GWT-001` 对应行为满足且真实测试 `spec_ref` 有效。

<a id="open-002"></a>
### OPEN-002 首页双行搜索与小趣头部证据待双端旅程

- 类型：`capability_gap`
- 优先级：`P1`
- 准出影响：`track`
- 影响或价值：`GWT-002` 的 local_contract 已证明大搜索框、带字小趣与全宽一级 Tab 在场，以及 Tab 在可滚动时尽量居中；深浅主题、文字放大、窄屏与平板 44pt 热区及 Android/iOS 真实旅程尚未闭合。
- 完成判定：`GWT-002` 由子句级 spec_ref 与双端旅程直接绑定。

<a id="open-003"></a>
### OPEN-003 首页末行全文入口设备证据

- 类型：`capability_gap`
- 优先级：`P1`
- 准出影响：`track`
- 影响或价值：`GWT-003` 尚缺 Android/iOS 设备验收证据，末行排版、文字放大、RTL、emoji 与独立热区需双端旅程直接证明；本地测试不替代设备闭环。
- 完成判定：`GWT-003` 的子句级 spec_ref、本地交互证据及双端设备验收全部闭合。
