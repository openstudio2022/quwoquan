config:
  version: "v2026.06.17.rootless-service-plane"
  min_image_version: "1.20260617.rootless-service-plane"
  max_image_version: "1.20260617.rootless-service-plane"

service:
  name: seed-box
  rollout_plane: service
  rollout_mode: rootless-service-plane

notes:
  summary: "stackctl gray/full rollout state snapshot for the prod service-plane rootless migration"
  actual_runtime_services:
    - rec-model-service
    - content-service
    - chat-service
    - user-service
    - assistant-service
    - product-ops-service
    - tag-service
