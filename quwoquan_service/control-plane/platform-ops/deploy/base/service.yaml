apiVersion: v1
kind: Service
metadata:
  name: platform-ops-service
spec:
  selector:
    app.kubernetes.io/name: platform-ops-service
  ports:
    - name: http
      port: 18088
      targetPort: http
