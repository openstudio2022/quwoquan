apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-ops-service
  labels:
    app.kubernetes.io/name: platform-ops-service
  annotations:
    quwoquan.io/config-version: package-required
    quwoquan.io/image-version: package-required
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: platform-ops-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: platform-ops-service
      annotations:
        quwoquan.io/config-version: package-required
        quwoquan.io/image-version: package-required
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: platform-ops-service
          image: quwoquan/platform-ops-service:package-required
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 18088
          envFrom:
            - secretRef:
                name: platform-ops-service-runtime-secrets
          env:
            - name: SERVICE_NAME
              value: platform-ops-service
            - name: APP_ENV
              valueFrom:
                fieldRef:
                  fieldPath: metadata.labels['quwoquan.io/environment']
            - name: CONFIG_ROOT
              value: /etc/qwq/config
            - name: CONFIG_VERSION
              valueFrom:
                fieldRef:
                  fieldPath: metadata.annotations['quwoquan.io/config-version']
            - name: IMAGE_VERSION
              valueFrom:
                fieldRef:
                  fieldPath: metadata.annotations['quwoquan.io/image-version']
          volumeMounts:
            - name: runtime-config
              mountPath: /etc/qwq/config/platform-ops-service.yaml
              subPath: platform-ops-service.yaml
              readOnly: true
          # 就绪探针必须打深层 /readyz：它跑 Postgres/Redis/仓库树/config sync
          # 就绪检查。用浅层 /healthz 判就绪会让依赖断裂时仍然报绿。
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            failureThreshold: 30
            periodSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 1Gi
      volumes:
        - name: runtime-config
          configMap:
            name: platform-ops-service-runtime-config
