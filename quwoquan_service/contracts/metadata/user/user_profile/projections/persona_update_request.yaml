version: 1
read_model: PersonaUpdateRequest

description: |
  PATCH /v1/user/personas/{subAccountId}（UpdatePersona）请求体 wire。
  与 user_profile/service.yaml UpdatePersona 的 request_fields 一致（SubAccountProfileMutation 子集）。

source_entities:
  - SubAccountProfileMutation

consumers:
  - app

client_projection:
  dart_class: PersonaUpdateRequestDto
  base_class: ""
  output_path: cloud/runtime/generated/user/persona_update_request_dto.g.dart
  fields:
    - name: displayName
      dart_type: String
      nullable: true
      source: displayName
      aliases: [displayName]
      default: "null"

    - name: userHandle
      dart_type: String
      nullable: true
      source: userHandle
      aliases: [userHandle]
      default: "null"

    - name: phone
      dart_type: String
      nullable: true
      source: phone
      aliases: [phone]
      default: "null"

    - name: email
      dart_type: String
      nullable: true
      source: email
      aliases: [email]
      default: "null"

    - name: avatarUrl
      dart_type: String
      nullable: true
      source: avatarUrl
      aliases: [avatarUrl]
      default: "null"

    - name: isolationLevel
      dart_type: String
      nullable: true
      source: isolationLevel
      aliases: [isolationLevel]
      default: "null"

    - name: purposeHint
      dart_type: String
      nullable: true
      source: purposeHint
      aliases: [purposeHint]
      default: "null"

    - name: applyScope
      dart_type: String
      nullable: true
      source: applyScope
      aliases: [applyScope]
      default: "null"

    - name: syncTargetIds
      dart_type: List<String>
      nullable: true
      source: syncTargetIds
      aliases: [syncTargetIds]
      default: "null"

    - name: fieldsMask
      dart_type: List<String>
      nullable: true
      source: fieldsMask
      aliases: [fieldsMask]
      default: "null"
