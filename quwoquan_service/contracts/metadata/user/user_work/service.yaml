version: 1
entity: UserWork

service:
  name: user-service
  domain: user

api_routes:
  - method: GET
    path: /v1/users/{userId}/works
    operation: ListUserWorks
    description: 用户作品集列表
    query_params: [cursor, limit]
    response_entity: UserWork
    response_body: UserWork
    response_body_kind: page

consumers:
  - name: app
    capabilities: [read_works]
