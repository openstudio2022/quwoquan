version: 1
aggregate: FollowedSubjectVisitState

events:
  - name: FollowedSubjectVisited
    description: 当前 viewer 推进单个关注对象的私有访问水位
    producer: user-service
    consumers: [following-subject-projector, behavior-service]
    channel: event_store
    payload_entity: FollowedSubjectVisitState
    payload_fields: [personaId, subjectType, subjectId, lastVisitedAt, updatedAt]
