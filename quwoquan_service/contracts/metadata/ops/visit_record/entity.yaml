domain: ops
entity: VisitRecord
object_kind: append_only_fact

description: |
  用户页面或内容访问的只追加事实。MongoDB 承担极高写入量；
  统计结果由具名 Reader 投影，不在事实对象上维护可变计数。

storage_backend: mongodb
cache_layer: none
capabilities:
  - queryable
  - aggregatable

taggable: false
vector_enabled: false

ddd_layer_mapping:
  domain_model: internal/domain/ops/model
  repository: internal/domain/ops/repository
  application: internal/application/ops
  adapter_rest: internal/adapter/rest/ops
  adapter_event: internal/adapter/event/ops
