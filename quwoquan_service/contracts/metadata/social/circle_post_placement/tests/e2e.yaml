aggregate: CirclePostPlacement
layer: e2e

description: |
  端云集成测试（gamma）。

scenarios:
  - name: publish_post_into_circle
    steps:
      - author publishes post selecting a circle
      - moderator pins the placement in circle feed
    assertions:
      - placement_visible_in_circle_feed
      - pinned_placement_leads_feed
