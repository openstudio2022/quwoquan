aggregate: CircleFile
layer: e2e

description: |
  端云集成测试（gamma）。

scenarios:
  - name: upload_and_browse_storage
    steps:
      - member uploads media asset via content media chain
      - member creates circle file referencing ready asset
      - member browses storage section
    assertions:
      - file_visible_with_quota_usage
      - non_member_denied
