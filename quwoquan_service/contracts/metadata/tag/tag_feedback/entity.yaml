version: 1
domain: tag
entity: TagFeedback
object_kind: append_only_fact

description: |
  Persona 或设备对标签推荐的反馈事实。反馈只追加且不修改 TagTaxonomyRelease 或 TagNode。

storage_backend: mongodb
capabilities:
  - auditable
