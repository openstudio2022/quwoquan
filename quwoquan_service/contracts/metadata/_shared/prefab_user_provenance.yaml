schema: prefab_user_provenance
description: 预制用户现行真相源。共享场景 fixture 是端云测试身份的唯一来源。
fixtureSets:
  shared_scenarios:
    source: seed-box.shared_pool
    fixturePath: _shared/test_fixtures/user_pool.json
    purpose: 跨服务场景、端云集成与用户旅程的确定性身份集合
currentUser:
  userId: fixture_user_current
  subAccountId: fixture_persona_daily
