version: 1
domain: circle
aggregate_root: Circle

description: |
  圈子聚合。兴趣/本地/行业/活动/校友等分类圈子。
  MongoDB 存储，支撑圈子发现和成员管理的高并发读取。
  圈子可绑定群聊（Conversation type=circle）。

storage_backend: mongodb
cache_layer: redis
cache_ttl_seconds: 600

capabilities:
  - queryable
  - searchable
  - aggregatable

taggable: true
vector_enabled: false

members:
  - entity: CircleMember
    relation: 1:N
    cascade_delete: true
    description: 圈子成员

ddd_layer_mapping:
  domain_model: internal/domain/circle/model
  repository: internal/domain/circle/repository
  application: internal/application/circle
  adapter_rest: internal/adapter/rest/circle
  adapter_event: internal/adapter/event/circle
