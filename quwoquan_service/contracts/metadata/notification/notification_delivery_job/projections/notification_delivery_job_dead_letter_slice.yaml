read_model: NotificationDeliveryJobDeadLetterSlice

description: NotificationDeliveryJob dead-letter operator 查询的具名切片。

storage_backend: mongodb
collection: notification_delivery_jobs
projection_mode: authoritative_query
source_entities: [NotificationDeliveryJob]
consumers: [notification-service, platform-ops]

fields:
  - { name: items, type: "list<NotificationDeliveryJobDeadLetter>" }
