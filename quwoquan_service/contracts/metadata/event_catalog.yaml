# Event Catalog — root aggregator (canonical per-entity definitions are in domain subdirs)
# New location: contracts/metadata/{domain}/{entity}/events.yaml
events:
  - name: PostCreated
    domain: content
    entity: Post
    canonical: contracts/metadata/content/post/events.yaml

  - name: PostDeleted
    domain: content
    entity: Post
    canonical: contracts/metadata/content/post/events.yaml

  - name: CommentCreated
    domain: content
    entity: Comment
    canonical: contracts/metadata/content/post/events.yaml

  - name: ContentReacted
    domain: content
    entity: Post
    canonical: contracts/metadata/content/post/events.yaml

  - name: MessageSent
    domain: messages
    entity: Conversation
    canonical: contracts/metadata/messages/conversation/events.yaml

  - name: AssistantRunStarted
    domain: assistant
    entity: AssistantRun
    canonical: contracts/metadata/assistant/assistant_run/events.yaml

  - name: LocationNearbyQueried
    domain: integration
    entity: LocationPoi
    canonical: contracts/metadata/integration/location/events.yaml

  - name: LocationSearchQueried
    domain: integration
    entity: LocationPoi
    canonical: contracts/metadata/integration/location/events.yaml
