# 推荐模型服务为 API 门面，无领域聚合根；本文件仅满足目录校验，契约以 service.yaml + fields.yaml 为准。
version: 1
domain: recommendation
entity: ModelScoreRequest

description: |
  推荐模型服务（rec-model-service）为推荐平台下推理服务，Python 独立部署，仅暴露 POST /v1/score 与 GET /health；训练由 rec-model-training 负责。
  本目录无独立存储与领域事件；request/response 契约见 fields.yaml 与 contracts/openapi/rec-model-service.v1.yaml。
