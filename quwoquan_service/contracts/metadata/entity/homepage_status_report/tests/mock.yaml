aggregate: HomepageStatusReport
layer: app_side

description: |
  端侧状态上报 alpha mock 契约。

scenarios:
  - name: submit_status_report
    assertions:
      - report_created_with_pending_review
      - reason_and_evidence_echoed_in_record
