aggregate: HomepageClaimRequest
layer: app_side

description: |
  端侧认领申请 alpha mock 契约。

scenarios:
  - name: submit_claim_request
    assertions:
      - claim_request_created_with_pending_review
      - materials_echoed_in_record

  - name: duplicate_pending_rejected
    assertions:
      - second_pending_claim_rejected
