version: 1
aggregate: Post
layer: service_side

description: |
  云侧契约测试场景（从 service.yaml contract_test.service_side 迁出）。
  服务只依赖自身，使用真实测试数据库，不 mock 存储层。
  执行：go test ./services/content-service/...

real_engines:
  mongodb: testcontainers (mongo:7)
  redis: miniredis/v2

spy_targets: [event_publisher]
mock_targets: [embedding_client, cross_service_clients, llm_client]

data_management:
  seed: 从 fields.yaml 生成 fixture 工厂（fixture.NewPost() 等）
  cleanup: "collection.DeleteMany(ctx, bson.D{}) per test"
  redis_cleanup: miniredis.FlushAll()

# ── CRUD 场景 ────────────────────────────────────────────────────────
scenarios:
  - name: create_post_aggregate
    operation: CreatePost
    go_func: TestCreatePostAggregate
    command: CreatePost (image type)
    seed: []
    assertions:
      - db.posts.FindOne({_id: result.id}) → document exists with correct fields
      - db.media_assets.Find({postId: result.id}) → assets created
      - eventSpy.AssertPublished("PostCreated", {_id, authorId, contentType, tagRefs})
      - embedding mock called with correct input fields

  - name: create_post_all_types
    operation: CreatePost
    go_func: TestCreatePostAllTypes
    command: CreatePost
    variants: [image, video, micro, article]
    seed: []
    assertions:
      - each variant: db.posts.FindOne → contentType matches input
      - each variant: eventSpy.AssertPublished("PostCreated")

  - name: get_post_success
    operation: GetPost
    go_func: TestGetPostSuccess
    command: GetPost
    seed: [fixture.NewPost()]
    assertions:
      - response.postId == seeded.id
      - response does not contain embedding field (privacy: never_expose)
      - response does not contain moderationStatus (visibility: platform-ops only)

  - name: get_post_not_found
    operation: GetPost
    go_func: TestGetPostNotFound
    command: GetPost(nonExistentId)
    seed: []
    assertions:
      - response.code == "CONTENT.USER.post_not_found"
      - http_status == 404

  - name: update_post_forbidden
    operation: UpdatePost
    go_func: TestUpdatePostForbidden
    command: UpdatePost (wrong owner)
    seed: [fixture.NewPost(authorId="user_a")]
    caller: user_b
    assertions:
      - response.code == "CONTENT.USER.forbidden_edit"
      - http_status == 403

  - name: create_post_invalid_content_type
    operation: CreatePost
    go_func: TestCreatePostInvalidContentType
    command: CreatePost (contentType="invalid_type")
    assertions:
      - response.code == "CONTENT.USER.invalid_content_type"
      - http_status == 400

  # ── 互动计数场景 ──────────────────────────────────────────────────
  - name: react_with_counter_strategy
    operation: LikePost
    go_func: TestReactWithCounterStrategy
    command: LikePost
    seed: [fixture.NewPost()]
    assertions:
      - db.content_reactions.FindOne({postId, userId}) → liked=true (upsert verified)
      - miniredis.Get('counter:{postId}:like') → '1' (INCR verified)
      - eventSpy.AssertPublished("ContentReacted", {postId, userId, liked: true})
      - miniredis.Exists('cache:post:{postId}') → false (invalidated)

  - name: react_idempotent
    operation: LikePost
    go_func: TestReactIdempotent
    command: LikePost x2 on same post
    seed: [fixture.NewPost()]
    assertions:
      - db.content_reactions.CountDocuments({postId, userId}) → exactly 1
      - miniredis.Get('counter:{postId}:like') → '1' (not double counted)

  - name: unlike_decrements_counter
    operation: UnlikePost
    go_func: TestUnlikeDecrementsCounter
    command: LikePost then UnlikePost
    seed: [fixture.NewPost()]
    setup:
      - LikePost to establish baseline
    assertions:
      - db.content_reactions.FindOne → liked=false
      - miniredis.Get('counter:{postId}:like') → '0' (or not exist)
      - eventSpy.AssertPublished("ContentReacted", {liked: false})

  # ── 评论场景 ──────────────────────────────────────────────────────
  - name: comment_with_notification
    operation: CreateComment
    go_func: TestCommentWithNotification
    command: CreateComment
    seed: [fixture.NewPost()]
    assertions:
      - db.comments.FindOne({_id: result.id}) → document with correct postId + authorId
      - db.posts.FindOne({_id: postId}).commentCount → incremented by 1
      - eventSpy.AssertPublished("CommentCreated", {postId, authorId, content})

  - name: comment_list_pagination
    operation: ListComments
    go_func: TestCommentListPagination
    command: ListComments with cursor pagination
    seed: [fixture.NewPost(), fixture.NewComments(count=25)]
    assertions:
      - first page: len(comments) == 20 (default limit)
      - cursor present in response
      - second page using cursor: len(comments) == 5
      - no overlap between pages

  - name: comment_list_recommended_latest_most_liked
    operation: ListComments
    go_func: TestCommentListRecommendedLatestMostLiked
    command: ListComments(sort=recommended/latest/most_liked)
    seed: [fixture.NewPost(), fixture.NewComments(withMixedScore=true)]
    assertions:
      - recommended order uses recommendedScore/freshness stable tie-breaker
      - latest order uses createdAt DESC
      - most_liked order uses likeCount DESC then createdAt DESC
      - hidden/deleted comments are excluded

  - name: comment_reply_preview_and_expand_contract
    operation: ListCommentReplies
    go_func: TestCommentReplyPreviewAndExpandContract
    command: ListComments then ListCommentReplies
    seed: [fixture.NewPost(), fixture.NewCommentWithReplies(count=21)]
    assertions:
      - ListComments first parent includes replyPreview length == reply_preview_count
      - replyNextCursor is present when replyCount exceeds preview count
      - ListCommentReplies returns at most reply_expand_page_size replies
      - second replies page has no overlap

  - name: create_comment_with_attachment_and_mentions
    operation: CreateComment
    go_func: TestCreateCommentWithAttachmentAndMentions
    command: CreateComment(attachmentMediaIds, mentions)
    seed: [fixture.NewPost(), fixture.NewReadyCommentAttachment(), fixture.NewMentionTargets()]
    assertions:
      - db.comments.FindOne stores attachmentMediaIds and mentions
      - response.attachments contains ready media snapshot
      - assistantMentioned == true when mentions contains assistant subject

  - name: react_to_comment_three_state_contract
    operation: ReactToComment
    go_func: TestReactToCommentThreeStateContract
    command: ReactToComment(like -> dislike -> none)
    seed: [fixture.NewPost(), fixture.NewComment()]
    assertions:
      - like sets viewerReaction=like and increments likeCount only once
      - dislike clears like, sets viewerReaction=dislike, increments dislikeCount
      - none clears both counters for the viewer
      - eventSpy.AssertPublished("CommentReacted")

  # ── Feed 场景 ─────────────────────────────────────────────────────
  - name: get_feed_by_type
    operation: GetFeed
    go_func: TestGetFeedByType
    command: GetFeed (type=image)
    seed: [fixture.NewPosts(count=10, contentType=image), fixture.NewPosts(count=5, contentType=video)]
    assertions:
      - response.items all have contentType=image
      - response.items.length == 10
      - cursor present

  - name: get_feed_by_identity
    operation: GetFeed
    go_func: TestGetFeedByIdentity
    command: GetFeed (identity=moment/work)
    seed: [fixture.NewPosts(count=6, contentType=micro), fixture.NewPosts(count=4, contentType=image)]
    assertions:
      - identity=moment -> response.items all resolve identity=moment
      - identity=work -> response.items all resolve identity=work
      - identity=moment 时忽略 type 过滤

  - name: get_feed_excludes_private_posts
    operation: GetFeed
    go_func: TestGetFeedExcludesPrivatePosts
    command: GetFeed (any type)
    seed: [post(micro,visibility=public), post(micro,visibility=private)]
    assertions:
      - response.items do not contain the private post
      - private content never appears in discovery feed

  - name: get_feed_cursor_pagination
    operation: GetFeed
    go_func: TestGetFeedCursorPagination
    command: GetFeed with cursor
    seed: [fixture.NewPosts(count=30, contentType=image)]
    assertions:
      - first page: 20 items, cursor present
      - second page using cursor: 10 items, no duplicate

  - name: get_feed_recommend_sort_with_cursor
    operation: GetFeed
    go_func: TestGetFeedRecommendSortWithCursor
    command: GetFeed (sort=recommend) with cursor
    seed: [fixture.NewPosts(count=30, contentType=image)]
    assertions:
      - first page: cursor present and opaque token format
      - second page using cursor: no overlap with page1
      - pagination remains stable for recommend sort

  - name: get_feed_future_window_changes_only
    operation: GetFeed
    go_func: TestGetFeedFutureWindowChangesOnly
    command: GetFeed (sort=recommend) after feedback update
    seed: [fixture.NewPosts(count=40, mixed contentType)]
    assertions:
      - items returned before cursor are treated as client-owned history
      - strong feedback affects only future window after cursor
      - future window keeps no duplicate guarantee

  - name: get_feed_issues_server_feed_request_id
    operation: GetFeed
    go_func: TestFeedIssuesServerFeedRequestID
    command: GetFeed (sort=recommend) issues server-authoritative feedRequestId
    seed: [fixture.NewPosts(count=10, contentType=image)]
    assertions:
      - response envelope feedRequestId is server-issued with frq_ prefix
      - response envelope carries rankingVersion + reasonVersion
      - echoing feedRequestId on next page keeps the same attribution id

  # ── 行为上报场景 ──────────────────────────────────────────────────
  - name: behavior_batch_report
    operation: ReportBehaviors
    go_func: TestBehaviorBatchReport
    command: ReportBehaviors (batch of impression+dwell+click)
    assertions:
      - http_status == 200
      - response.status == "ok"
      - response.accepted == 3

  - name: behavior_batch_empty
    operation: ReportBehaviors
    go_func: TestBehaviorBatchEmpty
    command: ReportBehaviors (empty events array)
    assertions:
      - response.code == "CONTENT.USER.invalid_argument"
      - http_status == 400

  # ── 计数回写场景 ──────────────────────────────────────────────────
  - name: counter_flush
    go_func: TestCounterFlush
    status: pending
    command: FlushCounters (timer-triggered)
    seed: [fixture.NewPost()]
    setup:
      - miniredis.Set('counter:{postId}:like', '42')
    assertions:
      - db.posts.FindOne({_id}).likeCount → 42 (flushed from Redis)
      - miniredis.Exists('counter:{postId}:like') → false or reset

  # ── 内容审核流转场景 ──────────────────────────────────────────────
  - name: post_moderation_flow
    go_func: TestPostModerationFlow
    status: pending
    command: ModeratePost (pending → approved → rejected)
    seed: [fixture.NewPost(moderationStatus=pending)]
    assertions:
      - after approve: db.posts.FindOne.moderationStatus → 'approved'
      - after reject: db.posts.FindOne.moderationStatus → 'rejected'
      - eventSpy.AssertPublished("ContentModerated") for each transition

  # ── 未覆盖 API 占位（status: pending，实施时补 go_func）────────────────
  - name: search_posts_contract
    operation: SearchPosts
    go_func: TestSearchPostsContract
    status: pending
    command: SearchPosts
  - name: list_profile_interaction_activities_received_contract
    operation: ListProfileInteractionActivitiesReceived
    go_func: TestListProfileInteractionActivitiesReceivedContract
    status: pending
    command: ListProfileInteractionActivitiesReceived
  - name: list_profile_interaction_activities_sent_contract
    operation: ListProfileInteractionActivitiesSent
    go_func: TestListProfileInteractionActivitiesSentContract
    status: pending
    command: ListProfileInteractionActivitiesSent
  - name: publish_post_contract
    operation: PublishPost
    go_func: TestPublishPostContract
    command: PublishPost
  - name: delete_post_contract
    operation: DeletePost
    go_func: TestDeletePostContract
    command: DeletePost
  - name: update_post_circles_contract
    operation: UpdatePostCircles
    go_func: TestUpdatePostCirclesContract
    status: pending
    command: UpdatePostCircles
  - name: delete_comment_contract
    operation: DeleteComment
    go_func: TestDeleteComment
    command: DeleteComment
  - name: get_reaction_state_contract
    operation: GetReactionState
    go_func: TestGetReactionStateContract
    status: pending
    command: GetReactionState
  - name: get_counters_contract
    operation: GetCounters
    go_func: TestGetCounters
    command: GetCounters
  - name: repost_to_circle_contract
    operation: RepostToCircle
    go_func: TestRepostToCircleContract
    status: pending
    command: RepostToCircle
  - name: quote_to_circle_contract
    operation: QuoteToCircle
    go_func: TestQuoteToCircleContract
    status: pending
    command: QuoteToCircle
  - name: init_media_upload_contract
    operation: InitMediaUpload
    go_func: TestInitMediaUploadContract
    status: pending
    command: InitMediaUpload
  - name: complete_media_upload_contract
    operation: CompleteMediaUpload
    go_func: TestCompleteMediaUploadContract
    status: pending
    command: CompleteMediaUpload
  - name: abort_media_upload_contract
    operation: AbortMediaUpload
    go_func: TestAbortMediaUploadContract
    status: pending
    command: AbortMediaUpload
  - name: get_media_asset_contract
    operation: GetMediaAsset
    go_func: TestGetMediaAssetContract
    status: pending
    command: GetMediaAsset
  - name: select_auto_video_cover_contract
    operation: SelectAutoVideoCover
    go_func: TestSelectAutoVideoCoverContract
    status: pending
    command: SelectAutoVideoCover
  - name: select_manual_video_cover_contract
    operation: SelectManualVideoCover
    go_func: TestSelectManualVideoCoverContract
    status: pending
    command: SelectManualVideoCover
  - name: generate_article_summary_contract
    operation: GenerateArticleSummary
    go_func: TestGenerateArticleSummaryContract
    status: pending
    command: GenerateArticleSummary
  - name: get_helper_read_contract
    operation: GetHelperRead
    go_func: TestGetHelperReadContract
    status: pending
    command: GetHelperRead
  - name: update_post_settings_contract
    operation: UpdatePostSettings
    go_func: TestUpdatePostSettingsContract
    command: UpdatePostSettings
  - name: promote_post_to_work_contract
    operation: PromotePostToWork
    go_func: TestPromotePostToWorkContract
    command: PromotePostToWork
  - name: promote_post_preserves_threads_contract
    operation: PromotePostToWork
    go_func: TestPromotePostKeepsCountersAndCommentThread
    command: PromotePostToWork(retain postId/comments/reactions)
  - name: list_user_posts_contract
    operation: ListUserPosts
    go_func: TestListUserPosts
    command: ListUserPosts
  - name: list_user_posts_by_identity
    operation: ListUserPosts
    go_func: TestListUserPostsByIdentity
    command: ListUserPosts(identity=work,type=article)
  - name: circle_visible_view_access_contract
    operation: GetPost
    go_func: TestCircleVisiblePostAllowsCircleMemberViewer
    command: GetPost/ListUserPosts(circle_visible member access)
  - name: assistant_access_revoked_after_settings_change
    operation: UpdatePostSettings
    go_func: TestAssistantAccessRevokedAfterSettingsChange
    command: UpdatePostSettings(assistantUsePolicy=exclude or visibility=private)
  - name: react_to_comment_contract
    operation: ReactToComment
    go_func: TestReactToCommentContract
    command: ReactToComment(like/dislike/none)
  - name: list_comment_replies_contract
    operation: ListCommentReplies
    go_func: TestCommentRepliesAttachmentsAndMentionsContract
    command: ListCommentReplies(reply pagination)
  - name: bind_media_assets_to_comment_contract
    operation: BindMediaAssetsToComment
    go_func: TestCommentRepliesAttachmentsAndMentionsContract
    command: BindMediaAssetsToComment/CreateComment(attachmentMediaIds)
  - name: list_comments_by_author_contract
    operation: ListCommentsByAuthor
    go_func: TestListCommentsByAuthor
    command: ListCommentsByAuthor
  - name: list_comments_for_post_author_contract
    operation: ListCommentsForPostAuthor
    go_func: TestListCommentsForPostAuthorContract
    status: pending
    command: ListCommentsForPostAuthor
  - name: get_app_config_contract
    operation: GetAppConfig
    go_func: TestGetAppConfig
    command: GetAppConfig
  - name: pin_comment_contract
    operation: PinComment
    go_func: TestCommentPinContractApiIntegration
    command: PinComment
  - name: unpin_comment_contract
    operation: UnpinComment
    go_func: TestCommentPinContractApiIntegration
    command: UnpinComment
  - name: get_author_impact_contract
    operation: GetAuthorImpact
    go_func: TestGetAuthorImpactReturnsBehaviorAggregation
    command: GetAuthorImpact
  - name: list_author_impact_evidence_contract
    operation: ListAuthorImpactEvidence
    go_func: TestAuthorImpactEvidenceDrilldownIsContentAnchoredAndPrivacySafe
    command: ListAuthorImpactEvidence
  - name: share_post_contract
    operation: SharePost
    go_func: TestShareCountersStayAuthoritativeAndIdempotent
    command: SharePost
  - name: unshare_post_contract
    operation: UnsharePost
    go_func: TestCommentCountersStayConsistentAcrossReadModels
    command: UnsharePost
  - name: get_comment_counts_delta_contract
    operation: GetCommentCountsDelta
    go_func: TestCommentCountsDelta_ExplainableHalfOpenWindow
    command: GetCommentCountsDelta
  - name: get_my_footprint_contract
    operation: GetMyFootprint
    go_func: TestGetMyFootprintContract
    command: GetMyFootprint
  - name: request_original_image_access_contract
    operation: RequestOriginalImageAccess
    go_func: TestRequestOriginalImageAccessContract
    command: RequestOriginalImageAccess
  - name: bind_media_assets_to_post_contract
    operation: BindMediaAssetsToPost
    go_func: TestBindMediaAssetsToPostContract
    command: BindMediaAssetsToPost
  - name: get_my_intersection_summary_contract
    operation: GetMyIntersectionSummary
    go_func: TestIntersectionServiceLocalContractTest
    command: GetMyIntersectionSummary
  - name: list_my_intersections_contract
    operation: ListMyIntersections
    go_func: TestIntersectionServiceLocalContractTest
    command: ListMyIntersections
  - name: mark_intersections_visited_contract
    operation: MarkIntersectionsVisited
    go_func: TestIntersectionServiceLocalContractTest
    command: MarkIntersectionsVisited
  - name: get_object_intersections_contract
    operation: GetObjectIntersections
    go_func: TestIntersectionServiceLocalContractTest
    command: GetObjectIntersections
