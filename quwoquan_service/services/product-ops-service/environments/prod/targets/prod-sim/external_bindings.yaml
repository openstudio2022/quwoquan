externalBindings:
  product.telemetry.sink:
    state: enabled
    adapter: ext.obs.elasticsearch
    endpointRef: local_topology:elasticsearch
  runtime.log.sink:
    state: enabled
    adapter: ext.obs.elasticsearch
    endpointRef: local_topology:elasticsearch
