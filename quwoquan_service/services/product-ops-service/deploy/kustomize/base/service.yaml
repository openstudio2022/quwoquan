apiVersion: v1
kind: Service
metadata:
  name: product-ops-service
  labels:
    app.kubernetes.io/name: product-ops-service
spec:
  selector:
    app.kubernetes.io/name: product-ops-service
  ports:
    - name: http
      protocol: TCP
      port: 18086
      targetPort: http
