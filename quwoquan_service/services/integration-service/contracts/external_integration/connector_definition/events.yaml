events:
- name: ConnectorDefinitionPublished
  description: 官方 Connector immutable release 激活后发布目录变更摘要。
  producer: integration-service
  consumers: [assistant-service]
  channel: integration.connector_definition
  payload_entity: ConnectorDefinition
  payload_fields: [connectorId, capabilities, authorizationMode, confirmationPolicy, dataClassification, status, releaseDigest, publishedAt]

