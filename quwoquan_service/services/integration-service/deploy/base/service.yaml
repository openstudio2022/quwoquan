apiVersion: v1
kind: Service
metadata:
  name: integration-service
  labels:
    app.kubernetes.io/name: integration-service
spec:
  selector:
    app.kubernetes.io/name: integration-service
  ports:
    - name: http
      port: 18086
      targetPort: http
