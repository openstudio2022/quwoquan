description: TripPlanContentLink 放置、移动、移除和 named Reader。
commercial_defaults:
  status: blocked
  block_reason: 缺 Content Post authority、投影、真实 API 与双端 UAT
  gap_id: TRAVEL_TRIP_CONTENT_LINK_COMMERCIAL_CLOSURE
  target_story: trip-moment-content-link
api_routes:
- method: PUT
  path: /travel/trips/{tripId}/content-links/{postId}
  operation: PutTripPlanContentLink
  actor: persona
  request_bindings:
    path: [{name: tripId, field: tripId}, {name: postId, field: postId}]
  security: {auth_mode: required}
  application: {kind: command, facet: TripPlanContentLinkCommandFacet, method: putTripPlanContentLink, aggregate_owner: TripPlanContentLink, mutation_target: TripPlanContentLink, invariant_target: TripPlanContentLink}
  request_entity: PutTripPlanContentLinkRequest
  response_entity: TripPlanContentLinkSlice
  authorization: {principal: persona, ownership_policy: active_trip_member_and_visible_post}
  reliability: {timeout_ms: 2500, cancellation: supported, retry_mode: idempotent, max_attempts: 2, idempotency: required}
  error_codes: [TRAVEL.USER.trip_content_link_invalid_argument, TRAVEL.USER.trip_not_found, TRAVEL.USER.trip_permission_denied, TRAVEL.USER.trip_content_link_revision_conflict, TRAVEL.USER.trip_content_link_idempotency_conflict, TRAVEL.DEPENDENCY.trip_content_link_post_unavailable, TRAVEL.SYSTEM.trip_content_link_storage_failed]
  privacy: {request_classification: SENSITIVE, response_classification: SENSITIVE, log_policy: metadata_only}
  telemetry: {metric: travel_trip_content_link_put, trace: true, attributes: [outcome, visibility]}
  slo: {latency_p95_ms: 900, availability_percent: 99.9}
  request_body_kind: object
- method: POST
  path: /travel/trips/{tripId}/content-link-removals/{postId}
  operation: RemoveTripPlanContentLink
  actor: persona
  request_bindings:
    path: [{name: tripId, field: tripId}, {name: postId, field: postId}]
  security: {auth_mode: required}
  application: {kind: command, facet: TripPlanContentLinkCommandFacet, method: removeTripPlanContentLink, aggregate_owner: TripPlanContentLink, mutation_target: TripPlanContentLink, invariant_target: TripPlanContentLink}
  request_entity: RemoveTripPlanContentLinkRequest
  response_entity: TripPlanContentLinkSlice
  authorization: {principal: persona, ownership_policy: link_owner_or_trip_organizer}
  reliability: {timeout_ms: 2000, cancellation: supported, retry_mode: idempotent, max_attempts: 2, idempotency: required}
  error_codes: [TRAVEL.USER.trip_content_link_invalid_argument, TRAVEL.USER.trip_content_link_not_found, TRAVEL.USER.trip_permission_denied, TRAVEL.USER.trip_content_link_revision_conflict, TRAVEL.USER.trip_content_link_idempotency_conflict, TRAVEL.SYSTEM.trip_content_link_storage_failed]
  privacy: {request_classification: SENSITIVE, response_classification: SENSITIVE, log_policy: metadata_only}
  telemetry: {metric: travel_trip_content_link_remove, trace: true, attributes: [outcome]}
  slo: {latency_p95_ms: 700, availability_percent: 99.9}
  request_body_kind: object
- method: GET
  path: /travel/trips/{tripId}/content-links
  operation: ListTripPlanContentLinks
  actor: persona
  request_bindings: {path: [{name: tripId, field: tripId}]}
  request_entity: ListTripPlanContentLinksQuery
  security: {auth_mode: required}
  application: {kind: query, facet: TripPlanContentLinkQueryFacet, method: listTripPlanContentLinks, reader: TripPlanContentLinkReader, slice: TripPlanContentLinkListSlice}
  response_entity: TripPlanContentLinkListSlice
  authorization: {principal: persona, ownership_policy: active_trip_member}
  reliability: {timeout_ms: 1000, cancellation: supported, retry_mode: idempotent, max_attempts: 2, idempotency: none}
  error_codes: [TRAVEL.USER.trip_not_found, TRAVEL.USER.trip_permission_denied, TRAVEL.SYSTEM.trip_content_link_storage_failed]
  privacy: {request_classification: SENSITIVE, response_classification: SENSITIVE, log_policy: metadata_only}
  telemetry: {metric: travel_trip_content_link_list, trace: true, attributes: [outcome, link_count]}
  slo: {latency_p95_ms: 500, availability_percent: 99.9}
  request_body_kind: none
