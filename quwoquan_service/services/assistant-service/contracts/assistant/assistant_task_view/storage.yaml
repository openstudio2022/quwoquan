backend: owner_named_readers
role: projection
description: >-
  AssistantTaskView 不物化数据库或缓存副本；读取时只经 SkillSubscription
  owner named reader 与 digest 验证的 active SkillCatalog 联邦派生。任务身份、状态、
  nextAttemptAt 与 updatedAt 继续只由 SkillSubscription 持久化。
