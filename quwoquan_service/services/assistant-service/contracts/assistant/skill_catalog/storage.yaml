backend: service_resource
role: projection
description: >-
  SkillCatalog 由服务内 active-release CatalogSource 在请求时读取 digest 已验证的
  immutable package 资产；本对象不物化数据库、缓存副本或 codegen Store，账号
  Consent、Setting 与 Subscription 的持久化仍只归各自独立对象契约所有。
