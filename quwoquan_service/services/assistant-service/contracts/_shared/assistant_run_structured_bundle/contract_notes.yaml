# 逻辑契约说明（非 codegen 输入）：Run `structuredResponse` 根 JSON 与 [AssistantRunStructuredBundle] 对齐。
# 实现：quwoquan_app/lib/assistant/contracts/assistant_run_structured_bundle.dart
# 组成：runArtifacts（metadata run_artifacts）+ AssistantStructuredResponseWire 子树（扁平键）。
