dart_class: SlotFillPlanDto
library_path: service/assistant_service/assistant/assistant_run/domain/planner_contracts.dart
output_path: service/assistant_service/assistant/assistant_run/domain/generated/planner_contracts.g.dart
contract: planner_contracts
kind: runtime_protocol_contract
fields:
  - name: entries
    source: entries
    type: map<object>
    ref: slot_fill_entry
    default: {}
subcontracts:
  slot_fill_entry:
    class_name: SlotFillEntry
    fields:
      - name: slotId
        source: slotId
        type: string
        required: true
      - name: value
        source: value
        type: any
      - name: source
        source: source
        type: enum
        enum_ref: SlotSource
        default: ''
      - name: action
        source: action
        type: enum
        enum_ref: SlotFillAction
        default: ''
      - name: confidence
        source: confidence
        type: double
        default: 0.0
