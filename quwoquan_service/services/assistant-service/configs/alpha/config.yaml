# alpha overrides (developer single-instance validation)
service:
  http:
    addr: ":18087"

postgres:
  dsn: "postgres://quwoquan:quwoquan@127.0.0.1:5432/quwoquan?sslmode=disable"

mongodb:
  uri: "mongodb://127.0.0.1:27017"
  database: quwoquan_assistant

redis:
  rec:
    mode: standalone
    addr: "127.0.0.1:6379"
    db: 0
  general:
    mode: standalone
    addr: "127.0.0.1:6379"
    db: 0

# alpha 单机验证：app_search 直连本地 search-service canonical SearchQuery。
search_service:
  base_url: "http://127.0.0.1:18095"
  timeout_ms: 3000

entity_service:
  base_url: "http://127.0.0.1:18084"
  timeout_ms: 3000

content_service:
  base_url: "http://127.0.0.1:18080"
  timeout_ms: 3000

# alpha 单机验证：主动 skill 个性化读取本地 user-service 兴趣画像
user_profile:
  base_url: "http://127.0.0.1:18081"
  timeout_ms: 5000

notification_service:
  base_url: "http://127.0.0.1:18088"
  timeout_ms: 5000
