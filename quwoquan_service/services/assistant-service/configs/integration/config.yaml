# integration overrides (shared test env)
service:
  http:
    addr: ":18087"

postgres:
  dsn: ""

mongodb:
  uri: ""
  database: quwoquan_assistant

redis:
  rec:
    mode: standalone
    addr: ""
    db: 0
  general:
    mode: standalone
    addr: ""
    db: 1
