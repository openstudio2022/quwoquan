package local_contract

import "testing"

// TestMigratedCommercialMetrics validates the former white-box behavior through the public assistant application port.
func TestMigratedCommercialMetricsApplicationPort(t *testing.T) {
	assertMigratedAssistantApplicationPort(t)
}
