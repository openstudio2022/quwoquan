apiVersion: v1
kind: Service
metadata:
  name: assistant-service
  labels:
    app.kubernetes.io/name: assistant-service
spec:
  selector:
    app.kubernetes.io/name: assistant-service
  ports:
    - name: http
      port: 18087
      targetPort: http
