apiVersion: v1
kind: Service
metadata:
  name: tag-service
  labels:
    app.kubernetes.io/name: tag-service
spec:
  selector:
    app.kubernetes.io/name: tag-service
  ports:
    - name: http
      port: 18092
      targetPort: http
