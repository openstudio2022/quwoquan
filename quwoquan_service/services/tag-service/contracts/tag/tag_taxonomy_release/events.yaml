description: TagTaxonomyRelease 的写入口只有数据工程 publish/import，仓内没有 taxonomy release publisher 或 outbox；下游 TagNodeView 与 ObjectTagIndexView
  以 active release digest 为 checkpoint 主动拉取激活版本，跨服务只引用不可变 taxonomyReleaseId 并向 taxonomy authority 校验，因此不声明激活事件。
events: []
