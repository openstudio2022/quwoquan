apiVersion: v1
kind: Service
metadata:
  name: api-edge
  labels:
    app.kubernetes.io/name: api-edge
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: api-edge
  ports:
  - name: http
    port: 18079
    targetPort: http
