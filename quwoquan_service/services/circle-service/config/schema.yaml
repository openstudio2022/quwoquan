description: 该自治边界的运行配置定义与默认值；环境只维护差异和引用。
configs:
- key: sys.circle-service.user_account_security_authority.base_url
  type: string
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
- key: sys.circle-service.user_account_security_authority.timeout_ms
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
- key: sys.circle-service.es.embeddingDims
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 0
- key: sys.circle-service.es.enabled
  type: bool
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: false
- key: sys.circle-service.es.endpoints
  type: list
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: []
- key: sys.circle-service.es.index
  type: string
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: quwoquan_objects
- key: sys.circle-service.es.insecureTls
  type: bool
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: false
- key: sys.circle-service.es.replicas
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 1
- key: sys.circle-service.es.requestTimeoutMs
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 800
- key: sys.circle-service.es.shards
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 1
- key: sys.circle-service.es.synonyms
  type: list
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: []
- key: sys.circle-service.mongo.database
  type: string
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: quwoquan_circle
- key: sys.circle-service.mongo.uri
  type: string
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: true
- key: sys.circle-service.redis.general.mode
  type: string
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
- key: sys.circle-service.redis.general.addr
  type: string
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: ''
- key: sys.circle-service.redis.general.db
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 0
- key: sys.circle-service.redis.general.password
  type: string
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: true
- key: sys.circle-service.service.http.addr
  type: string
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: :18082
