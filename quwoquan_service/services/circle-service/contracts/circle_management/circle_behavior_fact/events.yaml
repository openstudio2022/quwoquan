events:
- name: CircleBehaviorFactAppended
  description: 非 canonical 结构字段迁移注记：recommend_impact="hot_path_signal"。
  delivery_semantics: transactional_outbox
  payload_entity: CircleBehaviorFact
  payload_fields:
  - _id
  - actorKind
  - personaId
  - deviceActorId
  - circleId
  - eventType
  - sessionId
  - requestId
  - occurredAt
