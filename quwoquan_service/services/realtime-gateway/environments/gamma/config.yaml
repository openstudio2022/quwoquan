overrides:
  sys.realtime-gateway.user_service.account_security.base_url: http://user-service:18081
  sys.realtime-gateway.user_service.account_security.timeout_ms: 500
secretRefs:
  sys.realtime-gateway.redis.realtime.password: REALTIME_REDIS_PASSWORD
externalBindings:
  runtime.message.transport:
    state: enabled
