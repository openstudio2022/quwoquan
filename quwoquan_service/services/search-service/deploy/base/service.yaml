apiVersion: v1
kind: Service
metadata:
  name: search-service
  labels:
    app.kubernetes.io/name: search-service
spec:
  selector:
    app.kubernetes.io/name: search-service
  ports:
    - name: http
      port: 18095
      targetPort: http
