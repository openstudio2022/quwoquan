# beta overrides (developer local cloud-client integration)
# beta runs the dedicated ES cluster; endpoints are injected via SEARCH_ES_ENDPOINTS.
service:
  http:
    addr: ":18095"

es:
  enabled: true
  endpoints: []
  index: "quwoquan_objects"

redis:
  rec:
    mode: standalone
    addr: "127.0.0.1:6379"
    db: 0
