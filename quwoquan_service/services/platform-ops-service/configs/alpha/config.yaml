# alpha overrides (developer single-instance validation)
service:
  http:
    addr: ":18087"

postgres:
  dsn: "${POSTGRES_DSN}"

redis:
  rec:
    mode: standalone
    addr: "127.0.0.1:6379"
    db: 0
