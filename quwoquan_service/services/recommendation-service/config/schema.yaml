description: 该自治边界的运行配置定义与默认值；环境只维护差异和引用。
configs:
- key: ops.reco.discovery.prerank.new_content_boost
  type: float
  owner: recommendation
  default: 1.08
  scope: environment
  reload: hot
  rollout: progressive
  risk_level: high
  ui_editable: true
- key: ops.reco.discovery.rank.author_diversity_weight
  type: float
  owner: recommendation
  default: 0.42
  scope: environment
  reload: hot
  rollout: progressive
  risk_level: high
  ui_editable: true
- key: ops.reco.discovery.recall.whitelist_enabled
  type: bool
  owner: recommendation
  default: false
  scope: environment
  reload: hot
  rollout: progressive
  risk_level: medium
  ui_editable: true
- key: sys.recommendation-service.redis.general.addr
  type: string
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: ''
- key: sys.recommendation-service.redis.general.addrs
  type: list
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: []
- key: sys.recommendation-service.redis.general.db
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 1
- key: sys.recommendation-service.redis.general.mode
  type: string
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: standalone
- key: sys.recommendation-service.redis.general.password
  type: string
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: true
- key: sys.recommendation-service.redis.general.pool.dial_timeout_ms
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 500
- key: sys.recommendation-service.redis.general.pool.min_idle
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 0
- key: sys.recommendation-service.redis.general.pool.read_timeout_ms
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 200
- key: sys.recommendation-service.redis.general.pool.size
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 0
- key: sys.recommendation-service.redis.general.pool.write_timeout_ms
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 200
- key: sys.recommendation-service.redis.general.tls
  type: bool
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: false
- key: sys.recommendation-service.redis.rec.addr
  type: string
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: ''
- key: sys.recommendation-service.redis.rec.addrs
  type: list
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: []
- key: sys.recommendation-service.redis.rec.db
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 0
- key: sys.recommendation-service.redis.rec.mode
  type: string
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: standalone
- key: sys.recommendation-service.redis.rec.password
  type: string
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: true
- key: sys.recommendation-service.redis.rec.pool.dial_timeout_ms
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 500
- key: sys.recommendation-service.redis.rec.pool.min_idle
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 0
- key: sys.recommendation-service.redis.rec.pool.read_timeout_ms
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 100
- key: sys.recommendation-service.redis.rec.pool.size
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 0
- key: sys.recommendation-service.redis.rec.pool.write_timeout_ms
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 100
- key: sys.recommendation-service.redis.rec.tls
  type: bool
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: false
- key: sys.recommendation-service.ranked_window.quota_shard_count
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 256
- key: sys.recommendation-service.ranked_window.maximum_live_records_per_shard
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 128
- key: sys.recommendation-service.ranked_window.maximum_live_bytes_per_shard
  type: int
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: 134217728
- key: sys.recommendation-service.service.http.addr
  type: string
  scope: workload
  reload: restart
  rollout: progressive
  sensitive: false
  default: :8000
