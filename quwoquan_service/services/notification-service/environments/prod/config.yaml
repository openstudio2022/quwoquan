overrides:
  sys.notification-service.accountSecurityAuthority.baseUrl: http://user-service:18081
  sys.notification-service.accountSecurityAuthority.timeoutMs: 300
secretRefs:
  sys.notification-service.mongo.uri: NOTIFICATION_MONGO_URI
  sys.notification-service.redis.password: REDIS_PASSWORD
externalBindings:
  runtime.message.transport:
    state: enabled
