events:
- name: PersonaCreated
  delivery_semantics: transactional_outbox
  payload_entity: Persona
  payload_fields:
  - userId
  - personaId
- name: PersonaUpdated
  delivery_semantics: transactional_outbox
  payload_entity: Persona
  payload_fields:
  - userId
  - personaId
- name: PersonaRetired
  delivery_semantics: transactional_outbox
  payload_entity: Persona
  payload_fields:
  - userId
  - personaId
- name: PersonaActivated
  delivery_semantics: transactional_outbox
  payload_entity: Persona
  payload_fields:
  - userId
  - personaId
