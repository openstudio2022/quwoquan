events:
- name: PersonaCreated
  producer: user-service
  consumers:
  - projection-workers
  channel: transactional_outbox
  payload_entity: Persona
  payload_fields:
  - userId
  - personaId
- name: PersonaUpdated
  producer: user-service
  consumers:
  - projection-workers
  channel: transactional_outbox
  payload_entity: Persona
  payload_fields:
  - userId
  - personaId
- name: PersonaRetired
  producer: user-service
  consumers:
  - projection-workers
  channel: transactional_outbox
  payload_entity: Persona
  payload_fields:
  - userId
  - personaId
- name: PersonaActivated
  producer: user-service
  consumers:
  - projection-workers
  channel: transactional_outbox
  payload_entity: Persona
  payload_fields:
  - userId
  - personaId
