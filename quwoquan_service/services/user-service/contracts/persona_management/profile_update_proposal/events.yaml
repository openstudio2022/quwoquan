events:
- name: ProfileUpdateProposalCreated
  delivery_semantics: transactional_event_log
  topic: events.user.profile_update_proposal
  payload_entity: ProfileUpdateProposal
  payload_fields:
  - id
  - personaId
  - source
  - proposedChanges
  - reason
  - evidenceRefs
  - impactScope
  - createdBy
  - createdRequestId
  - createdTraceId
  - status
  - version
  - createdAt
  no_consumer_reason: Proposal 创建与审核记录已在对象事务中持久化；事件只作为对象级审计日志保留，没有下游订阅方。
- name: ProfileUpdateProposalConfirmed
  delivery_semantics: transactional_event_log
  topic: events.user.profile_update_proposal
  payload_entity: ProfileUpdateProposal
  payload_fields:
  - id
  - personaId
  - status
  - version
  - reviewedBy
  - targetPersonaExpectedVersion
  no_consumer_reason: Proposal 确认与审核记录已在对象事务中持久化；事件只作为对象级审计日志保留，没有下游订阅方。
- name: ProfileUpdateProposalApplyStarted
  delivery_semantics: transactional_event_log
  topic: events.user.profile_update_proposal
  payload_entity: ProfileUpdateProposal
  payload_fields:
  - id
  - personaId
  - status
  - version
  - applyActorPersonaId
  - applyRequestId
  - applyTraceId
  no_consumer_reason: apply 启动态与审核记录已在对象事务中持久化；事件只作为对象级审计日志保留，没有下游订阅方。
- name: ProfileUpdateProposalApplied
  delivery_semantics: transactional_event_log
  topic: events.user.profile_update_proposal
  payload_entity: ProfileUpdateProposal
  payload_fields:
  - id
  - personaId
  - status
  - version
  - resolvedAt
  - applyAuditId
  - rollbackDeadline
  no_consumer_reason: apply 终态与审核记录已在对象事务中持久化；事件只作为对象级审计日志保留，没有下游订阅方。
- name: ProfileUpdateProposalRollbackStarted
  delivery_semantics: transactional_event_log
  topic: events.user.profile_update_proposal
  payload_entity: ProfileUpdateProposal
  payload_fields:
  - id
  - personaId
  - status
  - version
  - applyAuditId
  - rollbackActorPersonaId
  - rollbackRequestId
  - rollbackTraceId
  no_consumer_reason: rollback 启动态与审核记录已在对象事务中持久化；事件只作为对象级审计日志保留，没有下游订阅方。
- name: ProfileUpdateProposalRollbackAborted
  delivery_semantics: transactional_event_log
  topic: events.user.profile_update_proposal
  payload_entity: ProfileUpdateProposal
  payload_fields:
  - id
  - personaId
  - status
  - version
  - applyAuditId
  no_consumer_reason: rollback 中止态与审核记录已在对象事务中持久化；事件只作为对象级审计日志保留，没有下游订阅方。
- name: ProfileUpdateProposalRolledBack
  delivery_semantics: transactional_event_log
  topic: events.user.profile_update_proposal
  payload_entity: ProfileUpdateProposal
  payload_fields:
  - id
  - personaId
  - status
  - version
  - resolvedAt
  - applyAuditId
  - rollbackAuditId
  no_consumer_reason: rollback 终态与审核记录已在对象事务中持久化；事件只作为对象级审计日志保留，没有下游订阅方。
- name: ProfileUpdateProposalRejected
  delivery_semantics: transactional_event_log
  topic: events.user.profile_update_proposal
  payload_entity: ProfileUpdateProposal
  payload_fields:
  - id
  - personaId
  - status
  - version
  - reviewedBy
  - resolvedAt
  no_consumer_reason: Proposal 拒绝态与审核记录已在对象事务中持久化；事件只作为对象级审计日志保留，没有下游订阅方。
- name: ProfileUpdateProposalExpired
  delivery_semantics: transactional_event_log
  topic: events.user.profile_update_proposal
  payload_entity: ProfileUpdateProposal
  payload_fields:
  - id
  - personaId
  - status
  - version
  - resolvedAt
  no_consumer_reason: Proposal 过期态与审核记录已在对象事务中持久化；事件只作为对象级审计日志保留，没有下游订阅方。
