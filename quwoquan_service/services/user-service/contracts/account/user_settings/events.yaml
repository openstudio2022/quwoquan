events:
- name: UserSettingsChanged
  delivery_semantics: transactional_event_log
  payload_entity: UserSettings
  payload_fields:
  - userId
  no_consumer_reason: The same PostgreSQL transaction stores the audit fact; no relay or independent settings-audit consumer
    is assembled.
