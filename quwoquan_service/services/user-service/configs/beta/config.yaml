# beta overrides (developer local cloud-client integration)
service:
  http:
    addr: ":18081"

postgres:
  dsn: "postgres://quwoquan:quwoquan@127.0.0.1:5432/quwoquan?sslmode=disable"

mongodb:
  uri: "mongodb://127.0.0.1:27017"

# beta：搜索索引写侧默认关闭，按需经 SEARCH_ES_* 注入开启。
es:
  enabled: false
  endpoints: []

redis:
  general:
    mode: standalone
    addr: "127.0.0.1:6379"
    db: 0

integration:
  external_interaction_base_url: "https://integration-service.local"
  otp:
    mode: fixed_test
