config:
  version: "v2026.07.12.0"
  min_image_version: "0.0.1"
  max_image_version: "9.9.9"

service:
  name: user-service

# Prod one-tap login resolves carrier credentials through the Aliyun provider.
