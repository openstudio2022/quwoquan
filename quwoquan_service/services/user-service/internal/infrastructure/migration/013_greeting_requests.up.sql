-- Code generated by codegen_storage. DO NOT EDIT.

CREATE TABLE IF NOT EXISTS greeting_requests (
    id                       UUID PRIMARY KEY,
    requester_sub_account_id VARCHAR(64) NOT NULL,
    target_sub_account_id    VARCHAR(64) NOT NULL,
    request_message          TEXT,
    status                   VARCHAR(20) NOT NULL,
    source                   VARCHAR(30) NOT NULL,
    promoted_conversation_id VARCHAR(64),
    expire_at                TIMESTAMPTZ,
    decision_at              TIMESTAMPTZ,
    created_at               TIMESTAMPTZ NOT NULL,
    updated_at               TIMESTAMPTZ NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_gr_inbox ON greeting_requests (target_sub_account_id, status, created_at DESC);

CREATE INDEX IF NOT EXISTS idx_gr_outbox ON greeting_requests (requester_sub_account_id, status, created_at DESC);

CREATE UNIQUE INDEX IF NOT EXISTS idx_gr_unique_pending ON greeting_requests (requester_sub_account_id, target_sub_account_id);

CREATE INDEX IF NOT EXISTS idx_gr_expire ON greeting_requests (expire_at);
