-- Code generated by codegen_storage. DO NOT EDIT.

CREATE TABLE IF NOT EXISTS user_settings (
    user_id                  VARCHAR(36) PRIMARY KEY,
    enable_push              BOOLEAN NOT NULL DEFAULT true,
    enable_marketing         BOOLEAN NOT NULL DEFAULT false,
    quiet_hours_start        TIME,
    quiet_hours_end          TIME,
    allow_stranger_msg       BOOLEAN NOT NULL DEFAULT true,
    profile_visibility       VARCHAR(16) NOT NULL DEFAULT 'public',
    content_language         VARCHAR(16),
    feed_preference          VARCHAR(16),
    assistant_enabled        BOOLEAN NOT NULL DEFAULT true,
    updated_at               TIMESTAMPTZ NOT NULL DEFAULT NOW()
    ,CONSTRAINT fk_user_settings_user_id FOREIGN KEY (user_id) REFERENCES user_profiles(user_id) ON DELETE CASCADE
);
