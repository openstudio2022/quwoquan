-- Code generated by codegen_storage. DO NOT EDIT.

CREATE TABLE IF NOT EXISTS contact_discovery_records (
    id                       VARCHAR(36) PRIMARY KEY,
    owner_account_id         VARCHAR(36) NOT NULL,
    hashed_phones            TEXT[] NOT NULL,
    matched_sub_account_ids  TEXT[],
    status                   VARCHAR(16) NOT NULL DEFAULT 'pending',
    match_count              INTEGER NOT NULL DEFAULT 0,
    expire_at                TIMESTAMPTZ NOT NULL,
    created_at               TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    completed_at             TIMESTAMPTZ
    ,CONSTRAINT fk_contact_discovery_records_owner_account_id FOREIGN KEY (owner_account_id) REFERENCES user_profiles(user_id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_contact_discovery_owner ON contact_discovery_records (owner_account_id);

CREATE INDEX IF NOT EXISTS idx_contact_discovery_expire ON contact_discovery_records (expire_at);

CREATE INDEX IF NOT EXISTS idx_contact_discovery_status ON contact_discovery_records (status);
