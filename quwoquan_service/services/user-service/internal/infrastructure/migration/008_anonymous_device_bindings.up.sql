-- Code generated by codegen_storage. DO NOT EDIT.

CREATE TABLE IF NOT EXISTS anonymous_device_bindings (
    id                       VARCHAR(64) PRIMARY KEY,
    owner_id                 VARCHAR(96) NOT NULL,
    install_id_hash          VARCHAR(256) NOT NULL,
    device_fingerprint_hash  VARCHAR(256) NOT NULL,
    platform                 VARCHAR(16) NOT NULL,
    app_version              VARCHAR(32),
    last_seen_at             TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    created_at               TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at               TIMESTAMPTZ NOT NULL DEFAULT NOW()
    ,CONSTRAINT fk_anonymous_device_bindings_owner_id FOREIGN KEY (owner_id) REFERENCES user_profiles(user_id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_anonymous_device_bindings_owner_id ON anonymous_device_bindings (owner_id);

CREATE INDEX IF NOT EXISTS idx_anonymous_device_bindings_install_id_hash ON anonymous_device_bindings (install_id_hash);

ALTER TABLE anonymous_device_bindings ADD CONSTRAINT uq_anonymous_device_bindings_fingerprint UNIQUE (device_fingerprint_hash);
