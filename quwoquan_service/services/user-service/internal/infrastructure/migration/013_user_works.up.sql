-- Code generated by codegen_storage. DO NOT EDIT.

CREATE TABLE IF NOT EXISTS user_works (
    id                       VARCHAR(64) PRIMARY KEY,
    user_id                  VARCHAR(96) NOT NULL,
    title                    VARCHAR(128) NOT NULL,
    cover_url                TEXT,
    work_type                VARCHAR(32) NOT NULL,
    ref_id                   VARCHAR(64) NOT NULL,
    sort_order               INTEGER NOT NULL DEFAULT 0,
    created_at               TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at               TIMESTAMPTZ NOT NULL DEFAULT NOW()
    ,CONSTRAINT fk_user_works_user_id FOREIGN KEY (user_id) REFERENCES user_profiles(user_id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_user_works_user_id ON user_works (user_id);

CREATE INDEX IF NOT EXISTS idx_user_works_user_sort ON user_works (user_id, sort_order);
