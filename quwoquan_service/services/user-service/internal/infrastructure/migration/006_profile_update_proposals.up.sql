-- Code generated by codegen_storage. DO NOT EDIT.

CREATE TABLE IF NOT EXISTS profile_update_proposals (
    id                       VARCHAR(36) PRIMARY KEY,
    user_id                  VARCHAR(36) NOT NULL,
    source                   VARCHAR(16) NOT NULL,
    proposed_changes         JSONB NOT NULL,
    status                   VARCHAR(16) NOT NULL,
    reviewed_by              VARCHAR(36),
    version                  INTEGER NOT NULL DEFAULT 1,
    created_at               TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    resolved_at              TIMESTAMPTZ
    ,CONSTRAINT fk_profile_update_proposals_user_id FOREIGN KEY (user_id) REFERENCES user_profiles(user_id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_proposals_user_status ON profile_update_proposals (user_id, status) WHERE status IN ('created', 'confirmed');
