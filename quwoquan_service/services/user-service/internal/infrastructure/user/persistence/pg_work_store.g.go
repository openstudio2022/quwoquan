// Code generated by codegen_storage. DO NOT EDIT.
package persistence

import (
	"context"
	"errors"
	"fmt"
	"time"

	"github.com/jackc/pgx/v5"
	"github.com/jackc/pgx/v5/pgxpool"

	model "quwoquan_service/services/user-service/internal/domain/user/model"
)

var _ = fmt.Sprintf
var _ = time.Now

// pgWorkStoreBase provides generated PostgreSQL CRUD for UserWork.
// Embed this in a public store type defined in _ext.go.
type pgWorkStoreBase struct {
	pool *pgxpool.Pool
}

const userWorkCols = `id, user_id, title, cover_url, work_type, ref_id, sort_order, created_at, updated_at`

func scanUserWork(row pgx.Row) (*model.UserWork, error) {
	e := &model.UserWork{}
	err := row.Scan(&e.ID, &e.UserID, &e.Title, &e.CoverURL, &e.WorkType, &e.RefID, &e.SortOrder, &e.CreatedAt, &e.UpdatedAt)
	if errors.Is(err, pgx.ErrNoRows) {
		return nil, nil
	}
	if err != nil {
		return nil, err
	}
	return e, nil
}

// FindByID retrieves a UserWork by primary key.
func (s *pgWorkStoreBase) FindByID(ctx context.Context, id string) (*model.UserWork, error) {
	row := s.pool.QueryRow(ctx, `SELECT `+userWorkCols+` FROM user_works WHERE id = $1`, id)
	return scanUserWork(row)
}

// Create inserts a new UserWork record.
func (s *pgWorkStoreBase) Create(ctx context.Context, e *model.UserWork) error {
	now := time.Now().UTC()
	e.CreatedAt = now
	e.UpdatedAt = now
	_, err := s.pool.Exec(ctx,
		`INSERT INTO user_works (id, user_id, title, cover_url, work_type, ref_id, sort_order, created_at, updated_at) VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)`,
		e.ID, e.UserID, e.Title, e.CoverURL, e.WorkType, e.RefID, e.SortOrder, e.CreatedAt, e.UpdatedAt)
	return err
}

// Update modifies an existing UserWork record (all non-PK columns).
func (s *pgWorkStoreBase) Update(ctx context.Context, e *model.UserWork) error {
	e.UpdatedAt = time.Now().UTC()
	tag, err := s.pool.Exec(ctx,
		`UPDATE user_works SET user_id=$2, title=$3, cover_url=$4, work_type=$5, ref_id=$6, sort_order=$7, created_at=$8, updated_at=$9 WHERE id = $1`,
		e.ID, e.UserID, e.Title, e.CoverURL, e.WorkType, e.RefID, e.SortOrder, e.CreatedAt, e.UpdatedAt)
	if err != nil {
		return err
	}
	if tag.RowsAffected() == 0 {
		return fmt.Errorf("UserWork not found: %s", e.ID)
	}
	return nil
}

// Delete removes a UserWork by primary key.
func (s *pgWorkStoreBase) Delete(ctx context.Context, id string) error {
	_, err := s.pool.Exec(ctx, `DELETE FROM user_works WHERE id = $1`, id)
	return err
}

// ListByUserID returns all UserWork records for the given foreign key.
func (s *pgWorkStoreBase) ListByUserID(ctx context.Context, fkID string) ([]model.UserWork, error) {
	rows, err := s.pool.Query(ctx,
		`SELECT `+userWorkCols+` FROM user_works WHERE user_id = $1 ORDER BY created_at DESC`, fkID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var result []model.UserWork
	for rows.Next() {
		var e model.UserWork
		if err := rows.Scan(&e.ID, &e.UserID, &e.Title, &e.CoverURL, &e.WorkType, &e.RefID, &e.SortOrder, &e.CreatedAt, &e.UpdatedAt); err != nil {
			return nil, err
		}
		result = append(result, e)
	}
	return result, rows.Err()
}
