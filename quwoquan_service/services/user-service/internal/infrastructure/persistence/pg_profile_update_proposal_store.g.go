// Code generated by codegen_storage. DO NOT EDIT.
package persistence

import (
	"context"
	"errors"
	"fmt"
	"time"

	"github.com/jackc/pgx/v5"
	"github.com/jackc/pgx/v5/pgxpool"

	model "quwoquan_service/services/user-service/internal/domain/user/model"
)

var _ = fmt.Sprintf
var _ = time.Now

// pgProfileUpdateProposalStoreBase provides generated PostgreSQL CRUD for ProfileUpdateProposal.
// Embed this in a public store type defined in _ext.go.
type pgProfileUpdateProposalStoreBase struct {
	pool *pgxpool.Pool
}

const profileUpdateProposalCols = `id, user_id, source, proposed_changes, status, reviewed_by, version, created_at, resolved_at`

func scanProfileUpdateProposal(row pgx.Row) (*model.ProfileUpdateProposal, error) {
	e := &model.ProfileUpdateProposal{}
	err := row.Scan(&e.ID, &e.UserID, &e.Source, &e.ProposedChanges, &e.Status, &e.ReviewedBy, &e.Version, &e.CreatedAt, &e.ResolvedAt)
	if errors.Is(err, pgx.ErrNoRows) {
		return nil, nil
	}
	if err != nil {
		return nil, err
	}
	return e, nil
}

// FindByID retrieves a ProfileUpdateProposal by primary key.
func (s *pgProfileUpdateProposalStoreBase) FindByID(ctx context.Context, id string) (*model.ProfileUpdateProposal, error) {
	row := s.pool.QueryRow(ctx, `SELECT `+profileUpdateProposalCols+` FROM profile_update_proposals WHERE id = $1`, id)
	return scanProfileUpdateProposal(row)
}

// Create inserts a new ProfileUpdateProposal record.
func (s *pgProfileUpdateProposalStoreBase) Create(ctx context.Context, e *model.ProfileUpdateProposal) error {
	now := time.Now().UTC()
	e.CreatedAt = now
	_, err := s.pool.Exec(ctx,
		`INSERT INTO profile_update_proposals (id, user_id, source, proposed_changes, status, reviewed_by, version, created_at, resolved_at) VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)`,
		e.ID, e.UserID, e.Source, e.ProposedChanges, e.Status, e.ReviewedBy, e.Version, e.CreatedAt, e.ResolvedAt)
	return err
}

// Update modifies an existing ProfileUpdateProposal record (all non-PK columns).
func (s *pgProfileUpdateProposalStoreBase) Update(ctx context.Context, e *model.ProfileUpdateProposal) error {
	tag, err := s.pool.Exec(ctx,
		`UPDATE profile_update_proposals SET user_id=$2, source=$3, proposed_changes=$4, status=$5, reviewed_by=$6, version=$7, created_at=$8, resolved_at=$9 WHERE id = $1`,
		e.ID, e.UserID, e.Source, e.ProposedChanges, e.Status, e.ReviewedBy, e.Version, e.CreatedAt, e.ResolvedAt)
	if err != nil {
		return err
	}
	if tag.RowsAffected() == 0 {
		return fmt.Errorf("ProfileUpdateProposal not found: %s", e.ID)
	}
	return nil
}

// Delete removes a ProfileUpdateProposal by primary key.
func (s *pgProfileUpdateProposalStoreBase) Delete(ctx context.Context, id string) error {
	_, err := s.pool.Exec(ctx, `DELETE FROM profile_update_proposals WHERE id = $1`, id)
	return err
}

// ListByUserID returns all ProfileUpdateProposal records for the given foreign key.
func (s *pgProfileUpdateProposalStoreBase) ListByUserID(ctx context.Context, fkID string) ([]model.ProfileUpdateProposal, error) {
	rows, err := s.pool.Query(ctx,
		`SELECT `+profileUpdateProposalCols+` FROM profile_update_proposals WHERE user_id = $1 ORDER BY created_at DESC`, fkID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var result []model.ProfileUpdateProposal
	for rows.Next() {
		var e model.ProfileUpdateProposal
		if err := rows.Scan(&e.ID, &e.UserID, &e.Source, &e.ProposedChanges, &e.Status, &e.ReviewedBy, &e.Version, &e.CreatedAt, &e.ResolvedAt); err != nil {
			return nil, err
		}
		result = append(result, e)
	}
	return result, rows.Err()
}
