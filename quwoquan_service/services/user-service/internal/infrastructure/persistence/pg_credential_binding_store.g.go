// Code generated by codegen_storage. DO NOT EDIT.
package persistence

import (
	"context"
	"errors"
	"fmt"

	"github.com/jackc/pgx/v5"
	"github.com/jackc/pgx/v5/pgxpool"

	model "quwoquan_service/services/user-service/internal/domain/user/model"
)

var _ = fmt.Sprintf

// pgCredentialBindingStoreBase provides generated PostgreSQL CRUD for CredentialBinding.
// Embed this in a public store type defined in _ext.go.
type pgCredentialBindingStoreBase struct {
	pool *pgxpool.Pool
}

const credentialBindingCols = `id, owner_id, credential_type, credential_key, display_label, is_active, bound_at, last_used_at`

func scanCredentialBinding(row pgx.Row) (*model.CredentialBinding, error) {
	e := &model.CredentialBinding{}
	err := row.Scan(&e.ID, &e.OwnerID, &e.CredentialType, &e.CredentialKey, &e.DisplayLabel, &e.IsActive, &e.BoundAt, &e.LastUsedAt)
	if errors.Is(err, pgx.ErrNoRows) {
		return nil, nil
	}
	if err != nil {
		return nil, err
	}
	return e, nil
}

// FindByID retrieves a CredentialBinding by primary key.
func (s *pgCredentialBindingStoreBase) FindByID(ctx context.Context, id string) (*model.CredentialBinding, error) {
	row := s.pool.QueryRow(ctx, `SELECT `+credentialBindingCols+` FROM credential_bindings WHERE id = $1`, id)
	return scanCredentialBinding(row)
}

// Create inserts a new CredentialBinding record.
func (s *pgCredentialBindingStoreBase) Create(ctx context.Context, e *model.CredentialBinding) error {
	_, err := s.pool.Exec(ctx,
		`INSERT INTO credential_bindings (id, owner_id, credential_type, credential_key, display_label, is_active, bound_at, last_used_at) VALUES ($1, $2, $3, $4, $5, $6, $7, $8)`,
		e.ID, e.OwnerID, e.CredentialType, e.CredentialKey, e.DisplayLabel, e.IsActive, e.BoundAt, e.LastUsedAt)
	return err
}

// Update modifies an existing CredentialBinding record (all non-PK columns).
func (s *pgCredentialBindingStoreBase) Update(ctx context.Context, e *model.CredentialBinding) error {
	tag, err := s.pool.Exec(ctx,
		`UPDATE credential_bindings SET owner_id=$2, credential_type=$3, credential_key=$4, display_label=$5, is_active=$6, bound_at=$7, last_used_at=$8 WHERE id = $1`,
		e.ID, e.OwnerID, e.CredentialType, e.CredentialKey, e.DisplayLabel, e.IsActive, e.BoundAt, e.LastUsedAt)
	if err != nil {
		return err
	}
	if tag.RowsAffected() == 0 {
		return fmt.Errorf("CredentialBinding not found: %s", e.ID)
	}
	return nil
}

// Delete removes a CredentialBinding by primary key.
func (s *pgCredentialBindingStoreBase) Delete(ctx context.Context, id string) error {
	_, err := s.pool.Exec(ctx, `DELETE FROM credential_bindings WHERE id = $1`, id)
	return err
}

// ListByOwnerID returns all CredentialBinding records for the given foreign key.
func (s *pgCredentialBindingStoreBase) ListByOwnerID(ctx context.Context, fkID string) ([]model.CredentialBinding, error) {
	rows, err := s.pool.Query(ctx,
		`SELECT `+credentialBindingCols+` FROM credential_bindings WHERE owner_id = $1 ORDER BY created_at DESC`, fkID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var result []model.CredentialBinding
	for rows.Next() {
		var e model.CredentialBinding
		if err := rows.Scan(&e.ID, &e.OwnerID, &e.CredentialType, &e.CredentialKey, &e.DisplayLabel, &e.IsActive, &e.BoundAt, &e.LastUsedAt); err != nil {
			return nil, err
		}
		result = append(result, e)
	}
	return result, rows.Err()
}
