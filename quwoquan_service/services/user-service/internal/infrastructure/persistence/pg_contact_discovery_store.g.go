// Code generated by codegen_storage. DO NOT EDIT.
package persistence

import (
	"context"
	"errors"
	"fmt"
	"time"

	"github.com/jackc/pgx/v5"
	"github.com/jackc/pgx/v5/pgxpool"

	model "quwoquan_service/services/user-service/internal/domain/user/model"
)

var _ = fmt.Sprintf
var _ = time.Now

// pgContactDiscoveryStoreBase provides generated PostgreSQL CRUD for ContactDiscoveryRecord.
// Embed this in a public store type defined in _ext.go.
type pgContactDiscoveryStoreBase struct {
	pool *pgxpool.Pool
}

const contactDiscoveryRecordCols = `id, owner_account_id, hashed_phones, matched_sub_account_ids, status, match_count, expire_at, created_at, completed_at`

func scanContactDiscoveryRecord(row pgx.Row) (*model.ContactDiscoveryRecord, error) {
	e := &model.ContactDiscoveryRecord{}
	err := row.Scan(&e.ID, &e.OwnerAccountID, &e.HashedPhones, &e.MatchedSubAccountIds, &e.Status, &e.MatchCount, &e.ExpireAt, &e.CreatedAt, &e.CompletedAt)
	if errors.Is(err, pgx.ErrNoRows) {
		return nil, nil
	}
	if err != nil {
		return nil, err
	}
	return e, nil
}

// FindByID retrieves a ContactDiscoveryRecord by primary key.
func (s *pgContactDiscoveryStoreBase) FindByID(ctx context.Context, id string) (*model.ContactDiscoveryRecord, error) {
	row := s.pool.QueryRow(ctx, `SELECT `+contactDiscoveryRecordCols+` FROM contact_discovery_records WHERE id = $1`, id)
	return scanContactDiscoveryRecord(row)
}

// Create inserts a new ContactDiscoveryRecord record.
func (s *pgContactDiscoveryStoreBase) Create(ctx context.Context, e *model.ContactDiscoveryRecord) error {
	now := time.Now().UTC()
	e.CreatedAt = now
	_, err := s.pool.Exec(ctx,
		`INSERT INTO contact_discovery_records (id, owner_account_id, hashed_phones, matched_sub_account_ids, status, match_count, expire_at, created_at, completed_at) VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)`,
		e.ID, e.OwnerAccountID, e.HashedPhones, e.MatchedSubAccountIds, e.Status, e.MatchCount, e.ExpireAt, e.CreatedAt, e.CompletedAt)
	return err
}

// Update modifies an existing ContactDiscoveryRecord record (all non-PK columns).
func (s *pgContactDiscoveryStoreBase) Update(ctx context.Context, e *model.ContactDiscoveryRecord) error {
	tag, err := s.pool.Exec(ctx,
		`UPDATE contact_discovery_records SET owner_account_id=$2, hashed_phones=$3, matched_sub_account_ids=$4, status=$5, match_count=$6, expire_at=$7, created_at=$8, completed_at=$9 WHERE id = $1`,
		e.ID, e.OwnerAccountID, e.HashedPhones, e.MatchedSubAccountIds, e.Status, e.MatchCount, e.ExpireAt, e.CreatedAt, e.CompletedAt)
	if err != nil {
		return err
	}
	if tag.RowsAffected() == 0 {
		return fmt.Errorf("ContactDiscoveryRecord not found: %s", e.ID)
	}
	return nil
}

// Delete removes a ContactDiscoveryRecord by primary key.
func (s *pgContactDiscoveryStoreBase) Delete(ctx context.Context, id string) error {
	_, err := s.pool.Exec(ctx, `DELETE FROM contact_discovery_records WHERE id = $1`, id)
	return err
}

// ListByOwnerAccountID returns all ContactDiscoveryRecord records for the given foreign key.
func (s *pgContactDiscoveryStoreBase) ListByOwnerAccountID(ctx context.Context, fkID string) ([]model.ContactDiscoveryRecord, error) {
	rows, err := s.pool.Query(ctx,
		`SELECT `+contactDiscoveryRecordCols+` FROM contact_discovery_records WHERE owner_account_id = $1 ORDER BY created_at DESC`, fkID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var result []model.ContactDiscoveryRecord
	for rows.Next() {
		var e model.ContactDiscoveryRecord
		if err := rows.Scan(&e.ID, &e.OwnerAccountID, &e.HashedPhones, &e.MatchedSubAccountIds, &e.Status, &e.MatchCount, &e.ExpireAt, &e.CreatedAt, &e.CompletedAt); err != nil {
			return nil, err
		}
		result = append(result, e)
	}
	return result, rows.Err()
}
