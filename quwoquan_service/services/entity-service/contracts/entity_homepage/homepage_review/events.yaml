events:
- name: HomepageReviewPublished
  delivery_semantics: transactional_outbox
  payload_entity: HomepageReview
  payload_fields:
  - reviewId
  - homepageId
  - authorPersonaId
  - rating
  - tagRefs
  - status
  - createdAt
  - version
- name: HomepageReviewUpdated
  delivery_semantics: transactional_outbox
  payload_entity: HomepageReview
  payload_fields:
  - reviewId
  - homepageId
  - authorPersonaId
  - rating
  - tagRefs
  - status
  - updatedAt
  - version
- name: HomepageReviewRemoved
  delivery_semantics: transactional_outbox
  payload_entity: HomepageReview
  payload_fields:
  - reviewId
  - homepageId
  - status
  - updatedAt
  - version
