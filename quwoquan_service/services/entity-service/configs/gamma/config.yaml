# gamma overrides (cloud integration env)
service:
  http:
    addr: ":18084"

redis:
  rec:
    mode: standalone
    addr: ""
    db: 0

# gamma enables the write-time index; endpoints/credentials come from SEARCH_ES_*.
es:
  enabled: true
  endpoints: []
  index: "quwoquan_objects"
  # gamma-local ES 为 amd64 模拟（R-S06-S-1），冷启动/单请求可超默认 800ms；
  # 放宽硬超时避免 EnsureIndex fail-fast 把服务打死，真集群另行校准。
  requestTimeoutMs: 5000
  shards: 1
  replicas: 1
