events:
- name: MediaImageReprocessRunStarted
  delivery_semantics: not_published
  payload_entity: MediaImageReprocessRun
  payload_fields:
  - runId
  - targetDerivativePolicyVersion
  - status
  - assetIds
  - startedAt
  no_consumer_reason: run 事实即受控运维审计记录；worker 直接读取 aggregate，不经事件投递。
- name: MediaImageReprocessRunPaused
  delivery_semantics: not_published
  payload_entity: MediaImageReprocessRun
  payload_fields:
  - runId
  - status
  - nextAssetIndex
  - processedCount
  - failedCount
  - pausedAt
  no_consumer_reason: run 事实即受控运维审计记录；worker 直接读取 aggregate，不经事件投递。
- name: MediaImageReprocessRunResumed
  delivery_semantics: not_published
  payload_entity: MediaImageReprocessRun
  payload_fields:
  - runId
  - status
  - nextAssetIndex
  - processedCount
  - failedCount
  - updatedAt
  no_consumer_reason: run 事实即受控运维审计记录；worker 直接读取 aggregate，不经事件投递。
- name: MediaImageReprocessRunCompleted
  delivery_semantics: not_published
  payload_entity: MediaImageReprocessRun
  payload_fields:
  - runId
  - status
  - processedCount
  - failedCount
  - completedAt
  no_consumer_reason: run 事实即受控运维审计记录；worker 直接读取 aggregate，不经事件投递。
- name: MediaImageReprocessRunRolledBack
  delivery_semantics: not_published
  payload_entity: MediaImageReprocessRun
  payload_fields:
  - runId
  - status
  - processedCount
  - failedCount
  - rolledBackAt
  no_consumer_reason: run 事实即受控运维审计记录；worker 直接读取 aggregate，不经事件投递。
