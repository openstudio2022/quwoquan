config:
  version: "v2026.07.21.0"
  min_image_version: "0.0.1"
  max_image_version: "9.9.9"

# Bounded local content-feedback lifecycle verification. This profile does not
# claim embedding generation or vector recall coverage; those require the
# externally provisioned content.embedding binding in the full workload.
embedding:
  enabled: false
  vector_recall_enabled: false
