# 兼容指引（已迁移到仓库根目录）

端云一体化特性目录已统一在仓库根维护：
- `../../changes/README.md`
- `../../changes/feature_catalog.yaml`

新建特性请在仓库根执行：

```bash
cd ..
bash scripts/new_feature_fullstack.sh "<slug>"
```

本目录仅保留兼容入口，避免旧路径失效。

