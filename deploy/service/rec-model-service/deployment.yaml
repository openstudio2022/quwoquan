apiVersion: apps/v1
kind: Deployment
metadata:
  name: rec-model-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rec-model-service
  template:
    metadata:
      labels:
        app: rec-model-service
    spec:
      containers:
        - name: rec-model-service
          image: quwoquan/rec-model-service:1.0.0
          env:
            - name: APP_ENV
              value: prod
            - name: SERVICE_NAME
              value: rec-model-service
            - name: CONFIG_VERSION
              value: v2026.02.28.0
            - name: IMAGE_VERSION
              value: "1.0.0"
            - name: CONFIG_ROOT
              value: /etc/qwq-config
