apiVersion: v1
kind: Service
metadata:
  name: content-service
  labels:
    app.kubernetes.io/name: content-service
spec:
  selector:
    app.kubernetes.io/name: content-service
  ports:
    - name: http
      protocol: TCP
      port: 18080
      targetPort: http
