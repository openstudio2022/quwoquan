apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  labels:
    app.kubernetes.io/name: recommendation-service
spec:
  selector:
    app.kubernetes.io/name: recommendation-service
  ports:
    - name: rec-http
      protocol: TCP
      port: 8000
      targetPort: rec-http
