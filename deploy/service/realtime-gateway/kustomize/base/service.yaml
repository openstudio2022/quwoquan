apiVersion: v1
kind: Service
metadata:
  name: realtime-gateway
  labels:
    app.kubernetes.io/name: realtime-gateway
spec:
  selector:
    app.kubernetes.io/name: realtime-gateway
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
