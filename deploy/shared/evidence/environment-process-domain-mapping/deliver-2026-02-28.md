# environment-process-domain-mapping deliver evidence (2026-02-28)

## Scope

- Feature: `runtime/runtime-config/config-provider-layering/environment-process-domain-mapping`
- Goal: domain-service 与组合部署解耦，`recommendation-service` 独立 Python 进程，门禁强制化

## Completed checkpoints

- D6: recommendation-service 配置分层加载实现（`default -> env -> version -> env vars`）
- D12: split-dev / integration-prod 拓扑回归校验脚本落地并接入 `make verify`
- D13: content-service 到 recommendation-service HTTP 契约稳定性测试补强
- D14: 拓扑变化不影响 recommendation API 语义/路由的回归校验落地
- D15: 交付证据包产出（本文件）

## Command evidence

- `make build && make test-contract` (in `quwoquan_service`) — PASS
  - `services/content-service/internal/infrastructure/recommendation/http_model_client_test.go` 新增契约稳定测试通过
- `make gate-full` (repo root) — PASS
  - `[verify] topology + contract regression` — OK
  - `[gate] running recommendation-service python tests` — `11 passed`

## Key implementation artifacts

- Python runtime contract + layered config
  - `quwoquan_service/services/rec-model-service/runtime_contract.py`
  - `quwoquan_service/services/rec-model-service/tests/test_runtime_contract.py`
- topology + contract regression gate
  - `scripts/verify_topology_contract_regression.sh`
- recommendation deploy/config naming alignment
  - `deploy/service/recommendation-service/deployment.yaml`
  - `releases/config/recommendation-service/v2026.02.28.0.yaml`
- content -> recommendation HTTP contract stability
  - `quwoquan_service/services/content-service/internal/infrastructure/recommendation/http_model_client_test.go`
